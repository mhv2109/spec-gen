# Drift Specification

> Generated by spec-gen v1.0.0 on 2026-03-25 22:10
> Source files: src/core/drift/git-diff.ts, src/core/drift/spec-mapper.ts, src/types/index.ts

## Purpose

Identifies discrepancies between code changes and specification documentation, ensuring
documentation remains accurate and up-to-date.

## Entities

### GitDiffOptions

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Configuration options for generating Git
diffs

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| rootPath | string | Root directory of the Git repository |
| baseRef | string | Git reference to compare against |
| pathFilter | string[] | Optional array of paths to include in diff |
| includeUnstaged | boolean | Whether to include unstaged changes |

### GitDiffResult

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Result of a Git diff operation

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| resolvedBase | string | The resolved base reference used for comparison |
| files | ChangedFile[] | Array of changed files with their status |
| hasUnstagedChanges | boolean | Whether there are unstaged changes |
| currentBranch | string | Name of the current Git branch |

**Relationships:**

- contains ChangedFile (GitDiffResult contains multiple ChangedFile entities)

### ChangedFile

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a file that has changed in a
Git repository

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| path | string | Path to the file |
| status | string | Status of the file (added, modified, deleted, renamed) |
| oldPath | string | Original path if the file was renamed |
| isTest | boolean | Whether the file is a test file |
| isConfig | boolean | Whether the file is a configuration file |
| isGenerated | boolean | Whether the file is a generated file |
| extension | string | File extension |

### GitDiff

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents the diff information between
the current working tree and a base reference in a Git repository.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| rootPath | string | The root path of the Git repository |
| baseRef | string | The base reference (branch, commit, or tag) to compare against |
| includeUnstaged | boolean | Whether to include unstaged changes in the diff |
| maxChars | number | The maximum number of characters to include in the diff output |

**Relationships:**

- has-many ChangedFile (A GitDiff can contain multiple ChangedFiles)

### SpecMapping

Represents the mapping between a domain and its associated source files and specifications.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| domain | string | The domain name |
| specPath | string | Path to the specification file |
| declaredSourceFiles | string[] | Source files explicitly declared in the spec header |
| inferredSourceFiles | string[] | Source files inferred from the spec content |
| allSourceFiles | string[] | Combined list of declared and inferred source files |
| requirements | string[] | List of requirements extracted from the spec |
| entities | string[] | List of entities extracted from the spec |

### SpecMap

Represents a bidirectional map between domains and source files, along with statistics.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| byDomain | Map<string, SpecMapping> | Map of domains to their specifications |
| byFile | Map<string, string[]> | Map of source files to their associated domains |
| domainCount | number | Total number of domains |
| totalMappedFiles | number | Total number of source files mapped |

**Relationships:**

- contains SpecMapping (SpecMap contains multiple SpecMapping instances)

### SpecMapperOptions

Options for configuring the SpecMapper.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| rootPath | string | Root path of the project |
| openspecPath | string | Path to the OpenSpec directory |
| repoStructurePath | string | Optional path to the repository structure file |

### RepoStructureDomain

Represents a domain in the repository structure.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the domain |
| files | string[] | List of files associated with the domain |
| suggestedSpecPath | string | Suggested path for the specification file |

### DriftIssue

An issue detected during drift analysis, indicating discrepancies between code and specifications.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the issue |
| kind | DriftIssueKind | Type of drift issue |
| severity | DriftSeverity | Severity level of the issue |
| message | string | Description of the issue |
| filePath | string | Path to the affected file |
| domain | string | Domain affected by the issue |
| specPath | string | Path to the related specification |
| changedLines | object | Lines added and removed |
| suggestion | string | Suggested resolution for the issue |

## Requirements

### Requirement: GitDiffOptionsValidation

The system SHALL validate GitDiffOptions according to these rules:
- rootPath must be a valid directory path
- baseRef must be a valid Git reference
- includeUnstaged must be a boolean

#### Scenario: ConfigureGitDiffWithPathFilter
- **GIVEN** A Git repository with multiple files
- **WHEN** GitDiffOptions is created with pathFilter
- **THEN** Only files matching the path filter are included in the diff

### Requirement: GitDiffResultValidation

The system SHALL validate GitDiffResult according to these rules:
- resolvedBase must be a valid Git reference
- files must be an array of ChangedFile objects
- hasUnstagedChanges must be a boolean
- currentBranch must be a string

#### Scenario: GenerateGitDiffResult
- **GIVEN** A Git repository with changes
- **WHEN** gitDiff is called with valid options
- **THEN** GitDiffResult is returned with the correct files and status

### Requirement: ChangedFileValidation

The system SHALL validate ChangedFile according to these rules:
- path must be a valid file path
- status must be one of: added, modified, deleted, renamed
- oldPath must be a valid file path if status is renamed
- isTest, isConfig, isGenerated must be booleans
- extension must be a string

#### Scenario: ClassifyATestFile
- **GIVEN** A file path matching a test pattern
- **WHEN** classifyFile is called
- **THEN** isTest is true and isConfig/isGenerated are false

### Requirement: GitDiffValidation

The system SHALL validate GitDiff according to these rules:
- rootPath must be a valid directory path
- baseRef must be a valid Git reference (branch, commit, or tag)
- maxChars must be a positive integer

#### Scenario: GetChangedFiles
- **GIVEN** A Git repository with changes between the working tree and a base reference
- **WHEN** getChangedFiles() is called with the rootPath and baseRef
- **THEN** Returns a list of ChangedFiles with their respective additions and deletions

#### Scenario: GetFileDiff
- **GIVEN** A Git repository with changes to a specific file
- **WHEN** getFileDiff() is called with the rootPath, filePath, and baseRef
- **THEN** Returns the unified diff content for the specified file, truncated to maxChars if necessary

### Requirement: DriftIssueValidation

The system SHALL validate DriftIssue according to these rules:
- kind must be one of the defined DriftIssueKind values
- severity must be one of the defined DriftSeverity values

#### Scenario: DriftDetection
- **GIVEN** Project with existing specifications
- **WHEN** verify command is executed
- **THEN** DriftIssue objects are created for any detected discrepancies

### Requirement: Classifyfile

The system SHALL classifies a file based on its path and name to determine if it is a test, configuration, or generated file.

#### Scenario: TestFileClassification
- **GIVEN** A file path matching a test pattern
- **WHEN** classifyFile is called
- **THEN** The file is classified as a test file

#### Scenario: ConfigFileClassification
- **GIVEN** A file path matching a config pattern
- **WHEN** classifyFile is called
- **THEN** The file is classified as a config file

#### Scenario: GeneratedFileClassification
- **GIVEN** A file path matching a generated pattern
- **WHEN** classifyFile is called
- **THEN** The file is classified as a generated file

### Requirement: Isskippablefile

The system SHALL determines if a file should be skipped based on its extension or name.

#### Scenario: SkippableFile
- **GIVEN** A file with a skippable extension or name
- **WHEN** isSkippableFile is called
- **THEN** The function returns true

#### Scenario: NonSkippableFile
- **GIVEN** A file with a non-skippable extension or name
- **WHEN** isSkippableFile is called
- **THEN** The function returns false

### Requirement: Isgitrepository

The system SHALL checks if the given path is a git repository.

#### Scenario: ValidGitRepository
- **GIVEN** A path containing a .git directory
- **WHEN** isGitRepository is called
- **THEN** The function returns true

#### Scenario: InvalidGitRepository
- **GIVEN** A path without a .git directory
- **WHEN** isGitRepository is called
- **THEN** The function returns false

### Requirement: Getcurrentbranch

The system SHALL retrieves the current branch name of the git repository.

#### Scenario: CurrentBranchRetrieved
- **GIVEN** A valid Git repository
- **WHEN** getCurrentBranch is called
- **THEN** The current branch name is returned

#### Scenario: ErrorHandling
- **GIVEN** An invalid Git repository
- **WHEN** getCurrentBranch is called
- **THEN** An error is logged and 'unknown' is returned

### Requirement: Validategitref

The system SHALL validates a user-supplied git reference to prevent argument injection.

#### Scenario: ValidGitReference
- **GIVEN** A valid Git reference
- **WHEN** validateGitRef is called
- **THEN** No error is thrown

#### Scenario: InvalidGitReference
- **GIVEN** An invalid Git reference
- **WHEN** validateGitRef is called
- **THEN** An error is thrown

### Requirement: Resolvebaseref

The system SHALL resolves a base reference for git operations, falling back to common defaults if necessary.

#### Scenario: PreferredReferenceResolved
- **GIVEN** A valid preferred reference
- **WHEN** resolveBaseRef is called
- **THEN** The preferred reference is returned

#### Scenario: DefaultReferenceResolved
- **GIVEN** An invalid preferred reference
- **WHEN** resolveBaseRef is called
- **THEN** A default reference (main, master, or HEAD~1) is returned

#### Scenario: EmptyTreeReferenceResolved
- **GIVEN** No valid references available
- **WHEN** resolveBaseRef is called
- **THEN** The empty tree SHA is returned

### Requirement: Parsegitstatus

The system SHALL parses a git status character into a changedfile status.

#### Scenario: AddedStatus
- **GIVEN** A status character 'A'
- **WHEN** parseGitStatus is called
- **THEN** The status 'added' is returned

#### Scenario: DeletedStatus
- **GIVEN** A status character 'D'
- **WHEN** parseGitStatus is called
- **THEN** The status 'deleted' is returned

#### Scenario: ModifiedStatus
- **GIVEN** A status character 'M'
- **WHEN** parseGitStatus is called
- **THEN** The status 'modified' is returned

#### Scenario: RenamedStatus
- **GIVEN** A status character 'R'
- **WHEN** parseGitStatus is called
- **THEN** The status 'renamed' is returned

#### Scenario: CopiedStatus
- **GIVEN** A status character 'C'
- **WHEN** parseGitStatus is called
- **THEN** The status 'added' is returned

#### Scenario: UnknownStatus
- **GIVEN** An unknown status character
- **WHEN** parseGitStatus is called
- **THEN** The status 'modified' is returned

### Requirement: Parsenamestatus

The system SHALL parses git diff --name-status output into file entries.

#### Scenario: ParseAddedFile
- **GIVEN** Git diff output with an added file
- **WHEN** parseNameStatus is called
- **THEN** An entry with status 'added' is returned

#### Scenario: ParseDeletedFile
- **GIVEN** Git diff output with a deleted file
- **WHEN** parseNameStatus is called
- **THEN** An entry with status 'deleted' is returned

#### Scenario: ParseModifiedFile
- **GIVEN** Git diff output with a modified file
- **WHEN** parseNameStatus is called
- **THEN** An entry with status 'modified' is returned

#### Scenario: ParseRenamedFile
- **GIVEN** Git diff output with a renamed file
- **WHEN** parseNameStatus is called
- **THEN** An entry with status 'renamed' and oldPath is returned

### Requirement: Parsenumstat

The system SHALL parses git numstat output to extract file change statistics.

#### Scenario: ValidNumstatOutput
- **GIVEN** A valid git numstat output string
- **WHEN** parseNumstat is called
- **THEN** A map of file paths to their addition and deletion counts is returned

#### Scenario: BinaryFileEntry
- **GIVEN** A git numstat output string with a binary file entry
- **WHEN** parseNumstat is called
- **THEN** The binary file entry is parsed with zero additions and deletions

#### Scenario: RenamedFileEntry
- **GIVEN** A git numstat output string with a renamed file entry
- **WHEN** parseNumstat is called
- **THEN** The renamed file entry is parsed and the new file path is used

### Requirement: Getfilediff

The system SHALL retrieves the unified diff content for a specific file against a base reference.

#### Scenario: SuccessfulThreeDotDiff
- **GIVEN** A valid root path, file path, and base reference
- **WHEN** getFileDiff is called
- **THEN** The unified diff content is returned

#### Scenario: FailedThreeDotDiff
- **GIVEN** A valid root path, file path, and base reference
- **WHEN** getFileDiff is called and the three-dot diff fails
- **THEN** The two-dot diff is attempted and the unified diff content is returned if successful

#### Scenario: FailedTwoDotDiff
- **GIVEN** A valid root path, file path, and base reference
- **WHEN** getFileDiff is called and both three-dot and two-dot diffs fail
- **THEN** The unstaged/staged diff is attempted and the unified diff content is returned if successful

#### Scenario: DiffExceedsMaxchars
- **GIVEN** A valid root path, file path, and base reference with a diff that exceeds maxChars
- **WHEN** getFileDiff is called
- **THEN** The diff content is truncated and a truncated message is appended

### Requirement: Getchangedfiles

The system SHALL retrieves the list of changed files between the working tree and a base reference.

#### Scenario: SuccessfulChangedFilesRetrieval
- **GIVEN** Valid GitDiffOptions
- **WHEN** getChangedFiles is called
- **THEN** The list of changed files is returned

#### Scenario: FailedChangedFilesRetrieval
- **GIVEN** Invalid GitDiffOptions
- **WHEN** getChangedFiles is called
- **THEN** An error is thrown

### Requirement: Parsespecheader

The system SHALL extracts source files and generation date from a spec file header.

#### Scenario: ValidHeaderWithSourceFiles
- **GIVEN** Spec content with 'Source files:' line
- **WHEN** parseSpecHeader is called
- **THEN** Returns array of source files and null date if not present

#### Scenario: HeaderWithGenerationDate
- **GIVEN** Spec content with 'Generated on' line
- **WHEN** parseSpecHeader is called
- **THEN** Returns the parsed date along with source files

### Requirement: Parsespecreferences

The system SHALL extracts file references, requirements, and entities from spec content.

#### Scenario: SpecWithImplementationBlock
- **GIVEN** Spec content with 'Implementation:' section
- **WHEN** parseSpecReferences is called
- **THEN** Returns files referenced in implementation section

#### Scenario: SpecWithRequirementsSection
- **GIVEN** Spec content with 'Requirement:' headings
- **WHEN** parseSpecReferences is called
- **THEN** Returns array of requirement names

#### Scenario: SpecWithEntitiesSection
- **GIVEN** Spec content with 'Entities' section
- **WHEN** parseSpecReferences is called
- **THEN** Returns array of entity names

### Requirement: Inferdomainfrompath

The system SHALL infers a domain name from a file path based on known domains.

#### Scenario: FileInDomainDirectory
- **GIVEN** File path containing a known domain name
- **WHEN** inferDomainFromPath is called
- **THEN** Returns the matching domain name

#### Scenario: FileWithDomainPrefix
- **GIVEN** File name starting with a known domain
- **WHEN** inferDomainFromPath is called
- **THEN** Returns the matching domain name

#### Scenario: NoMatchingDomain
- **GIVEN** File path with no known domain references
- **WHEN** inferDomainFromPath is called
- **THEN** Returns null

### Requirement: Buildspecmap

The system SHALL builds a bidirectional mapping between spec domains and source files.

#### Scenario: ValidSpecsDirectory
- **GIVEN** Directory with valid spec files
- **WHEN** buildSpecMap is called
- **THEN** Returns complete SpecMap with domain and file mappings

#### Scenario: MissingSpecsDirectory
- **GIVEN** Non-existent specs directory
- **WHEN** buildSpecMap is called
- **THEN** Returns empty SpecMap

#### Scenario: WithRepoStructureFile
- **GIVEN** Valid repo structure file path
- **WHEN** buildSpecMap is called
- **THEN** Includes additional file mappings from repo structure

### Requirement: Getspeccontent

The system SHALL loads and returns spec content for a given domain.

#### Scenario: ExistingDomainSpec
- **GIVEN** Valid domain with spec file
- **WHEN** getSpecContent is called
- **THEN** Returns spec content truncated to maxChars

#### Scenario: NonExistentDomain
- **GIVEN** Domain not in specMap
- **WHEN** getSpecContent is called
- **THEN** Returns null

#### Scenario: MissingSpecFile
- **GIVEN** Domain with missing spec file
- **WHEN** getSpecContent is called
- **THEN** Returns null

### Requirement: Isspecrelevantchange

The system SHALL determines if a file change is relevant to specification documentation.

#### Scenario: RelevantChange
- **GIVEN** A file change that affects documented functionality
- **WHEN** isSpecRelevantChange is called
- **THEN** Returns true

#### Scenario: IrrelevantChange
- **GIVEN** A file change that doesn't affect documented functionality
- **WHEN** isSpecRelevantChange is called
- **THEN** Returns false

### Requirement: Computeseverity

The system SHALL calculates the severity of a drift issue based on its type and magnitude.

#### Scenario: HighSeverity
- **GIVEN** A significant change to critical functionality
- **WHEN** computeSeverity is called
- **THEN** Returns high severity

#### Scenario: LowSeverity
- **GIVEN** A minor change to non-critical functionality
- **WHEN** computeSeverity is called
- **THEN** Returns low severity

### Requirement: Extractchangedspecdomains

The system SHALL identifies specification domains affected by code changes.

#### Scenario: DomainsExtracted
- **GIVEN** Files changed that belong to specific domains
- **WHEN** extractChangedSpecDomains is called
- **THEN** Returns set of affected domain names

#### Scenario: NoDomains
- **GIVEN** Files changed that don't belong to any domain
- **WHEN** extractChangedSpecDomains is called
- **THEN** Returns empty set

### Requirement: Detectgaps

The system SHALL identifies code changes that lack corresponding specification updates.

#### Scenario: GapsDetected
- **GIVEN** Code changes in documented domains without spec updates
- **WHEN** detectGaps is called
- **THEN** Returns array of gap issues

#### Scenario: NoGaps
- **GIVEN** Code changes with corresponding spec updates
- **WHEN** detectGaps is called
- **THEN** Returns empty array

### Requirement: Detectstalespecs

The system SHALL identifies specifications that reference deleted or heavily modified files.

#### Scenario: StaleSpecsDetected
- **GIVEN** Specs referencing deleted or heavily modified files
- **WHEN** detectStaleSpecs is called
- **THEN** Returns array of stale spec issues

#### Scenario: NoStaleSpecs
- **GIVEN** Specs referencing existing files
- **WHEN** detectStaleSpecs is called
- **THEN** Returns empty array

### Requirement: Detectuncoveredfiles

The system SHALL identifies new files that lack specification coverage.

#### Scenario: UncoveredFilesDetected
- **GIVEN** New files without specification coverage
- **WHEN** detectUncoveredFiles is called
- **THEN** Returns array of uncovered file issues

#### Scenario: NoUncoveredFiles
- **GIVEN** New files with specification coverage
- **WHEN** detectUncoveredFiles is called
- **THEN** Returns empty array

### Requirement: Detectorphanedspecs

The system SHALL identifies specifications that reference non-existent files.

#### Scenario: OrphanedSpecsDetected
- **GIVEN** Specs referencing non-existent files
- **WHEN** detectOrphanedSpecs is called
- **THEN** Returns array of orphaned spec issues

#### Scenario: NoOrphanedSpecs
- **GIVEN** Specs referencing existing files
- **WHEN** detectOrphanedSpecs is called
- **THEN** Returns empty array

### Requirement: Extractchangedadrids

The system SHALL identifies adr ids affected by code changes.

#### Scenario: AdrIdsExtracted
- **GIVEN** Files changed that reference ADRs
- **WHEN** extractChangedADRIds is called
- **THEN** Returns set of affected ADR IDs

#### Scenario: NoAdrIds
- **GIVEN** Files changed that don't reference any ADRs
- **WHEN** extractChangedADRIds is called
- **THEN** Returns empty set

### Requirement: Detectadrgaps

The system SHALL identifies code changes that lack corresponding adr updates.

#### Scenario: AdrGapsDetected
- **GIVEN** Code changes in domains referenced by ADRs without ADR updates
- **WHEN** detectADRGaps is called
- **THEN** Returns array of ADR gap issues

#### Scenario: NoAdrGaps
- **GIVEN** Code changes with corresponding ADR updates
- **WHEN** detectADRGaps is called
- **THEN** Returns empty array

### Requirement: Detectadrorphaned

The system SHALL identifies adrs that reference non-existent domains.

#### Scenario: OrphanedAdrsDetected
- **GIVEN** ADRs referencing non-existent domains
- **WHEN** detectADROrphaned is called
- **THEN** Returns array of orphaned ADR issues

#### Scenario: NoOrphanedAdrs
- **GIVEN** ADRs referencing existing domains
- **WHEN** detectADROrphaned is called
- **THEN** Returns empty array

### Requirement: Enhancegapswithllm

The system SHALL uses an llm to analyze and classify gap issues for better accuracy.

#### Scenario: GapsEnhanced
- **GIVEN** Gap issues to be analyzed by LLM
- **WHEN** enhanceGapsWithLLM is called
- **THEN** Returns enhanced array of gap issues

#### Scenario: NoEnhancement
- **GIVEN** No gap issues to analyze
- **WHEN** enhanceGapsWithLLM is called
- **THEN** Returns original array of gap issues

### Requirement: Detectdrift

The system SHALL runs all drift detection algorithms and produces a combined result.

#### Scenario: DriftDetected
- **GIVEN** Code changes that cause drift issues
- **WHEN** detectDrift is called
- **THEN** Returns combined drift result with issues

#### Scenario: NoDrift
- **GIVEN** Code changes without drift issues
- **WHEN** detectDrift is called
- **THEN** Returns empty drift result


## Sub-components

> `DriftDetector` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: Extractchangedspecdomains

> Implements: `extractChangedSpecDomains`

Identifies specification domains affected by code changes in a changeset.

#### Requirement: Extractdomains

The system SHALL the function shall extract domain names from specification files that were modified in the changeset.

#### Scenario: Extractdomainsfrommodifiedspecs
- **GIVEN** A changeset containing modified specification files
- **WHEN** The function is called with the changeset and specification directory path
- **THEN** The function SHALL return a set of domain names corresponding to the modified specifications

### Sub-component: Detectgaps

> Implements: `detectGaps`

Identifies code changes that lack corresponding specification updates.

#### Requirement: Detectgaps

The system SHALL the function shall detect gaps where code changes in a domain do not have corresponding specification updates.

#### Scenario: Detectgapsinchangeddomains
- **GIVEN** A changeset with code changes in domains that have not been updated in specifications
- **WHEN** The function is called with the changeset, specification map, and changed domain names
- **THEN** The function SHALL return an array of drift issues indicating the gaps

### Sub-component: Detectstalespecs

> Implements: `detectStaleSpecs`

Identifies specifications that reference files that have been deleted or heavily modified.

#### Requirement: Detectstalespecs

The system SHALL the function shall detect specifications that reference files that have been deleted or heavily modified.

#### Scenario: Detectstalespecsfordeletedfiles
- **GIVEN** A changeset with deleted files referenced in specifications
- **WHEN** The function is called with the changeset and specification map
- **THEN** The function SHALL return an array of drift issues indicating the stale specifications

### Sub-component: Detectuncoveredfiles

> Implements: `detectUncoveredFiles`

Identifies new files that do not map to any specification domain.

#### Requirement: Detectuncoveredfiles

The system SHALL the function shall detect new files that do not map to any specification domain.

#### Scenario: Detectuncoveredfilesinchangeset
- **GIVEN** A changeset with new files not mapped to any specification domain
- **WHEN** The function is called with the changeset, specification map, and specification directory path
- **THEN** The function SHALL return an array of drift issues indicating the uncovered files

### Sub-component: Detectorphanedspecs

> Implements: `detectOrphanedSpecs`

Identifies specifications that declare source files that do not exist on disk.

#### Requirement: Detectorphanedspecs

The system SHALL the function shall detect specifications that declare source files that do not exist on disk.

#### Scenario: Detectorphanedspecsfornonexistentfiles
- **GIVEN** A specification map with specifications referencing non-existent files
- **WHEN** The function is called with the specification map and root path
- **THEN** The function SHALL return an array of drift issues indicating the orphaned specifications

### Sub-component: Extractchangedadrids

> Implements: `extractChangedADRIds`

Identifies ADR IDs affected by code changes in a changeset.

#### Requirement: Extractadrids

The system SHALL the function shall extract adr ids from adr files that were modified in the changeset.

#### Scenario: Extractadridsfrommodifiedadrs
- **GIVEN** A changeset containing modified ADR files
- **WHEN** The function is called with the changeset and ADR directory path
- **THEN** The function SHALL return a set of ADR IDs corresponding to the modified ADRs

### Sub-component: Detectadrgaps

> Implements: `detectADRGaps`

Identifies code changes that lack corresponding ADR updates.

#### Requirement: Detectadrgaps

The system SHALL the function shall detect gaps where code changes in domains referenced by adrs do not have corresponding adr updates.

#### Scenario: Detectadrgapsinchangeddomains
- **GIVEN** A changeset with code changes in domains referenced by ADRs that have not been updated in ADRs
- **WHEN** The function is called with the changeset, ADR map, specification map, changed ADR IDs, and ADR
directory path
- **THEN** The function SHALL return an array of drift issues indicating the ADR gaps

### Sub-component: Detectadrorphaned

> Implements: `detectADROrphaned`

Identifies ADRs that reference domains that no longer exist in specifications.

#### Requirement: Detectadrorphaned

The system SHALL the function shall detect adrs that reference domains that no longer exist in specifications.

#### Scenario: Detectadrorphanedfornonexistentdomains
- **GIVEN** An ADR map with ADRs referencing domains that no longer exist in specifications
- **WHEN** The function is called with the ADR map and specification map
- **THEN** The function SHALL return an array of drift issues indicating the orphaned ADRs

### Sub-component: Enhancegapswithllm

> Implements: `enhanceGapsWithLLM`

Post-processes gap issues using an LLM to determine if code changes are relevant to specifications.

#### Requirement: Enhancegapswithllm

The system SHALL the function shall post-process gap issues using an llm to determine if code changes are relevant to specifications.

#### Scenario: Enhancegapswithllmforrelevantchanges
- **GIVEN** An array of gap issues and an LLM
- **WHEN** The function is called with the gap issues, LLM, and other necessary parameters
- **THEN** The function SHALL return an array of enhanced drift issues indicating the relevance of code changes
to specifications

### Sub-component: Isspecrelevantchange

> Implements: `isSpecRelevantChange`

Determines if a changed file is relevant for specification drift detection.

#### Requirement: Isspecrelevantchange

The system SHALL the function shall determine if a changed file is relevant for specification drift detection.

#### Scenario: Determinespecrelevanceforchangedfile
- **GIVEN** A changed file
- **WHEN** The function is called with the changed file and specification directory path
- **THEN** The function SHALL return true if the file is relevant for specification drift detection, otherwise
false

## Technical Notes

- **Implementation**: `src/core/drift/git-diff.ts, src/core/drift/spec-mapper.ts, src/types/index.ts`
- **Dependencies**: node:child_process, node:fs/promises, node:path, node:util, ChangedFile, logger, DIFF_MAX_CHARS, fs/promises, path, SpecMapping, SpecMap, ADRMap, LLMProvider
