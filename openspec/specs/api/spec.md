# API Specification

> Generated by spec-gen v1.0.0 on 2026-03-25 21:54

## Purpose

This document specifies the HTTP API exposed by the system.

## Requirements

### Requirement: APIAuthentication

The API SHALL require authentication via: None, API keys for LLM providers (ANTHROPIC_API_KEY, OPENAI_API_KEY, GEMINI_API_KEY, or OPENAI_COMPAT_API_KEY), API key (ANTHROPIC_API_KEY, OPENAI_API_KEY, GEMINI_API_KEY, OPENAI_COMPAT_API_KEY), None (uses environment variables for LLM API keys), Environment variables (ANTHROPIC_API_KEY or OPENAI_API_KEY)

#### Scenario: AuthenticatedRequest
- **GIVEN** a request with valid authentication credentials
- **WHEN** the request is processed
- **THEN** the request is authenticated successfully

#### Scenario: UnauthenticatedRequest
- **GIVEN** a request without authentication
- **WHEN** accessing a protected endpoint
- **THEN** the response status is 401 Unauthorized

### Requirement: Postconfiguration

The API SHALL support `POST /api/init` to initialize a new spec-gen configuration for a project.

**Request:**

```json
{
  "force": "boolean",
  "openspecPath": "string",
  "rootPath": "string",
  "configPath": "string",
  "onProgress": "ProgressCallback"
}
```

**Response:**

```json
{
  "configPath": "string",
  "openspecPath": "string",
  "projectType": "string",
  "created": "boolean"
}
```

#### Scenario: InitializeNewConfiguration
- **GIVEN** A project directory without existing spec-gen configuration
- **WHEN** POST /api/init is called with default options
- **THEN** 201 Created with paths to new config and openspec directory
- **AND** The response includes the detected project type
- **AND** The 'created' field is true

#### Scenario: SkipInitialization
- **GIVEN** A project directory with existing spec-gen configuration
- **WHEN** POST /api/init is called without force option
- **THEN** 200 OK with paths to existing config and openspec directory
- **AND** The 'created' field is false

### Requirement: CliCommandconfiguration

The API SHALL support `CLI Command spec-gen init` to initialize spec-gen in the current project by detecting project type, creating configuration, and preparing for analysis.

**Request:**

```json
{
  "options": {
    "force": "boolean",
    "openspec-path": "string"
  }
}
```

**Response:**

```json
{
  "status": "string",
  "configPath": "string",
  "openspecPath": "string",
  "projectType": "string"
}
```

#### Scenario: InitializeWithDefaults
- **GIVEN** A valid project directory with a supported manifest file
- **WHEN** The command 'spec-gen init' is executed
- **THEN** A configuration file is created and the project is prepared for analysis
- **AND** The project type is detected
- **AND** The openspec/ directory structure is created if it doesn't exist
- **AND** The .gitignore file is updated to exclude .spec-gen/

#### Scenario: OverwriteExistingConfiguration
- **GIVEN** An existing .spec-gen/config.json file
- **WHEN** The command 'spec-gen init --force' is executed
- **THEN** The existing configuration is overwritten with new settings
- **AND** The project type is re-detected
- **AND** The openspec/ directory structure is recreated if necessary

#### Scenario: CustomOpenspecPath
- **GIVEN** A valid project directory
- **WHEN** The command 'spec-gen init --openspec-path ./docs/specs' is executed
- **THEN** The configuration is created with the custom openspec path
- **AND** The openspec/ directory structure is created at the specified path

### Requirement: Postanalysis

The API SHALL support `POST /api/analyze` to analyze project codebase to generate analysis artifacts.

**Request:**

```json
{
  "maxFiles": "number",
  "includePatterns": "string[]",
  "excludePatterns": "string[]",
  "force": "boolean",
  "outputPath": "string",
  "rootPath": "string",
  "configPath": "string",
  "onProgress": "ProgressCallback"
}
```

**Response:**

```json
{
  "repoMap": "RepositoryMap",
  "depGraph": "DependencyGraphResult",
  "artifacts": "AnalysisArtifacts",
  "duration": "number"
}
```

#### Scenario: SuccessfulAnalysis
- **GIVEN** A project with code to analyze
- **WHEN** POST /api/analyze is called with default options
- **THEN** 200 OK with analysis results including repository map and dependency graph
- **AND** The response includes analysis artifacts
- **AND** The duration field shows the analysis time

#### Scenario: ForcedAnalysis
- **GIVEN** A project with existing analysis artifacts
- **WHEN** POST /api/analyze is called with force option
- **THEN** 200 OK with fresh analysis results
- **AND** The analysis artifacts are regenerated

### Requirement: Functionanalysis

The API SHALL support `Function specGenAnalyze(options: AnalyzeApiOptions = {})` to run static analysis on the codebase. scans the repository, builds a dependency graph, and generates analysis artifacts. no llm involvement.

**Request:**

```json
{
  "options": {
    "rootPath": "string",
    "maxFiles": "number",
    "excludePatterns": "string[]",
    "includePatterns": "string[]",
    "force": "boolean",
    "outputPath": "string",
    "onProgress": "ProgressCallback"
  }
}
```

**Response:**

```json
{
  "repoMap": "object",
  "depGraph": "DependencyGraphResult",
  "artifacts": {
    "repoStructure": "RepoStructure",
    "summaryMarkdown": "string",
    "dependencyDiagram": "string",
    "llmContext": "LLMContext"
  },
  "duration": "number"
}
```

#### Scenario: AnalyzeWithDefaultOptions
- **GIVEN** No options provided
- **WHEN** specGenAnalyze() is called
- **THEN** Returns analysis results with default configuration
- **AND** Uses current working directory as rootPath
- **AND** Uses DEFAULT_MAX_FILES for maxFiles
- **AND** Uses SPEC_GEN_ANALYSIS_REL_PATH for outputPath

#### Scenario: AnalyzeWithCustomOptions
- **GIVEN** Custom options provided
- **WHEN** specGenAnalyze(options) is called
- **THEN** Returns analysis results with custom configuration
- **AND** Uses provided rootPath
- **AND** Uses provided maxFiles
- **AND** Uses provided outputPath

#### Scenario: AnalyzeWithForceOption
- **GIVEN** force: true in options
- **WHEN** specGenAnalyze(options) is called
- **THEN** Performs full analysis regardless of cache
- **AND** Does not check for existing analysis
- **AND** Generates new artifacts

### Requirement: Runanalysisanalysis

The API SHALL support `runAnalysis runAnalysis(rootPath: string, outputPath: string, options: { maxFiles: number; include: string[]; exclude: string[]; }): Promise<AnalysisResult>` to [partial spec — file too large to fully analyze (3 parts)] run the complete analysis pipeline including repository mapping, dependency graph building, and artifact generation.

**Request:**

```json
{
  "rootPath": "string",
  "outputPath": "string",
  "options": {
    "maxFiles": "number",
    "include": "string[]",
    "exclude": "string[]"
  }
}
```

**Response:**

```json
{
  "repoMap": "RepositoryMap",
  "depGraph": "DependencyGraphResult",
  "artifacts": "AnalysisArtifacts",
  "duration": "number"
}
```

#### Scenario: RunAnalysisWithDefaultOptions
- **GIVEN** A valid rootPath and outputPath
- **WHEN** runAnalysis is called with default options
- **THEN** Returns an AnalysisResult object with repoMap, depGraph, artifacts, and duration

#### Scenario: RunAnalysisWithCustomIncludeAndExcludePatterns
- **GIVEN** A valid rootPath and outputPath with custom include and exclude patterns
- **WHEN** runAnalysis is called with custom options
- **THEN** Returns an AnalysisResult object with repoMap, depGraph, artifacts, and duration, respecting the
custom patterns

### Requirement: Functionanalysis

The API SHALL support `Function getAnalysisAge(analysisPath: string)` to [partial spec — file too large to fully analyze (2 parts)] get the age of the analysis data if it exists.

**Request:**

```json
{
  "analysisPath": "string"
}
```

**Response:**

```json
{
  "age": "number | null"
}
```

#### Scenario: GetAnalysisAge
- **GIVEN** An analysis path with existing analysis data
- **WHEN** getAnalysisAge is called with the analysis path
- **THEN** Returns the age of the analysis data in milliseconds

#### Scenario: AnalysisDataDoesNotExist
- **GIVEN** An analysis path without existing analysis data
- **WHEN** getAnalysisAge is called with the analysis path
- **THEN** Returns null

### Requirement: Postgeneration

The API SHALL support `POST /api/generate` to generate openapi specifications from analyzed code.

**Request:**

```json
{
  "provider": "'anthropic' | 'openai' | 'openai-compat' | 'gemini'",
  "model": "string",
  "apiBase": "string",
  "sslVerify": "boolean",
  "openaiCompatBaseUrl": "string",
  "domains": "string[]",
  "writeMode": "'replace' | 'merge' | 'skip'",
  "adr": "boolean",
  "adrOnly": "boolean",
  "mapping": "boolean",
  "dryRun": "boolean",
  "analysisPath": "string",
  "rootPath": "string",
  "configPath": "string",
  "onProgress": "ProgressCallback"
}
```

**Response:**

```json
{
  "report": "GenerationReport",
  "pipelineResult": "PipelineResult",
  "duration": "number"
}
```

#### Scenario: GenerateSpecifications
- **GIVEN** Analyzed codebase
- **WHEN** POST /api/generate is called with default options
- **THEN** 200 OK with generation report and pipeline results
- **AND** The response includes the generation duration

#### Scenario: DryRunGeneration
- **GIVEN** Analyzed codebase
- **WHEN** POST /api/generate is called with dryRun option
- **THEN** 200 OK with generation report and pipeline results
- **AND** No files are actually written

### Requirement: Postverification

The API SHALL support `POST /api/verify` to verify generated specifications against codebase.

**Request:**

```json
{
  "provider": "'anthropic' | 'openai' | 'openai-compat' | 'gemini'",
  "model": "string",
  "apiBase": "string",
  "openaiCompatBaseUrl": "string",
  "sslVerify": "boolean",
  "samples": "number",
  "threshold": "number",
  "domains": "string[]",
  "rootPath": "string",
  "configPath": "string",
  "onProgress": "ProgressCallback"
}
```

**Response:**

```json
{
  "report": "VerificationReport",
  "duration": "number"
}
```

#### Scenario: SuccessfulVerification
- **GIVEN** Generated specifications
- **WHEN** POST /api/verify is called with default options
- **THEN** 200 OK with verification report
- **AND** The response includes verification duration

#### Scenario: VerificationWithThreshold
- **GIVEN** Generated specifications
- **WHEN** POST /api/verify is called with custom threshold
- **THEN** 200 OK with verification report
- **AND** The report reflects the custom threshold

### Requirement: Postdrift

The API SHALL support `POST /api/drift` to detect specification drift between code and specifications.

**Request:**

```json
{
  "baseRef": "string",
  "files": "string[]",
  "domains": "string[]",
  "llmEnhanced": "boolean",
  "provider": "'anthropic' | 'openai' | 'openai-compat' | 'gemini'",
  "model": "string",
  "apiBase": "string",
  "openaiCompatBaseUrl": "string",
  "sslVerify": "boolean",
  "failOn": "DriftSeverity",
  "maxFiles": "number",
  "rootPath": "string",
  "configPath": "string",
  "onProgress": "ProgressCallback"
}
```

**Response:**

```json
{
  "result": "DriftResult",
  "duration": "number"
}
```

#### Scenario: DetectDrift
- **GIVEN** Codebase with changes since last analysis
- **WHEN** POST /api/drift is called with default options
- **THEN** 200 OK with drift detection results
- **AND** The response includes drift duration

#### Scenario: LlmEnhancedDriftDetection
- **GIVEN** Codebase with changes since last analysis
- **WHEN** POST /api/drift is called with llmEnhanced option
- **THEN** 200 OK with enhanced drift detection results
- **AND** The response includes more detailed drift information

### Requirement: Postpipeline

The API SHALL support `POST /api/run` to run the complete spec-gen pipeline from initialization to verification.

**Request:**

```json
{
  "force": "boolean",
  "reanalyze": "boolean",
  "provider": "'anthropic' | 'openai' | 'openai-compat' | 'gemini'",
  "model": "string",
  "apiBase": "string",
  "sslVerify": "boolean",
  "openaiCompatBaseUrl": "string",
  "maxFiles": "number",
  "adr": "boolean",
  "dryRun": "boolean",
  "rootPath": "string",
  "configPath": "string",
  "onProgress": "ProgressCallback"
}
```

**Response:**

```json
{
  "init": "InitResult",
  "analysis": "AnalyzeResult",
  "generation": "GenerateResult",
  "duration": "number"
}
```

#### Scenario: CompletePipelineRun
- **GIVEN** A project ready for spec-gen
- **WHEN** POST /api/run is called with default options
- **THEN** 200 OK with results from all pipeline stages
- **AND** The response includes the total pipeline duration

#### Scenario: DryRunPipeline
- **GIVEN** A project ready for spec-gen
- **WHEN** POST /api/run is called with dryRun option
- **THEN** 200 OK with results from all pipeline stages
- **AND** No files are actually written

### Requirement: Functioninsertionrole

The API SHALL support `function classifyRole(fanIn: number, fanOut: number, isHub: boolean, isEntryPoint: boolean): InsertionRole` to classifies the role of a node in the codebase based on its fan-in, fan-out, hub status, and entry point status.

**Request:**

```json
{
  "fanIn": "number",
  "fanOut": "number",
  "isHub": "boolean",
  "isEntryPoint": "boolean"
}
```

**Response:**

```json
{
  "InsertionRole": "string"
}
```

#### Scenario: ClassifyANodeAsAHub
- **GIVEN** A node with high fan-in and fan-out, and marked as a hub
- **WHEN** classifyRole is called with these parameters
- **THEN** Returns an InsertionRole indicating the node is a hub

#### Scenario: ClassifyANodeAsAnEntryPoint
- **GIVEN** A node marked as an entry point
- **WHEN** classifyRole is called with these parameters
- **THEN** Returns an InsertionRole indicating the node is an entry point

### Requirement: Functioninsertionstrategy

The API SHALL support `function deriveStrategy(role: InsertionRole): InsertionStrategy` to derives the insertion strategy based on the classified role of a node.

**Request:**

```json
{
  "role": "InsertionRole"
}
```

**Response:**

```json
{
  "InsertionStrategy": "string"
}
```

#### Scenario: DeriveStrategyForAHubNode
- **GIVEN** An InsertionRole indicating the node is a hub
- **WHEN** deriveStrategy is called with this role
- **THEN** Returns an InsertionStrategy suitable for a hub node

#### Scenario: DeriveStrategyForAnEntryPointNode
- **GIVEN** An InsertionRole indicating the node is an entry point
- **WHEN** deriveStrategy is called with this role
- **THEN** Returns an InsertionStrategy suitable for an entry point node

### Requirement: Functioninsertiondecision

The API SHALL support `function buildReason(name: string, role: InsertionRole, strategy: InsertionStrategy, fanIn: number, fanOut: number): string` to builds a reason string explaining the insertion decision based on the node's properties.

**Request:**

```json
{
  "name": "string",
  "role": "InsertionRole",
  "strategy": "InsertionStrategy",
  "fanIn": "number",
  "fanOut": "number"
}
```

**Response:**

```json
{
  "reason": "string"
}
```

#### Scenario: BuildReasonForAHubNode
- **GIVEN** A hub node's properties
- **WHEN** buildReason is called with these properties
- **THEN** Returns a reason string explaining the insertion decision for a hub node

#### Scenario: BuildReasonForAnEntryPointNode
- **GIVEN** An entry point node's properties
- **WHEN** buildReason is called with these properties
- **THEN** Returns a reason string explaining the insertion decision for an entry point node

### Requirement: Functioninsertionscore

The API SHALL support `function compositeScore(semanticRelevance: number, role: InsertionRole): number` to calculates a composite score for a node based on its semantic relevance and role.

**Request:**

```json
{
  "semanticRelevance": "number",
  "role": "InsertionRole"
}
```

**Response:**

```json
{
  "score": "number"
}
```

#### Scenario: CalculateCompositeScoreForAHubNode
- **GIVEN** A hub node's semantic relevance
- **WHEN** compositeScore is called with these parameters
- **THEN** Returns a composite score for the hub node

#### Scenario: CalculateCompositeScoreForAnEntryPointNode
- **GIVEN** An entry point node's semantic relevance
- **WHEN** compositeScore is called with these parameters
- **THEN** Returns a composite score for the entry point node

### Requirement: Functioninsertioncandidate

The API SHALL support `function handleSearchCode(directory: string, query: string, limit: number = 10, language?: string, minFanIn?: number): Promise<InsertionCandidate[]>` to performs a semantic search for code in the specified directory and returns insertion candidates.

**Request:**

```json
{
  "directory": "string",
  "query": "string",
  "limit": "number",
  "language": "string",
  "minFanIn": "number"
}
```

**Response:**

```json
{
  "candidates": "InsertionCandidate[]"
}
```

#### Scenario: SearchForCodeInADirectory
- **GIVEN** A directory and a query
- **WHEN** handleSearchCode is called with these parameters
- **THEN** Returns a list of insertion candidates matching the query

#### Scenario: SearchForCodeWithASpecificLanguage
- **GIVEN** A directory, a query, and a language
- **WHEN** handleSearchCode is called with these parameters
- **THEN** Returns a list of insertion candidates matching the query and language

### Requirement: Functioninsertioncandidate

The API SHALL support `function handleSuggestInsertionPoints(directory: string, description: string, limit: number = 5, language?: string): Promise<InsertionCandidate[]>` to suggests the best insertion points in the codebase for implementing a new feature based on the provided description.

**Request:**

```json
{
  "directory": "string",
  "description": "string",
  "limit": "number",
  "language": "string"
}
```

**Response:**

```json
{
  "candidates": "InsertionCandidate[]"
}
```

#### Scenario: SuggestInsertionPointsForANewFeature
- **GIVEN** A directory and a feature description
- **WHEN** handleSuggestInsertionPoints is called with these parameters
- **THEN** Returns a list of insertion candidates for implementing the feature

#### Scenario: SuggestInsertionPointsWithASpecificLanguage
- **GIVEN** A directory, a feature description, and a language
- **WHEN** handleSuggestInsertionPoints is called with these parameters
- **THEN** Returns a list of insertion candidates matching the language

### Requirement: Functionspecdomain

The API SHALL support `function handleGetSpec(directory: string, domain: string): Promise<unknown>` to retrieves the full content of a spec domain's spec.md file along with its mapping entries.

**Request:**

```json
{
  "directory": "string",
  "domain": "string"
}
```

**Response:**

```json
{
  "specContent": "unknown"
}
```

#### Scenario: GetSpecContentForADomain
- **GIVEN** A directory and a domain name
- **WHEN** handleGetSpec is called with these parameters
- **THEN** Returns the full content of the spec.md file for the domain

### Requirement: Functionspecdomain

The API SHALL support `function handleListSpecDomains(directory: string): Promise<unknown>` to lists all spec domains available in the project by reading the openspec/specs/ directory.

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "domains": "unknown"
}
```

#### Scenario: ListAllSpecDomainsInADirectory
- **GIVEN** A directory
- **WHEN** handleListSpecDomains is called with this parameter
- **THEN** Returns a list of all spec domains available in the directory

### Requirement: Functionspecindex

The API SHALL support `function handleSearchSpecs(directory: string, query: string, limit: number = 10, domain?: string, section?: string): Promise<unknown>` to performs a semantic search over the spec index built by 'spec-gen analyze --embed'.

**Request:**

```json
{
  "directory": "string",
  "query": "string",
  "limit": "number",
  "domain": "string",
  "section": "string"
}
```

**Response:**

```json
{
  "results": "unknown"
}
```

#### Scenario: SearchSpecsInADirectory
- **GIVEN** A directory and a query
- **WHEN** handleSearchSpecs is called with these parameters
- **THEN** Returns a list of specs matching the query

#### Scenario: SearchSpecsInASpecificDomain
- **GIVEN** A directory, a query, and a domain
- **WHEN** handleSearchSpecs is called with these parameters
- **THEN** Returns a list of specs matching the query and domain

### Requirement: Startmcpwatcher

The API SHALL support `start McpWatcher.start()` to starts the mcpwatcher to monitor source files for changes and incrementally update signatures and vector index.

#### Scenario: StartWatchingFiles
- **GIVEN** A valid project root path
- **WHEN** McpWatcher.start() is called
- **THEN** The watcher starts monitoring the specified directory for changes

### Requirement: Stopmcpwatcher

The API SHALL support `stop McpWatcher.stop()` to stops the mcpwatcher and cleans up resources.

#### Scenario: StopWatchingFiles
- **GIVEN** The watcher is currently running
- **WHEN** McpWatcher.stop() is called
- **THEN** The watcher stops monitoring and cleans up resources

### Requirement: Handlechangemcpwatcher

The API SHALL support `handleChange McpWatcher.handleChange(absPath: string)` to re-indexes a single changed file by updating its signatures in llm-context.json and optionally re-embedding it in the vector index.

**Request:**

```json
{
  "absPath": "string"
}
```

#### Scenario: ReIndexAChangedFile
- **GIVEN** A valid absolute path to a changed source file
- **WHEN** McpWatcher.handleChange(absPath) is called
- **THEN** The file's signatures are updated in llm-context.json and optionally re-embedded in the vector index

### Requirement: Functionopenspec

The API SHALL support `Function specGenGenerate(options: GenerateApiOptions = {})` to generate openspec specification files from analysis results using llm.

**Request:**

```json
{
  "options": {
    "type": "object",
    "properties": {
      "GenerateApiOptions": {
        "type": "object"
      }
    }
  }
}
```

**Response:**

```json
{
  "type": "object",
  "properties": {
    "GenerateResult": {
      "type": "object"
    }
  }
}
```

#### Scenario: GenerateOpenspecFiles
- **GIVEN** Analysis results available
- **WHEN** specGenGenerate is called with options
- **THEN** OpenSpec specification files are generated
- **AND** LLM service is created
- **AND** Stages are executed in sequence
- **AND** Logs are saved
- **AND** Similarity scores are calculated

### Requirement: Getspecrequirement

The API SHALL support `GET /api/specs/requirements` to retrieve spec requirements referenced in mapping.json.

**Request:**

```json
{
  "rootPath": "string (optional)"
}
```

**Response:**

```json
{
  "generatedAt": "string (optional)",
  "requirements": {
    "type": "object",
    "properties": {
      "requirementKey": {
        "type": "object",
        "properties": {
          "title": "string",
          "body": "string",
          "specFile": "string (optional)",
          "domain": "string (optional)",
          "service": "string (optional)"
        }
      }
    }
  }
}
```

#### Scenario: RetrieveSpecRequirements
- **GIVEN** A valid mapping.json file exists in the project
- **WHEN** GET /api/specs/requirements is called
- **THEN** 200 OK with the requirements object in the response body
- **AND** The response includes the generatedAt timestamp if available
- **AND** Each requirement includes title, body, and optional specFile, domain, and service

#### Scenario: HandleMissingMapping.json
- **GIVEN** No mapping.json file exists in the project
- **WHEN** GET /api/specs/requirements is called
- **THEN** 200 OK with an empty requirements object in the response body
- **AND** The generatedAt field is undefined

#### Scenario: HandleInvalidMapping.json
- **GIVEN** The mapping.json file is invalid or cannot be parsed
- **WHEN** GET /api/specs/requirements is called
- **THEN** 200 OK with an empty requirements object in the response body
- **AND** The generatedAt field is undefined

### Requirement: Functionproject

The API SHALL support `function specGenInit(options: InitApiOptions = {})` to initialize spec-gen in a project directory.

**Request:**

```json
{
  "options": {
    "type": "object",
    "properties": {
      "InitApiOptions": {
        "type": "object"
      }
    }
  }
}
```

**Response:**

```json
{
  "InitResult": {
    "type": "object"
  }
}
```

#### Scenario: InitializeSpecGen
- **GIVEN** A project directory
- **WHEN** specGenInit is called with options
- **THEN** spec-gen is initialized in the project directory
- **AND** Configuration files are created
- **AND** OpenSpec structure is set up
- **AND** .gitignore is updated if necessary

### Requirement: Functionspecverification

The API SHALL support `Function specGenVerify(options: VerifyApiOptions = {})` to verify generated specs against actual source code by sampling files and validating that specs accurately describe behavior using an llm to predict behavior from specs and compare against code.

**Request:**

```json
{
  "options": {
    "rootPath": "string",
    "samples": "number",
    "threshold": "number",
    "onProgress": "ProgressCallback",
    "provider": "string",
    "model": "string",
    "apiBase": "string",
    "sslVerify": "boolean",
    "openaiCompatBaseUrl": "string"
  }
}
```

**Response:**

```json
{
  "report": "VerifyResult",
  "duration": "number"
}
```

#### Scenario: SuccessfulVerification
- **GIVEN** Valid spec-gen configuration, existing specs, analysis, and LLM API key
- **WHEN** specGenVerify() is called with valid options
- **THEN** Returns a VerifyResult object with verification report and duration
- **AND** Progress callbacks are called with verification steps
- **AND** LLM logs are saved

#### Scenario: MissingConfiguration
- **GIVEN** No spec-gen configuration found
- **WHEN** specGenVerify() is called
- **THEN** Throws an error with message 'No spec-gen configuration found. Run specGenInit() first.'

#### Scenario: MissingSpecs
- **GIVEN** No specs found in the expected directory
- **WHEN** specGenVerify() is called
- **THEN** Throws an error with message 'No specs found. Run specGenGenerate() first.'

#### Scenario: MissingAnalysis
- **GIVEN** No analysis found in the expected directory
- **WHEN** specGenVerify() is called
- **THEN** Throws an error with message 'No analysis found. Run specGenAnalyze() first.'

#### Scenario: MissingLlmApiKey
- **GIVEN** No LLM API key found in environment variables
- **WHEN** specGenVerify() is called
- **THEN** Throws an error with message 'No LLM API key found. Set ANTHROPIC_API_KEY, OPENAI_API_KEY,
GEMINI_API_KEY, or OPENAI_COMPAT_API_KEY.'

#### Scenario: NoVerificationCandidates
- **GIVEN** No suitable verification candidates found
- **WHEN** specGenVerify() is called
- **THEN** Throws an error with message 'No suitable verification candidates found.'

### Requirement: Functionspecification

The API SHALL support `Function specGenDrift` to detect specification drift in a project by analyzing changes between different versions of specifications and related files.

**Request:**

```json
{
  "options": {
    "type": "object",
    "properties": {
      "baseRef": {
        "type": "string",
        "description": "The base Git reference to compare against"
      },
      "specDir": {
        "type": "string",
        "description": "Directory containing specifications"
      },
      "adrDir": {
        "type": "string",
        "description": "Directory containing Architecture Decision Records"
      },
      "configFile": {
        "type": "string",
        "description": "Path to configuration file"
      },
      "llmService": {
        "type": "object",
        "description": "LLM service configuration"
      }
    }
  }
}
```

**Response:**

```json
{
  "type": "object",
  "properties": {
    "gaps": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "file": {
            "type": "string"
          },
          "domains": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "severity": {
            "type": "string"
          },
          "reason": {
            "type": "string"
          }
        }
      }
    },
    "staleSpecs": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "uncoveredFiles": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "orphanedSpecs": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "adrGaps": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "adrId": {
            "type": "string"
          },
          "relatedSpecs": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "severity": {
            "type": "string"
          }
        }
      }
    },
    "adrOrphaned": {
      "type": "array",
      "items": {
        "type": "string"
      }
    }
  }
}
```

#### Scenario: DetectSpecificationDrift
- **GIVEN** A Git repository with specifications and related files
- **WHEN** specGenDrift is called with appropriate options
- **THEN** Returns an object containing detected gaps, stale specifications, uncovered files, orphaned
specifications, ADR gaps, and orphaned ADRs
- **AND** Analyzes changes between the current state and a base reference
- **AND** Identifies specifications that are out of sync with code changes
- **AND** Detects files that are not covered by specifications
- **AND** Finds specifications that are no longer relevant
- **AND** Analyzes Architecture Decision Records for consistency

### Requirement: CliCommandcodebaseAnalysis

The API SHALL support `CLI Command spec-gen analyze` to [partial spec — file too large to fully analyze (3 parts)] run static analysis on the codebase without llm involvement. outputs repository map, dependency graph, and file significance scores.

**Request:**

```json
{
  "output": "string",
  "maxFiles": "number",
  "include": "string[]",
  "exclude": "string[]",
  "force": "boolean",
  "embed": "boolean",
  "reindexSpecs": "boolean",
  "quiet": "boolean",
  "verbose": "boolean",
  "noColor": "boolean",
  "config": "string"
}
```

**Response:**

```json
{
  "repoStructure": "object",
  "dependencyGraph": "object",
  "llmContext": "object",
  "refactorPriorities": "object",
  "duplicates": "object",
  "architectureMd": "string",
  "codebaseMd": "string",
  "outputFiles": "string[]"
}
```

#### Scenario: AnalyzeWithDefaults
- **GIVEN** A codebase with a spec-gen configuration
- **WHEN** The command 'spec-gen analyze' is run
- **THEN** The codebase is analyzed and results are written to the default output directory
- **AND** Repository structure is generated
- **AND** Dependency graph is created
- **AND** File significance scores are calculated

#### Scenario: AnalyzeWithCustomOptions
- **GIVEN** A codebase with a spec-gen configuration
- **WHEN** The command 'spec-gen analyze --max-files 1000 --include "*.graphql" --exclude "legacy/**"' is run
- **THEN** The codebase is analyzed with the specified options and results are written to the default output
directory
- **AND** 1000 files are analyzed
- **AND** GraphQL files are included
- **AND** Legacy files are excluded

#### Scenario: ForceReAnalysis
- **GIVEN** A codebase with a spec-gen configuration and existing analysis
- **WHEN** The command 'spec-gen analyze --force' is run
- **THEN** The codebase is re-analyzed and results are written to the default output directory
- **AND** Existing analysis is overwritten
- **AND** Repository structure is regenerated
- **AND** Dependency graph is recreated

#### Scenario: ReIndexSpecsOnly
- **GIVEN** A codebase with a spec-gen configuration and existing analysis
- **WHEN** The command 'spec-gen analyze --reindex-specs' is run
- **THEN** OpenSpec specs are re-indexed into the vector index without re-running full analysis
- **AND** Full analysis is skipped
- **AND** Specs are re-indexed in the vector index

### Requirement: Functionnode.jsVersion

The API SHALL support `Function checkNodeVersion` to [partial spec — file too large to fully analyze (2 parts)] check if the node.js version meets the minimum requirement.

**Response:**

```json
{
  "name": "string",
  "status": "string",
  "detail": "string",
  "fix": "string (optional)"
}
```

#### Scenario: Node.jsVersionCheck
- **GIVEN** Node.js version is below the minimum requirement
- **WHEN** checkNodeVersion is called
- **THEN** Returns a CheckResult with status 'fail' and a fix message

#### Scenario: Node.jsVersionCheck
- **GIVEN** Node.js version meets the minimum requirement
- **WHEN** checkNodeVersion is called
- **THEN** Returns a CheckResult with status 'ok'

### Requirement: FunctiongitRepository

The API SHALL support `Function checkGit` to [partial spec — file too large to fully analyze (2 parts)] check if the project is a git repository and if git is available.

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "name": "string",
  "status": "string",
  "detail": "string",
  "fix": "string (optional)"
}
```

#### Scenario: GitRepositoryCheck
- **GIVEN** No .git directory found
- **WHEN** checkGit is called
- **THEN** Returns a CheckResult with status 'warn' and a fix message

#### Scenario: GitRepositoryCheck
- **GIVEN** Git binary is not on PATH
- **WHEN** checkGit is called
- **THEN** Returns a CheckResult with status 'warn' and a fix message

#### Scenario: GitRepositoryCheck
- **GIVEN** Git repository is properly configured
- **WHEN** checkGit is called
- **THEN** Returns a CheckResult with status 'ok'

### Requirement: FunctionspecGenConfiguration

The API SHALL support `Function checkConfig` to [partial spec — file too large to fully analyze (2 parts)] check if the spec-gen configuration file exists and is valid.

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "name": "string",
  "status": "string",
  "detail": "string",
  "fix": "string (optional)"
}
```

#### Scenario: ConfigCheck
- **GIVEN** Configuration file does not exist
- **WHEN** checkConfig is called
- **THEN** Returns a CheckResult with status 'warn' and a fix message

#### Scenario: ConfigCheck
- **GIVEN** Configuration file exists but is invalid
- **WHEN** checkConfig is called
- **THEN** Returns a CheckResult with status 'fail' and a fix message

#### Scenario: ConfigCheck
- **GIVEN** Configuration file exists and is valid
- **WHEN** checkConfig is called
- **THEN** Returns a CheckResult with status 'ok'

### Requirement: FunctionanalysisArtifacts

The API SHALL support `Function checkAnalysis` to [partial spec — file too large to fully analyze (2 parts)] check if the analysis artifacts are present and not stale.

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "name": "string",
  "status": "string",
  "detail": "string",
  "fix": "string (optional)"
}
```

#### Scenario: AnalysisCheck
- **GIVEN** Analysis artifacts do not exist
- **WHEN** checkAnalysis is called
- **THEN** Returns a CheckResult with status 'warn' and a fix message

#### Scenario: AnalysisCheck
- **GIVEN** Analysis artifacts are stale
- **WHEN** checkAnalysis is called
- **THEN** Returns a CheckResult with status 'warn' and a fix message

#### Scenario: AnalysisCheck
- **GIVEN** Analysis artifacts are present and fresh
- **WHEN** checkAnalysis is called
- **THEN** Returns a CheckResult with status 'ok'

### Requirement: FunctionopenspecDirectory

The API SHALL support `Function checkOpenSpecDir` to [partial spec — file too large to fully analyze (2 parts)] check if the openspec directory exists.

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "name": "string",
  "status": "string",
  "detail": "string",
  "fix": "string (optional)"
}
```

#### Scenario: OpenspecDirectoryCheck
- **GIVEN** OpenSpec directory does not exist
- **WHEN** checkOpenSpecDir is called
- **THEN** Returns a CheckResult with status 'warn' and a fix message

#### Scenario: OpenspecDirectoryCheck
- **GIVEN** OpenSpec directory exists
- **WHEN** checkOpenSpecDir is called
- **THEN** Returns a CheckResult with status 'ok'

### Requirement: FunctionllmProvider

The API SHALL support `Function checkLLMProvider` to [partial spec — file too large to fully analyze (2 parts)] check if an llm provider is configured.

**Response:**

```json
{
  "name": "string",
  "status": "string",
  "detail": "string",
  "fix": "string (optional)"
}
```

#### Scenario: LlmProviderCheck
- **GIVEN** No LLM provider is configured
- **WHEN** checkLLMProvider is called
- **THEN** Returns a CheckResult with status 'fail' and a fix message

#### Scenario: LlmProviderCheck
- **GIVEN** An LLM provider is configured
- **WHEN** checkLLMProvider is called
- **THEN** Returns a CheckResult with status 'ok'

### Requirement: FunctiondiskSpace

The API SHALL support `Function checkDiskSpace` to [partial spec — file too large to fully analyze (2 parts)] check if there is enough disk space available.

**Request:**

```json
{
  "rootPath": "string"
}
```

**Response:**

```json
{
  "name": "string",
  "status": "string",
  "detail": "string",
  "fix": "string (optional)"
}
```

#### Scenario: DiskSpaceCheck
- **GIVEN** Disk space is below the minimum requirement
- **WHEN** checkDiskSpace is called
- **THEN** Returns a CheckResult with status 'fail' and a fix message

#### Scenario: DiskSpaceCheck
- **GIVEN** Disk space is low but above the minimum requirement
- **WHEN** checkDiskSpace is called
- **THEN** Returns a CheckResult with status 'warn' and a fix message

#### Scenario: DiskSpaceCheck
- **GIVEN** Disk space is sufficient
- **WHEN** checkDiskSpace is called
- **THEN** Returns a CheckResult with status 'ok'

### Requirement: Getenvironmentconfiguration

The API SHALL support `GET /api/doctor` to [partial spec — file too large to fully analyze (2 parts)] check the environment and configuration for common issues.

**Response:**

```json
{
  "checks": [
    {
      "name": "string",
      "status": "string",
      "detail": "string",
      "fix": "string"
    }
  ]
}
```

#### Scenario: RunAllChecks
- **GIVEN** The spec-gen doctor command is available
- **WHEN** The command is executed without arguments
- **THEN** All checks are performed and results are displayed in the console

#### Scenario: OutputResultsAsJson
- **GIVEN** The spec-gen doctor command is available
- **WHEN** The command is executed with the --json flag
- **THEN** All checks are performed and results are displayed as JSON

### Requirement: Postcodeanalysis

The API SHALL support `POST /api/mcp/orient` to analyze a file and provide recommendations for refactoring based on specifications.

**Request:**

```json
{
  "filePath": "string",
  "specPath": "string"
}
```

**Response:**

```json
{
  "recommendations": "array",
  "specs": "array",
  "functions": "array"
}
```

#### Scenario: OrientWithValidFileAndSpecs
- **GIVEN** A valid file path and spec path
- **WHEN** POST /api/mcp/orient is called
- **THEN** 200 OK with recommendations, specs, and functions in the response body

#### Scenario: OrientWithInvalidFilePath
- **GIVEN** An invalid file path
- **WHEN** POST /api/mcp/orient is called
- **THEN** 400 Bad Request with an error message

### Requirement: Postcodebaseanalysis

The API SHALL support `POST /api/mcp/analyze-codebase` to analyze the entire codebase for refactoring opportunities.

**Request:**

```json
{
  "directory": "string",
  "force": "boolean"
}
```

**Response:**

```json
{
  "analysis": "object"
}
```

#### Scenario: AnalyzeCodebaseWithValidDirectory
- **GIVEN** A valid directory path
- **WHEN** POST /api/mcp/analyze-codebase is called
- **THEN** 200 OK with analysis results in the response body

#### Scenario: AnalyzeCodebaseWithInvalidDirectory
- **GIVEN** An invalid directory path
- **WHEN** POST /api/mcp/analyze-codebase is called
- **THEN** 400 Bad Request with an error message

### Requirement: Getarchitectureoverview

The API SHALL support `GET /api/mcp/architecture-overview` to get an overview of the architecture of the codebase.

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "overview": "object"
}
```

#### Scenario: GetArchitectureOverviewWithValidDirectory
- **GIVEN** A valid directory path
- **WHEN** GET /api/mcp/architecture-overview is called
- **THEN** 200 OK with architecture overview in the response body

#### Scenario: GetArchitectureOverviewWithInvalidDirectory
- **GIVEN** An invalid directory path
- **WHEN** GET /api/mcp/architecture-overview is called
- **THEN** 400 Bad Request with an error message

### Requirement: Getrefactorreport

The API SHALL support `GET /api/mcp/refactor-report` to generate a report on refactoring opportunities in the codebase.

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "report": "object"
}
```

#### Scenario: GetRefactorReportWithValidDirectory
- **GIVEN** A valid directory path
- **WHEN** GET /api/mcp/refactor-report is called
- **THEN** 200 OK with refactor report in the response body

#### Scenario: GetRefactorReportWithInvalidDirectory
- **GIVEN** An invalid directory path
- **WHEN** GET /api/mcp/refactor-report is called
- **THEN** 400 Bad Request with an error message

### Requirement: Getcallgraph

The API SHALL support `GET /api/mcp/call-graph` to get the call graph of the codebase.

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "callGraph": "object"
}
```

#### Scenario: GetCallGraphWithValidDirectory
- **GIVEN** A valid directory path
- **WHEN** GET /api/mcp/call-graph is called
- **THEN** 200 OK with call graph in the response body

#### Scenario: GetCallGraphWithInvalidDirectory
- **GIVEN** An invalid directory path
- **WHEN** GET /api/mcp/call-graph is called
- **THEN** 400 Bad Request with an error message

### Requirement: Getfunctionsignatures

The API SHALL support `GET /api/mcp/signatures` to get the signatures of functions in the codebase.

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "signatures": "array"
}
```

#### Scenario: GetSignaturesWithValidDirectory
- **GIVEN** A valid directory path
- **WHEN** GET /api/mcp/signatures is called
- **THEN** 200 OK with signatures in the response body

#### Scenario: GetSignaturesWithInvalidDirectory
- **GIVEN** An invalid directory path
- **WHEN** GET /api/mcp/signatures is called
- **THEN** 400 Bad Request with an error message

### Requirement: Getsubgraph

The API SHALL support `GET /api/mcp/subgraph` to get a subgraph of the call graph based on a starting function.

**Request:**

```json
{
  "directory": "string",
  "startFunction": "string"
}
```

**Response:**

```json
{
  "subgraph": "object"
}
```

#### Scenario: GetSubgraphWithValidDirectoryAndStartFunction
- **GIVEN** A valid directory path and start function
- **WHEN** GET /api/mcp/subgraph is called
- **THEN** 200 OK with subgraph in the response body

#### Scenario: GetSubgraphWithInvalidDirectory
- **GIVEN** An invalid directory path
- **WHEN** GET /api/mcp/subgraph is called
- **THEN** 400 Bad Request with an error message

### Requirement: Getexecutionpath

The API SHALL support `GET /api/mcp/trace-execution-path` to trace the execution path from a starting function to a target function.

**Request:**

```json
{
  "directory": "string",
  "startFunction": "string",
  "targetFunction": "string"
}
```

**Response:**

```json
{
  "executionPath": "array"
}
```

#### Scenario: TraceExecutionPathWithValidDirectory,StartFunction,AndTargetFunction
- **GIVEN** A valid directory path, start function, and target function
- **WHEN** GET /api/mcp/trace-execution-path is called
- **THEN** 200 OK with execution path in the response body

#### Scenario: TraceExecutionPathWithInvalidDirectory
- **GIVEN** An invalid directory path
- **WHEN** GET /api/mcp/trace-execution-path is called
- **THEN** 400 Bad Request with an error message

### Requirement: Getcodespecmapping

The API SHALL support `GET /api/mcp/mapping` to get the mapping between code and specifications.

**Request:**

```json
{
  "directory": "string"
}
```

**Response:**

```json
{
  "mapping": "object"
}
```

#### Scenario: GetMappingWithValidDirectory
- **GIVEN** A valid directory path
- **WHEN** GET /api/mcp/mapping is called
- **THEN** 200 OK with mapping in the response body

#### Scenario: GetMappingWithInvalidDirectory
- **GIVEN** An invalid directory path
- **WHEN** GET /api/mcp/mapping is called
- **THEN** 400 Bad Request with an error message

### Requirement: Getimpactanalysis

The API SHALL support `GET /api/mcp/analyze-impact` to analyze the impact of a change on the codebase.

**Request:**

```json
{
  "directory": "string",
  "change": "object"
}
```

**Response:**

```json
{
  "impactAnalysis": "object"
}
```

#### Scenario: AnalyzeImpactWithValidDirectoryAndChange
- **GIVEN** A valid directory path and change object
- **WHEN** GET /api/mcp/analyze-impact is called
- **THEN** 200 OK with impact analysis in the response body

#### Scenario: AnalyzeImpactWithInvalidDirectory
- **GIVEN** An invalid directory path
- **WHEN** GET /api/mcp/analyze-impact is called
- **THEN** 400 Bad Request with an error message

### Requirement: Functionanalysisdata

The API SHALL support `Function loadAnalysis(analysisPath: string)` to [partial spec — file too large to fully analyze (2 parts)] load analysis data from disk

**Request:**

```json
{
  "analysisPath": "string"
}
```

**Response:**

```json
{
  "repoStructure": "RepoStructure",
  "llmContext": "LLMContext",
  "depGraph": "DependencyGraphResult",
  "age": "number",
  "timestamp": "string"
}
```

#### Scenario: LoadAnalysisData
- **GIVEN** Valid analysis path
- **WHEN** Function is called
- **THEN** Returns analysis data including repo structure, LLM context, dependency graph, age, and timestamp

### Requirement: Functioncostestimation

The API SHALL support `Function estimateCost(llmContext: LLMContext, provider: string, model: string)` to [partial spec — file too large to fully analyze (2 parts)] estimate cost for the full generation pipeline (all stages)

**Request:**

```json
{
  "llmContext": "LLMContext",
  "provider": "string",
  "model": "string"
}
```

**Response:**

```json
{
  "tokens": "number",
  "cost": "number"
}
```

#### Scenario: EstimateCost
- **GIVEN** Valid LLM context, provider, and model
- **WHEN** Function is called
- **THEN** Returns estimated tokens and cost for the generation pipeline

### Requirement: Functioncostestimation

The API SHALL support `Function estimateCost(llmContext: LLMContext, model: string)` to [partial spec — file too large to fully analyze (2 parts)] estimate the cost for generation based on the llm context and model.

**Request:**

```json
{
  "llmContext": "LLMContext",
  "model": "string"
}
```

**Response:**

```json
{
  "tokens": "number",
  "cost": "number"
}
```

#### Scenario: EstimateCost
- **GIVEN** An LLM context and a model
- **WHEN** estimateCost is called with the LLM context and model
- **THEN** Returns the estimated tokens and cost for generation

### Requirement: Functionuserconfirmation

The API SHALL support `Function promptConfirmation(message: string, autoYes: boolean)` to [partial spec — file too large to fully analyze (2 parts)] prompt user for confirmation

**Request:**

```json
{
  "message": "string",
  "autoYes": "boolean"
}
```

**Response:**

```json
{
  "result": "boolean"
}
```

#### Scenario: PromptUserForConfirmation
- **GIVEN** Confirmation message and autoYes flag
- **WHEN** Function is called
- **THEN** Returns user confirmation result

### Requirement: Functionapiconnectivity

The API SHALL support `Function verifyApiConnectivity(llm: LLMService)` to [partial spec — file too large to fully analyze (2 parts)] verify llm api connectivity

**Request:**

```json
{
  "llm": "LLMService"
}
```

**Response:**

```json
{
  "result": "boolean"
}
```

#### Scenario: VerifyApiConnectivity
- **GIVEN** Valid LLM service
- **WHEN** Function is called
- **THEN** Returns true if API connectivity is verified, false otherwise

### Requirement: CliCommandspecificationGeneration

The API SHALL support `CLI Command spec-gen generate` to [partial spec — file too large to fully analyze (2 parts)] generate openspec files from analysis using llm

**Request:**

```json
{
  "analysis": "string",
  "model": "string",
  "dryRun": "boolean",
  "domains": "string[]",
  "reanalyze": "boolean",
  "merge": "boolean",
  "noOverwrite": "boolean",
  "yes": "boolean",
  "outputDir": "string",
  "adr": "boolean",
  "adrOnly": "boolean"
}
```

**Response:**

```json
{
  "filesWritten": "string[]",
  "filesMerged": "string[]",
  "filesSkipped": "string[]",
  "filesBackedUp": "string[]",
  "configUpdated": "boolean",
  "warnings": "string[]",
  "validationErrors": "string[]",
  "nextSteps": "string[]",
  "duration": "number"
}
```

#### Scenario: GenerateSpecsWithDefaultSettings
- **GIVEN** Existing analysis and API key
- **WHEN** spec-gen generate is executed
- **THEN** OpenSpec files are generated in the openspec/specs/ directory
- **AND** config.yaml is updated
- **AND** Generation report is saved

#### Scenario: DryRunGeneration
- **GIVEN** Existing analysis and API key
- **WHEN** spec-gen generate --dry-run is executed
- **THEN** No files are written
- **AND** Preview of what would be generated is shown
- **AND** No changes are made to the filesystem

#### Scenario: GenerateSpecificDomains
- **GIVEN** Existing analysis and API key
- **WHEN** spec-gen generate --domains auth,api is executed
- **THEN** Only specs for auth and api domains are generated
- **AND** Other domains are skipped

#### Scenario: MergeWithExistingSpecs
- **GIVEN** Existing analysis, API key, and existing specs
- **WHEN** spec-gen generate --merge is executed
- **THEN** New specs are merged with existing ones
- **AND** No existing specs are overwritten

#### Scenario: SkipExistingSpecs
- **GIVEN** Existing analysis, API key, and existing specs
- **WHEN** spec-gen generate --no-overwrite is executed
- **THEN** Existing specs are skipped
- **AND** Only new specs are generated

#### Scenario: GenerateAdrsOnly
- **GIVEN** Existing analysis and API key
- **WHEN** spec-gen generate --adr-only is executed
- **THEN** Only ADRs are generated
- **AND** No specs are generated

### Requirement: Functionrunmetadata

The API SHALL support `Function saveRunMetadata(rootPath: string, metadata: RunMetadata)` to [partial spec — file too large to fully analyze (2 parts)] save run metadata to disk.

**Request:**

```json
{
  "rootPath": "string",
  "metadata": "RunMetadata"
}
```

#### Scenario: SaveRunMetadata
- **GIVEN** A root path and run metadata
- **WHEN** saveRunMetadata is called with the root path and metadata
- **THEN** Saves the run metadata to disk

### Requirement: Functionui

The API SHALL support `Function displayBanner(projectName: string, projectType: string, rootPath: string)` to [partial spec — file too large to fully analyze (2 parts)] display the pipeline banner with project information.

**Request:**

```json
{
  "projectName": "string",
  "projectType": "string",
  "rootPath": "string"
}
```

#### Scenario: DisplayBanner
- **GIVEN** Project name, project type, and root path
- **WHEN** displayBanner is called with the project name, project type, and root path
- **THEN** Displays the pipeline banner with the provided information

### Requirement: Functionui

The API SHALL support `Function displayCompletionBanner()` to [partial spec — file too large to fully analyze (2 parts)] display the completion banner with next steps.

#### Scenario: DisplayCompletionBanner
- **GIVEN** No parameters
- **WHEN** displayCompletionBanner is called
- **THEN** Displays the completion banner with next steps

### Requirement: CliCommandspecificationGenerationPipeline

The API SHALL support `CLI Command spec-gen run` to [partial spec — file too large to fully analyze (2 parts)] run the full spec-gen pipeline (init → analyze → generate)

**Request:**

```json
{
  "force": "boolean",
  "reanalyze": "boolean",
  "model": "string",
  "dryRun": "boolean",
  "yes": "boolean",
  "maxFiles": "number",
  "adr": "boolean"
}
```

**Response:**

```json
{
  "status": "string",
  "message": "string",
  "metadata": {
    "version": "string",
    "timestamp": "string",
    "duration": "number",
    "steps": {
      "init": {
        "status": "string",
        "reason": "string"
      },
      "analyze": {
        "status": "string",
        "reason": "string",
        "filesAnalyzed": "number"
      },
      "generate": {
        "status": "string",
        "specsGenerated": "number"
      }
    },
    "result": "string",
    "error": "string"
  }
}
```

#### Scenario: FullPipelineRun
- **GIVEN** A project directory with code to analyze
- **WHEN** spec-gen run is executed
- **THEN** The pipeline completes successfully with generated specifications
- **AND** Initializes configuration if needed
- **AND** Analyzes the codebase
- **AND** Generates specifications using LLM
- **AND** Writes specifications to openspec/ directory

#### Scenario: DryRun
- **GIVEN** A project directory with code to analyze
- **WHEN** spec-gen run --dry-run is executed
- **THEN** The pipeline shows what would be done without making changes
- **AND** Displays what files would be analyzed
- **AND** Shows what specifications would be generated
- **AND** Does not modify any files

#### Scenario: ForceReinitialize
- **GIVEN** An existing spec-gen configuration
- **WHEN** spec-gen run --force is executed
- **THEN** The pipeline reinitializes the configuration and reanalyzes the codebase
- **AND** Creates a new configuration file
- **AND** Recreates the openspec/ directory if needed
- **AND** Performs a fresh analysis of the codebase

### Requirement: CliCommandspecificationProcessing

The API SHALL support `CLI Command view` to configure and run a server for viewing and processing specifications.

#### Scenario: ConfigureAndRunServer
- **GIVEN** A CLI command 'view' is executed
- **WHEN** The view command is configured and run
- **THEN** The server is configured and started
- **AND** Sanitize error messages
- **AND** Check file existence
- **AND** Collect specification files
- **AND** Detect language
- **AND** Get skeleton content
- **AND** Run chat agent with different providers (Gemini, Anthropic, OpenAI)
- **AND** Send events
- **AND** Resolve provider configuration

### Requirement: Commandspecificationverification

The API SHALL support `Command verify` to [partial spec — file too large to fully analyze (2 parts)] verify the accuracy of generated specifications against the actual codebase.

**Request:**

```json
{
  "files": "string[]",
  "domains": "string[]",
  "json": "boolean"
}
```

**Response:**

```json
{
  "report": "VerificationReport",
  "results": "VerificationResult[]"
}
```

#### Scenario: VerifySpecifications
- **GIVEN** A set of generated specifications and corresponding code files
- **WHEN** The verify command is executed with optional file and domain filters
- **THEN** A verification report is generated and displayed
- **AND** Individual file verification results are shown with scores and feedback
- **AND** A summary report is displayed with overall confidence and domain accuracy
- **AND** Recommendations are provided based on the verification results

### Requirement: CliCommandspecificationverification

The API SHALL support `CLI Command spec-gen verify` to [partial spec — file too large to fully analyze (2 parts)] verify generated specs against actual source code

**Request:**

```json
{
  "samples": "number (default: 5)",
  "threshold": "number (0-1, default: 0.7)",
  "files": "string[] (comma-separated paths)",
  "domains": "string[] (comma-separated domains)",
  "verbose": "boolean (default: false)",
  "json": "boolean (default: false)"
}
```

**Response:**

```json
{
  "results": "VerificationResult[]",
  "overallScore": "number",
  "recommendation": "string",
  "domains": "DomainVerificationResult[]"
}
```

#### Scenario: BasicVerification
- **GIVEN** Generated specs exist and LLM API key is set
- **WHEN** spec-gen verify is called
- **THEN** Verification runs and reports overall score
- **AND** Selects sample files not used in generation
- **AND** Compares LLM predictions against actual code
- **AND** Displays results with confidence score

#### Scenario: DomainSpecificVerification
- **GIVEN** Multiple domains exist in the codebase
- **WHEN** spec-gen verify --domains user,order is called
- **THEN** Only verifies files from specified domains
- **AND** Provides domain-specific accuracy breakdown
- **AND** Maintains overall confidence score

#### Scenario: JsonOutput
- **GIVEN** Generated specs exist
- **WHEN** spec-gen verify --json is called
- **THEN** Outputs verification results as JSON
- **AND** Includes detailed prediction vs actual comparisons
- **AND** Maintains all verification metrics

### Requirement: Functiondriftseverity

The API SHALL support `Function severityLabel(severity: DriftSeverity): string` to [partial spec — file too large to fully analyze (2 parts)] converts a driftseverity enum value to a human-readable label.

**Request:**

```json
{
  "severity": "DriftSeverity"
}
```

**Response:**

```json
{
  "label": "string"
}
```

#### Scenario: ConvertErrorSeverity
- **GIVEN** A DriftSeverity value of 'error'
- **WHEN** The function is called with 'error'
- **THEN** Returns 'ERROR'

#### Scenario: ConvertWarningSeverity
- **GIVEN** A DriftSeverity value of 'warning'
- **WHEN** The function is called with 'warning'
- **THEN** Returns 'WARNING'

#### Scenario: ConvertInfoSeverity
- **GIVEN** A DriftSeverity value of 'info'
- **WHEN** The function is called with 'info'
- **THEN** Returns 'INFO'

### Requirement: Functiondriftseverity

The API SHALL support `Function severityIcon(severity: DriftSeverity): string` to [partial spec — file too large to fully analyze (2 parts)] converts a driftseverity enum value to a corresponding icon.

**Request:**

```json
{
  "severity": "DriftSeverity"
}
```

**Response:**

```json
{
  "icon": "string"
}
```

#### Scenario: GetErrorIcon
- **GIVEN** A DriftSeverity value of 'error'
- **WHEN** The function is called with 'error'
- **THEN** Returns '✗'

#### Scenario: GetWarningIcon
- **GIVEN** A DriftSeverity value of 'warning'
- **WHEN** The function is called with 'warning'
- **THEN** Returns '⚠'

#### Scenario: GetInfoIcon
- **GIVEN** A DriftSeverity value of 'info'
- **WHEN** The function is called with 'info'
- **THEN** Returns '→'

### Requirement: Functiondriftissue

The API SHALL support `Function kindLabel(kind: string): string` to [partial spec — file too large to fully analyze (2 parts)] converts a drift issue kind to a human-readable label.

**Request:**

```json
{
  "kind": "string"
}
```

**Response:**

```json
{
  "label": "string"
}
```

#### Scenario: ConvertGapKind
- **GIVEN** A kind value of 'gap'
- **WHEN** The function is called with 'gap'
- **THEN** Returns 'gap'

#### Scenario: ConvertStaleKind
- **GIVEN** A kind value of 'stale'
- **WHEN** The function is called with 'stale'
- **THEN** Returns 'stale'

#### Scenario: ConvertUncoveredKind
- **GIVEN** A kind value of 'uncovered'
- **WHEN** The function is called with 'uncovered'
- **THEN** Returns 'uncovered'

#### Scenario: ConvertOrphanedSpecKind
- **GIVEN** A kind value of 'orphaned-spec'
- **WHEN** The function is called with 'orphaned-spec'
- **THEN** Returns 'orphaned'

#### Scenario: ConvertAdrGapKind
- **GIVEN** A kind value of 'adr-gap'
- **WHEN** The function is called with 'adr-gap'
- **THEN** Returns 'adr-gap'

#### Scenario: ConvertAdrOrphanedKind
- **GIVEN** A kind value of 'adr-orphaned'
- **WHEN** The function is called with 'adr-orphaned'
- **THEN** Returns 'adr-orphaned'

#### Scenario: DefaultKind
- **GIVEN** An unknown kind value
- **WHEN** The function is called with an unknown kind
- **THEN** Returns the kind value as-is

### Requirement: Functiondriftissue

The API SHALL support `Function displayIssue(issue: DriftIssue, verbose: boolean): void` to [partial spec — file too large to fully analyze (2 parts)] displays a drift issue in the console.

**Request:**

```json
{
  "issue": "DriftIssue",
  "verbose": "boolean"
}
```

#### Scenario: DisplayErrorIssue
- **GIVEN** A DriftIssue with severity 'error' and verbose set to false
- **WHEN** The function is called with the issue and verbose=false
- **THEN** Displays the issue with the error icon and label

#### Scenario: DisplayWarningIssueVerbosely
- **GIVEN** A DriftIssue with severity 'warning' and verbose set to true
- **WHEN** The function is called with the issue and verbose=true
- **THEN** Displays the issue with the warning icon, label, and message

#### Scenario: DisplayInfoIssueWithChangedLines
- **GIVEN** A DriftIssue with severity 'info', verbose set to false, and changedLines
- **WHEN** The function is called with the issue and verbose=false
- **THEN** Displays the issue with the info icon, label, and changed lines

### Requirement: Functiondriftresult

The API SHALL support `Function displaySummary(result: DriftResult): void` to [partial spec — file too large to fully analyze (2 parts)] displays a summary of drift issues in the console.

**Request:**

```json
{
  "result": "DriftResult"
}
```

#### Scenario: DisplaySummaryWithGaps
- **GIVEN** A DriftResult with gaps in the summary
- **WHEN** The function is called with the result
- **THEN** Displays the summary with the number of gaps

#### Scenario: DisplaySummaryWithNoIssues
- **GIVEN** A DriftResult with no issues in the summary
- **WHEN** The function is called with the result
- **THEN** Displays 'No issues found'

#### Scenario: DisplaySummaryWithMultipleIssues
- **GIVEN** A DriftResult with multiple issues in the summary
- **WHEN** The function is called with the result
- **THEN** Displays the summary with all issue types and counts

### Requirement: Functiongithook

The API SHALL support `Function installPreCommitHook(rootPath: string): Promise<void>` to [partial spec — file too large to fully analyze (2 parts)] installs a pre-commit hook to check for spec drift.

**Request:**

```json
{
  "rootPath": "string"
}
```

#### Scenario: InstallHookInAGitRepository
- **GIVEN** A valid git repository path
- **WHEN** The function is called with the path
- **THEN** Installs the pre-commit hook and displays a success message

#### Scenario: InstallHookInANonGitRepository
- **GIVEN** A non-git repository path
- **WHEN** The function is called with the path
- **THEN** Displays an error message and exits with code 1

#### Scenario: AppendToExistingHook
- **GIVEN** An existing pre-commit hook without the spec-gen marker
- **WHEN** The function is called with the path
- **THEN** Appends the spec-gen drift check to the existing hook

### Requirement: Functiongithook

The API SHALL support `Function uninstallPreCommitHook(rootPath: string): Promise<void>` to [partial spec — file too large to fully analyze (2 parts)] uninstalls the spec-gen drift check from the pre-commit hook.

**Request:**

```json
{
  "rootPath": "string"
}
```

#### Scenario: UninstallFromExistingHook
- **GIVEN** An existing pre-commit hook with the spec-gen marker
- **WHEN** The function is called with the path
- **THEN** Removes the spec-gen drift check from the hook

#### Scenario: NoHookFound
- **GIVEN** No pre-commit hook exists
- **WHEN** The function is called with the path
- **THEN** Displays a warning message

#### Scenario: RemoveEmptyHook
- **GIVEN** An existing pre-commit hook that only contains the spec-gen marker
- **WHEN** The function is called with the path
- **THEN** Deletes the hook file

### Requirement: Clispecdrift

The API SHALL support `CLI spec-gen drift` to [partial spec — file too large to fully analyze (2 parts)] detect spec drift: find code changes not reflected in specs

#### Scenario: DetectDriftAgainstMainBranch
- **GIVEN** A git repository with code and specs
- **WHEN** The command 'spec-gen drift' is executed
- **THEN** The command analyzes changes and reports any spec drift

#### Scenario: CompareAgainstSpecificBranch
- **GIVEN** A git repository with code and specs
- **WHEN** The command 'spec-gen drift --base develop' is executed
- **THEN** The command compares changes against the develop branch and reports any spec drift

#### Scenario: OutputJsonForCiIntegration
- **GIVEN** A git repository with code and specs
- **WHEN** The command 'spec-gen drift --json' is executed
- **THEN** The command outputs the results in JSON format suitable for CI integration

#### Scenario: FailOnSpecificSeverity
- **GIVEN** A git repository with code and specs
- **WHEN** The command 'spec-gen drift --fail-on error' is executed
- **THEN** The command only fails on error-level drift issues

#### Scenario: UseLlmForSemanticAnalysis
- **GIVEN** A git repository with code and specs and an LLM API key
- **WHEN** The command 'spec-gen drift --use-llm' is executed
- **THEN** The command uses LLM for deeper semantic comparison and reports any spec drift

#### Scenario: InstallPreCommitHook
- **GIVEN** A git repository with code and specs
- **WHEN** The command 'spec-gen drift --install-hook' is executed
- **THEN** The command installs a pre-commit hook for drift detection

#### Scenario: RemovePreCommitHook
- **GIVEN** A git repository with a pre-commit hook installed
- **WHEN** The command 'spec-gen drift --uninstall-hook' is executed
- **THEN** The command removes the pre-commit hook
