# Analyzer Specification

> Generated by spec-gen v1.0.0 on 2026-03-25 22:10
> Source files: src/core/analyzer/repository-mapper.ts, src/api/types.ts, src/core/analyzer/embedding-service.ts, src/core/analyzer/subgraph-extractor.ts, src/core/analyzer/architecture-writer.ts, src/core/analyzer/dependency-graph.ts, src/core/analyzer/spec-vector-index.ts, src/core/analyzer/file-walker.ts, src/core/analyzer/import-resolver-bridge.ts, src/core/analyzer/refactor-analyzer.ts, src/core/analyzer/vector-index.ts, src/core/analyzer/import-parser.ts, src/core/analyzer/signature-extractor.ts, src/core/analyzer/artifact-generator.ts, src/core/analyzer/cpp-header-resolver.ts, src/core/analyzer/call-graph.ts, src/core/analyzer/duplicate-detector.ts, src/core/analyzer/type-inference-engine.ts, src/core/analyzer/significance-scorer.ts, src/core/analyzer/http-route-parser.ts, src/core/analyzer/function-registry-trie.ts

## Purpose

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Analyzes code structure to identify
potential refactoring opportunities and architectural issues.

## Entities

### LanguageBreakdown

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents the distribution of
programming languages in a repository.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| language | string | Name of the programming language |
| extension | string | File extension associated with the language |
| fileCount | number | Number of files using this language |
| percentage | number | Percentage of total files using this language |

### DetectedFramework

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Information about a detected framework in
the repository.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the framework |
| category | enum ('frontend', 'backend', 'database', 'testing', 'auth', 'ci', 'other') | Category of the framework |
| confidence | enum ('high', 'medium', 'low') | Confidence level of the detection |
| evidence | string[] | Evidence supporting the detection |

### DirectoryStats

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Statistics about a directory in the
repository.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| path | string | Path to the directory |
| fileCount | number | Number of files in the directory |
| purpose | string | Purpose or role of the directory |
| avgScore | number | Average significance score of files in the directory |

### FileClusters

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Groups of files clustered by different
criteria.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| byDirectory | Record<string, ScoredFile[]> | Files grouped by directory |
| byDomain | Record<string, ScoredFile[]> | Files grouped by domain |
| byLayer | object | Files grouped by architectural layer |

### RepositoryMapMetadata

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Metadata about the repository map.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| projectName | string | Name of the project |
| projectType | ProjectType | Type of the project |
| rootPath | string | Root path of the repository |
| analyzedAt | string | Timestamp when the analysis was performed |
| version | string | Version of the analysis tool |

### RepositoryMapSummary

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Summary statistics of the repository map.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| totalFiles | number | Total number of files in the repository |
| analyzedFiles | number | Number of files analyzed |
| skippedFiles | number | Number of files skipped during analysis |
| languages | LanguageBreakdown[] | Language distribution in the repository |
| frameworks | DetectedFramework[] | Detected frameworks in the repository |
| directories | DirectoryStats[] | Statistics about directories in the repository |

### RepositoryMap

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Complete map of the repository structure
and contents.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| metadata | RepositoryMapMetadata | Metadata about the repository map |
| summary | RepositoryMapSummary | Summary statistics of the repository |
| highValueFiles | ScoredFile[] | Files identified as high value |
| entryPoints | ScoredFile[] | Entry point files of the application |
| schemaFiles | ScoredFile[] | Schema files in the repository |
| configFiles | ScoredFile[] | Configuration files in the repository |
| clusters | FileClusters | File clusters by different criteria |
| allFiles | ScoredFile[] | All files in the repository |

### RepositoryMapperOptions

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Options for configuring the repository
mapping process.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| maxFiles | number | Maximum number of files to process |
| includePatterns | string[] | Patterns to force-include, overriding gitignore and excludePatterns |
| excludePatterns | string[] | Additional patterns to exclude |
| scoringConfig | ScoringConfig | Custom scoring configuration |
| onProgress | function | Progress callback function |
| outputDir | string | Output directory for analysis files |

### FrameworkDetector

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Detects and categorizes frameworks used
in a project based on file patterns and dependencies.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the framework (e.g., 'React', 'Next.js') |
| category | string | Category of the framework (e.g., 'frontend', 'backend', 'testing') |
| detect | function | Function that determines if the framework is used in the project |

### AnalyzeResult

Result of the code analysis process.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| repoMap | CoreRepositoryMap | Repository mapping data |
| depGraph | DependencyGraphResult | Dependency graph data |
| artifacts | AnalysisArtifacts | Generated analysis artifacts |
| duration | number | Duration of analysis in milliseconds |

### EmbeddingService

Computes text embeddings via any OpenAI-compatible `/embeddings` endpoint (OpenAI, Ollama, LocalAI,
vLLM, LM Studio, …).

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| baseUrl | string | Base URL of the OpenAI-compatible API, e.g. "http://localhost:11434/v1" |
| model | string | Embedding model name, e.g. "nomic-embed-text" or "text-embedding-3-small" |
| apiKey | string | API key — optional for local servers |
| batchSize | number | Maximum number of texts per API call (default: 64) |
| skipSslVerify | boolean | Disable SSL certificate verification (e.g. self-signed certs on local servers) |

### SubGraphNode

Represents a node in a subgraph, containing function information.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the function |
| file | string | File path where the function is defined |
| fanIn | number | Number of incoming calls to the function |
| fanOut | number | Number of outgoing calls from the function |

### SubGraph

Represents a subgraph extracted around a root function.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| root | SubGraphNode | Root node of the subgraph |
| nodes | SubGraphNode[] | Nodes reachable within the maximum depth |
| edges | Array<[string, string]> | Edges representing function calls between nodes |

**Relationships:**

- contains SubGraphNode (SubGraph contains root and nodes of type SubGraphNode)

### SubgraphOptions

Options for configuring subgraph extraction.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| maxDepth | number | Maximum depth to traverse the call graph |
| maxNodes | number | Maximum number of nodes to include in the subgraph |
| tokenBudget | number | Token budget for the subgraph extraction |

### FunctionNode

Represents a function node in the call graph.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the function node |
| name | string | Name of the function |
| filePath | string | File path where the function is defined |
| fanIn | number | Number of incoming calls to the function |
| fanOut | number | Number of outgoing calls from the function |

### SerializedCallGraph

Represents a serialized call graph.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| nodes | FunctionNode[] | List of function nodes in the call graph |
| edges | Array<{callerId: string, calleeId: string}> | List of edges representing function calls |

**Relationships:**

- contains FunctionNode (SerializedCallGraph contains nodes of type FunctionNode)

### FileSignatureMap

Represents a map of file signatures.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| path | string | File path |
| entries | Array<{signature: string, docstring: string}> | List of signatures and their docstrings |

### ClusterSummary

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a summary of a module cluster
in the architecture.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the cluster |
| name | string | Name of the cluster |
| fileCount | number | Number of files in the cluster |
| role | string | Architectural role of the cluster |
| entryPointCount | number | Number of entry points in the cluster |
| hubCount | number | Number of hubs in the cluster |
| dependsOn | string[] | List of cluster IDs that this cluster depends on |
| keyFiles | string[] | List of key files in the cluster (hubs and entries) |

### ArchitectureOverview

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents an overview of the entire
architecture.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| generatedAt | string | Timestamp when the overview was generated |
| summary | object | Summary statistics of the architecture |
| clusters | ClusterSummary[] | List of cluster summaries |
| globalEntryPoints | object[] | List of global entry points |
| criticalHubs | object[] | List of critical hubs |

**Relationships:**

- contains ClusterSummary (ArchitectureOverview contains multiple ClusterSummaries)

### Cluster

[PARTIAL SPEC — file too large to fully analyze (2 parts)] A group of tightly coupled files detected
by static dependency analysis.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the cluster |
| name | string | Name of the cluster |
| role | string | Role of the cluster in the architecture |
| fileCount | number | Number of files in the cluster |
| dependsOn | array | IDs of clusters this cluster depends on |
| keyFiles | array | Key files in the cluster |

### EntryPoint

[PARTIAL SPEC — file too large to fully analyze (2 parts)] A function with no internal callers,
representing a public-facing root of the codebase.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the entry point function |
| file | string | File containing the entry point function |
| language | string | Programming language of the entry point function |

### CriticalHub

[PARTIAL SPEC — file too large to fully analyze (2 parts)] A high fan-in function that many callers
depend on.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the critical hub function |
| file | string | File containing the critical hub function |
| fanIn | number | Number of callers depending on this function |
| fanOut | number | Number of functions this function calls |

### DependencyNode

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents a node in the dependency
graph, typically a file in the project.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the node |
| file | ScoredFile | File information associated with the node |
| exports | ExportInfo[] | List of exports from the file |
| metrics | object | Metrics for the node including degrees and centrality measures |
| cluster | object | Cluster information if the node belongs to a cluster |

**Relationships:**

- has-a ScoredFile (DependencyNode contains a ScoredFile object)
- has-many ExportInfo (DependencyNode contains multiple ExportInfo objects)
- belongs to FileCluster (DependencyNode may belong to a FileCluster)

### DependencyEdge

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents an edge in the dependency
graph, typically an import or HTTP call between files.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| source | string | Source node ID |
| target | string | Target node ID |
| importedNames | string[] | Names of the imported entities |
| isTypeOnly | boolean | Indicates if the import is type-only |
| weight | number | Weight of the edge |
| httpEdge | HttpEdge | HTTP edge information if the edge is derived from an HTTP call |
| isCallEdge | boolean | Indicates if the edge is synthesized from a call-graph cross-file call |

**Relationships:**

- has-a HttpEdge (DependencyEdge may contain an HttpEdge object)

### FileCluster

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents a cluster of related files in
the dependency graph.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the cluster |
| name | string | Name of the cluster |
| files | string[] | List of file IDs in the cluster |
| internalEdges | number | Number of internal edges within the cluster |
| externalEdges | number | Number of external edges from the cluster |
| cohesion | number | Cohesion metric for the cluster |
| coupling | number | Coupling metric for the cluster |
| suggestedDomain | string | Suggested domain for the cluster |
| color | string | Color associated with the cluster |
| isStructural | boolean | Indicates if the cluster has at least one internal edge |

### DependencyGraphResult

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents the complete dependency graph
result, including nodes, edges, clusters, and rankings.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| nodes | DependencyNode[] | List of nodes in the graph |
| edges | DependencyEdge[] | List of edges in the graph |
| clusters | FileCluster[] | List of all clusters in the graph |
| structuralClusters | FileCluster[] | List of structural clusters with internal edges |
| rankings | object | Rankings of nodes and clusters |
| cycles | string[][] | List of cycles in the graph |
| statistics | object | Statistics about the graph |

**Relationships:**

- has-many DependencyNode (DependencyGraphResult contains multiple DependencyNode objects)
- has-many DependencyEdge (DependencyGraphResult contains multiple DependencyEdge objects)
- has-many FileCluster (DependencyGraphResult contains multiple FileCluster objects)

### DependencyGraphOptions

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents options for building the
dependency graph.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| rootDir | string | Root directory of the project |
| extensions | string[] | File extensions to consider for import resolution |
| minClusterSize | number | Minimum cluster size to report |
| dampingFactor | number | PageRank damping factor |
| maxIterations | number | Maximum PageRank iterations |

### DependencyGraph

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents a graph structure that maps
file dependencies and relationships within a codebase.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| nodes | Map<string, DependencyNode> | A map of file paths to DependencyNode objects representing each file in the graph. |
| edges | DependencyEdge[] | An array of DependencyEdge objects representing the relationships between files. |
| adjacencyList | Map<string, Set<string>> | A map of file paths to sets of file paths that they depend on. |
| reverseAdjacencyList | Map<string, Set<string>> | A map of file paths to sets of file paths that depend on them. |
| httpEdgeCount | number | The count of HTTP edges in the graph. |
| parser | ImportExportParser | An instance of ImportExportParser used to parse files for imports and exports. |
| options | Required<DependencyGraphOptions> | Configuration options for the dependency graph builder. |

**Relationships:**

- contains DependencyNode (The DependencyGraph contains multiple DependencyNode objects.)
- contains DependencyEdge (The DependencyGraph contains multiple DependencyEdge objects.)
- contains FileCluster (The DependencyGraph contains multiple FileCluster objects.)
- uses ImportExportParser (The DependencyGraph uses an ImportExportParser to parse files.)

### SpecRecord

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a record in the specification
vector index, containing metadata and embedding information.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the record |
| domain | string | Domain category of the specification |
| section | string | Section type of the specification |
| title | string | Title of the specification section |
| text | string | Concatenated text used for embedding |
| linkedFiles | string | JSON-encoded string array of source files linked to this record |
| vector | number[] | Embedding vector for the record |

### SpecSearchResult

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents the result of a search query
in the specification vector index.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| record | object | The specification record found |
| score | number | Relevance score of the search result |

**Relationships:**

- contains SpecRecord (SpecSearchResult contains a SpecRecord)

### ParsedSection

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a parsed section from a
specification Markdown file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| section | string | Section type |
| title | string | Title of the section |
| content | string | Content of the section |
| requirementKey | string | Slugified requirement key |

### SpecVectorIndex

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Builds and queries a LanceDB vector index
over OpenSpec spec files. Each record represents a logical section (requirement, purpose, design
note...) parsed from the Markdown spec files.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| outputDir | string | Path to the output directory where the vector index is stored |
| specsDir | string | Path to the directory containing OpenSpec spec files |
| mappingJsonPath | string | Path to the mapping.json file for enrichment |
| query | string | The search query for semantic search |
| limit | number | The maximum number of results to return |
| domain | string | Filter results by domain |
| section | string | Filter results by section |

**Relationships:**

- uses EmbeddingService (SpecVectorIndex uses EmbeddingService to generate embeddings)
- stores-in LanceDB (SpecVectorIndex stores data in LanceDB)

### FileWalker

[PARTIAL SPEC — file too large to fully analyze (3 parts)] A utility for traversing and analyzing
files in a directory structure.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| maxFiles | number | Maximum number of files to process |
| includePatterns | string[] | Additional glob patterns to include |
| excludePatterns | string[] | Additional glob patterns to exclude |
| onProgress | function | Progress callback for UI updates |
| signal | AbortSignal | AbortController signal for cancellation |
| concurrency | number | Maximum concurrent file reads |

### FileWalkerProgress

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Progress information during file walking.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| filesFound | number | Number of files found so far |
| directoriesScanned | number | Number of directories scanned so far |
| currentPath | string | Current path being processed |

### FileMetadata

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Metadata about a file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| path | string | Absolute path to the file |
| relativePath | string | Path relative to the root directory |
| size | number | File size in bytes |
| lines | number | Number of lines in the file (-1 for files larger than MAX_READ_SIZE) |
| isEntryPoint | boolean | Whether the file is likely an entry point |
| isTestFile | boolean | Whether the file is a test file |
| isConfigFile | boolean | Whether the file is a configuration file |
| isGenerated | boolean | Whether the file is likely generated |
| hasShebang | boolean | Whether the file has a shebang line |

### FileWalkerResult

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Result of file walking.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| files | FileMetadata[] | Array of file metadata |
| stats | object | Statistics about the file walking process |

### ImportMap

A map that stores the resolved source file paths for imported names in each file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| filePath | string | The path of the file containing the imports. |
| localName | string | The local name of the imported entity. |
| resolvedSourceFilePath | string | The resolved path of the source file for the imported entity. |

### FileAnalysis

Represents the analysis of a file, including its imports.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| filePath | string | The path of the file being analyzed. |
| imports | Import[] | An array of imports found in the file. |

### Import

Represents an import statement in a file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| isRelative | boolean | Indicates if the import is relative. |
| source | string | The source path of the import. |
| importedNames | string[] | An array of names imported from the source. |

### RefactorEntry

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a function with refactoring
issues and their priority score.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| function | string | Name of the function |
| file | string | Path to the file containing the function |
| className | string | Name of the class containing the function |
| fanIn | number | Number of incoming calls to the function |
| fanOut | number | Number of outgoing calls from the function |
| depth | number | Hops from nearest entry point. -1 = unreachable |
| sccSize | number | SCC size. 1 = no cycle. >1 = part of a cycle |
| requirements | string[] | Requirement names this function is mapped to |
| issues | RefactorIssue[] | List of refactoring issues |
| priorityScore | number | Composite priority score for sorting (higher = more urgent) |

**Relationships:**

- belongs to RefactorReport (RefactorEntry is part of a refactor report)

### CycleSummary

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Summary of a strongly connected component
(cycle) in the call graph.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| sccId | number | Unique identifier for the SCC |
| size | number | Number of functions in the SCC |
| participants | Array<{ function: string; file: string }> | List of functions participating in the cycle |

**Relationships:**

- belongs to RefactorReport (CycleSummary is part of a refactor report)

### RefactorReport

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Report of refactoring issues and
statistics.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| generatedAt | string | Timestamp when the report was generated |
| stats | object | Statistics about the refactoring issues |
| priorities | RefactorEntry[] | Functions with at least one issue, sorted by priorityScore descending |
| cycles | CycleSummary[] | List of detected cycles |

### FunctionRecord

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a function with its metadata
and embedding vector for semantic search.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the function |
| name | string | Name of the function |
| filePath | string | Path to the file containing the function |
| className | string | Name of the class containing the function (if applicable) |
| language | string | Programming language of the function |
| signature | string | Function signature |
| docstring | string | Documentation string for the function |
| fanIn | number | Number of incoming calls to the function |
| fanOut | number | Number of outgoing calls from the function |
| isHub | boolean | Indicates if the function is a hub in the call graph |
| isEntryPoint | boolean | Indicates if the function is an entry point |
| text | string | Concatenated text used for embedding |
| vector | number[] | Embedding vector for the function |

### SearchResult

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents the result of a search query
with a function record and relevance score.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| record | Omit<FunctionRecord, 'vector'> | Function record without the vector field |
| score | number | Relevance score of the search result |

**Relationships:**

- contains FunctionRecord (Search result contains a function record)

### Bm25Corpus

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a corpus for BM25 sparse
retrieval, including term frequency maps and document statistics.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| docs | Array<{ id: string; tfMap: Map<string, number>; length: number }> | Array of documents with term frequency maps |
| df | Map<string, number> | Term to document frequency map |
| avgLength | number | Average document length |
| N | number | Total number of documents |

### VectorIndex

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Builds and queries a LanceDB vector index
over the call graph functions.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| outputDir | string | Directory where the vector index is stored |
| nodes | FunctionNode[] | Array of function nodes from the call graph |
| signatures | FileSignatureMap[] | Array of file signature maps |
| hubIds | Set<string> | Set of hub function IDs |
| entryPointIds | Set<string> | Set of entry point function IDs |
| embedSvc | EmbeddingService | Service for generating embeddings |
| fileContents | Map<string, string> | Optional map of file paths to source content |
| incremental | boolean | Whether to reuse cached vectors for unchanged functions |
| query | string | Search query for the vector index |
| limit | number | Maximum number of results to return |
| language | string | Optional language filter for search results |
| minFanIn | number | Minimum fan-in value for search results |
| hybrid | boolean | Whether to use hybrid dense+sparse retrieval |

**Relationships:**

- uses FunctionNode (VectorIndex uses FunctionNode data to build the index)
- uses FileSignatureMap (VectorIndex uses FileSignatureMap data to build the index)
- uses EmbeddingService (VectorIndex uses EmbeddingService to generate embeddings)

### ImportInfo

[PARTIAL SPEC — file too large to fully analyze (5 parts)] Information about an import statement in
a file

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| source | string | The source path of the import |
| isRelative | boolean | Whether the import is relative |
| isPackage | boolean | Whether the import is from a package |
| isBuiltin | boolean | Whether the import is a Node.js builtin |
| importedNames | string[] | Names being imported |
| hasDefault | boolean | Whether a default import exists |
| hasNamespace | boolean | Whether a namespace import exists |
| isTypeOnly | boolean | Whether the import is type-only |
| isDynamic | boolean | Whether the import is dynamic |
| line | number | Line number where the import occurs |

### ExportInfo

[PARTIAL SPEC — file too large to fully analyze (5 parts)] Information about an export statement in
a file

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the exported entity |
| isDefault | boolean | Whether it's a default export |
| isType | boolean | Whether it's a type export |
| isReExport | boolean | Whether it's a re-export |
| reExportSource | string | Source of re-export if applicable |
| kind | 'function' | 'class' | 'variable' | 'type' | 'interface' | 'enum' | 'unknown' | Type of exported entity |
| line | number | Line number where the export occurs |

### ResolveOptions

[PARTIAL SPEC — file too large to fully analyze (5 parts)] Options for import resolution

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| baseDir | string | Base directory for resolving relative imports |
| pathAliases | Record<string, string[]> | TypeScript paths aliases from tsconfig |
| extensions | string[] | File extensions to try when resolving |

### ImportExportParser

[PARTIAL SPEC — file too large to fully analyze (5 parts)] Parses source files to extract imports
and exports. Supports JavaScript/TypeScript and Python.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| cache | Map<string, FileAnalysis> | Stores parsed file analyses to avoid reprocessing |

**Relationships:**

- contains FileAnalysis (Parser produces FileAnalysis objects for each file)

### Export

[PARTIAL SPEC — file too large to fully analyze (5 parts)] Represents an export statement in a
source file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | The name of the exported entity |
| type | string | The type of export (e.g., 'named', 'default') |

### ExtractedSignature

Represents a signature extracted from a source code file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the signature |
| type | string | Type of the signature (e.g., function, interface) |
| parameters | array | List of parameters for the signature |
| returnType | string | Return type of the signature |
| language | string | Programming language of the signature |

### SignatureExtractor

Service for extracting signatures from source code files.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| STAGE1_MAX_CHARS | number | Maximum number of characters for Stage 1 LLM prompt |

**Relationships:**

- creates ExtractedSignature (Extracts signatures from source code)
- creates FileSignatureMap (Creates file signature maps)

### AnalysisArtifacts

Collection of artifacts generated during code analysis.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| repoStructure | RepoStructure | Repository structure artifact |
| summaryMarkdown | string | Summary in Markdown format |
| dependencyDiagram | string | Dependency diagram in Mermaid format |
| llmContext | LLMContext | Context for LLM analysis |

**Relationships:**

- derived-from RepositoryMap (AnalysisArtifacts are derived from a RepositoryMap)
- derived-from DependencyGraph (AnalysisArtifacts are derived from a DependencyGraph)

### ArchitectureLayer

Represents a layer in the architecture of a codebase.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the layer |
| description | string | Description of the layer |
| directories | string[] | List of directories belonging to the layer |

**Relationships:**

- belongs to RepositoryMap (ArchitectureLayer belongs to a RepositoryMap)

### DetectedDomain

Represents a detected domain in the codebase, mapping to an OpenSpec specification.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the domain |
| description | string | Description of the domain |
| entities | Entity[] | List of entities in the domain |

**Relationships:**

- belongs to RepositoryMap (DetectedDomain belongs to a RepositoryMap)
- belongs to DependencyGraph (DetectedDomain belongs to a DependencyGraph)

### CppInclude

Represents a C++ #include directive parsed from a source file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| headerPath | string | Path to the included header file |
| isRelative | boolean | Whether the include is relative (e.g., "header.h") |
| isSystem | boolean | Whether the include is a system header (e.g., <iostream>) |

### HeaderToImplMap

Maps header files to their corresponding implementation files based on naming conventions.

**Relationships:**

- uses CppInclude (Uses CppInclude to determine accessible implementation files)

### CppImportMap

Maps C++ source files to sets of implementation files reachable via their #include directives.

**Relationships:**

- uses CppInclude (Uses CppInclude to determine accessible implementation files)
- uses HeaderToImplMap (Uses HeaderToImplMap to resolve header paths to implementation files)

### CallGraphResult

Represents the result of call graph analysis, containing nodes and edges.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| nodes | FunctionNode[] | Collection of function nodes in the graph |
| edges | CallEdge[] | Collection of call edges between nodes |
| classNodes | ClassNode[] | Collection of class nodes in the graph |
| inheritanceEdges | InheritanceEdge[] | Collection of inheritance edges between class nodes |
| layerViolations | LayerViolation[] | Collection of detected layer violations |

**Relationships:**

- contains FunctionNode (CallGraphResult contains multiple FunctionNodes)
- contains CallEdge (CallGraphResult contains multiple CallEdges)
- contains ClassNode (CallGraphResult contains multiple ClassNodes)
- contains InheritanceEdge (CallGraphResult contains multiple InheritanceEdges)
- contains LayerViolation (CallGraphResult contains multiple LayerViolations)

### CallEdge

Represents a call relationship between two FunctionNodes.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| source | string | ID of the calling FunctionNode |
| target | string | ID of the called FunctionNode |
| confidence | EdgeConfidence | Confidence level of the call relationship |
| lineNumber | number | Line number where the call occurs |

**Relationships:**

- connects FunctionNode (CallEdge connects two FunctionNodes)

### ClassNode

Represents a class or interface in the call graph.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| id | string | Unique identifier for the class |
| name | string | Name of the class |
| qualifiedName | string | Fully qualified name including namespace |
| filePath | string | Path to the source file containing the class |
| startLine | number | Starting line number in the file |
| endLine | number | Ending line number in the file |
| docstring | string | Documentation string for the class |
| methods | FunctionNode[] | Collection of method nodes belonging to this class |

**Relationships:**

- contains FunctionNode (ClassNode contains multiple FunctionNodes (methods))
- connected-to InheritanceEdge (ClassNode is connected to other ClassNodes via InheritanceEdges)

### InheritanceEdge

Represents an inheritance or implementation relationship between two ClassNodes.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| source | string | ID of the subclass ClassNode |
| target | string | ID of the superclass ClassNode |
| type | string | Type of relationship (inheritance or implementation) |

**Relationships:**

- connects ClassNode (InheritanceEdge connects two ClassNodes)

### LayerViolation

Represents a violation of architectural layer boundaries.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| sourceLayer | string | Name of the source layer |
| targetLayer | string | Name of the target layer |
| sourceNode | string | ID of the source FunctionNode |
| targetNode | string | ID of the target FunctionNode |
| severity | string | Severity level of the violation |

**Relationships:**

- references FunctionNode (LayerViolation references source and target FunctionNodes)

### CloneInstance

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents an instance of a code clone
found in the source code.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| file | string | The file path where the clone instance is located |
| functionName | string | The name of the function containing the clone |
| className | string | The name of the class containing the clone, if applicable |
| startLine | number | The starting line number of the clone instance |
| endLine | number | The ending line number of the clone instance |

### CloneGroup

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a group of code clones that
are similar to each other.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| type | CloneType | The type of clone (exact, structural, or near) |
| similarity | number | The similarity score between the clones (1.0 for exact/structural, Jaccard similarity for near) |
| instances | CloneInstance[] | An array of CloneInstance objects representing the instances of the clones in the group |
| lineCount | number | The number of lines in the smallest instance of the cloned block |

**Relationships:**

- has-many CloneInstance (A CloneGroup contains multiple CloneInstance objects)

### DuplicateDetectionResult

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents the result of a duplicate
detection analysis.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| cloneGroups | CloneGroup[] | An array of CloneGroup objects representing the groups of code clones found |
| stats | object | An object containing statistics about the duplicate detection analysis |

**Relationships:**

- has-many CloneGroup (A DuplicateDetectionResult contains multiple CloneGroup objects)

### TypeInferenceEngine

Engine for inferring types from source code using regex patterns for multiple programming languages.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| source | string | Source code to analyze |
| language | string | Programming language of the source code |

**Relationships:**

- uses FunctionRegistryTrie (Uses FunctionRegistryTrie to resolve method calls)
- returns FunctionNode (Returns FunctionNode instances for resolved method calls)

### InferredTypes

Map of variable names to inferred class names.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| variableName | string | Name of the variable |
| className | string | Inferred class name for the variable |

### ScoringConfig

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Configuration for custom scoring rules in
the significance analyzer

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| highValueNames | Record<string, number> | Custom high-value name patterns with scores |
| negativeNames | Record<string, number> | Custom negative name patterns with scores |
| highValuePaths | Record<string, number> | Custom high-value path patterns with scores |
| minScore | number | Minimum score to include in results |

### FileRelationship

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Import/export relationship for
connectivity scoring

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| filePath | string | Path to the file |
| imports | string[] | List of files this file imports |
| importedBy | string[] | List of files that import this file |

### ScoredFile

[PARTIAL SPEC — file too large to fully analyze (2 parts)] A file with its calculated significance
score

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| path | string | Absolute path to the file |
| score | number | Calculated significance score |
| nameScore | number | Name-based score component |
| pathScore | number | Path-based score component |
| structureScore | number | Structure-based score component |
| connectivityScore | number | Connectivity-based score component |

### SignificanceScorer

[PARTIAL SPEC — file too large to fully analyze (2 parts)] A class responsible for scoring files
based on their significance to the system architecture.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| config | ScoringConfig | Configuration for the scoring algorithm |
| relationships | Map<string, FileRelationship> | Map of file relationships for connectivity scoring |

**Relationships:**

- uses FileMetadata (SignificanceScorer uses FileMetadata to score files)

### HttpCall

[PARTIAL SPEC — file too large to fully analyze (4 parts)] Represents an HTTP call found in a
JavaScript/TypeScript source file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| file | string | Absolute path of the file containing the call |
| method | string | HTTP method, upper-cased. 'UNKNOWN' when it cannot be determined. |
| url | string | URL as written in source — may be a template literal or variable ref |
| normalizedUrl | string | Normalised, static portion of the URL (params stripped, prefix removed) |
| line | number | 1-based source line |
| client | string | HTTP client used (axios / fetch / ky / got / custom) |

### RouteDefinition

[PARTIAL SPEC — file too large to fully analyze (4 parts)] Represents a route handler found in a
Python source file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| file | string | Absolute path of the file containing the handler |
| method | string | HTTP method, upper-cased |
| path | string | Path pattern as declared (may contain {param} or <param> placeholders) |
| normalizedPath | string | Normalised path for matching |
| handlerName | string | Name of the handler function |
| framework | string | Framework used (fastapi / flask / django / starlette) |
| line | number | 1-based source line |

### HttpEdge

[PARTIAL SPEC — file too large to fully analyze (4 parts)] Represents a resolved cross-language edge
between an HTTP call and a route definition.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| callerFile | string | Absolute path of the JS/TS caller file |
| handlerFile | string | Absolute path of the Python handler file |
| method | string | HTTP method |
| path | string | Normalised path used for the match |
| confidence | string | How confident the match is (exact / path / fuzzy) |

**Relationships:**

- has-one HttpCall (HttpEdge contains an HttpCall)
- has-one RouteDefinition (HttpEdge contains a RouteDefinition)

### HTTP Call

[PARTIAL SPEC — file too large to fully analyze (4 parts)] Represents an HTTP call made from a
frontend file to a backend endpoint.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| file | string | Path to the file containing the HTTP call |
| method | string | HTTP method (GET, POST, etc.) |
| url | string | Original URL of the HTTP call |
| normalizedUrl | string | Normalized URL for matching purposes |
| line | number | Line number in the file where the call is made |
| client | string | HTTP client used (fetch, axios, ky, got) |

**Relationships:**

- calls HTTP Route (HTTP call is made to an HTTP route)

### FunctionRegistryTrie

A three-way index structure for efficient lookup of FunctionNodes by name, qualified name, or ID.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| byName | Map<string, FunctionNode[]> | Map of FunctionNodes indexed by simple name |
| byQualified | Map<string, FunctionNode[]> | Map of FunctionNodes indexed by qualified name (ClassName.methodName) |
| byId | Map<string, FunctionNode> | Map of FunctionNodes indexed by unique ID |

**Relationships:**

- contains FunctionNode (FunctionRegistryTrie contains multiple FunctionNodes)

## Requirements

### Requirement: FrameworkDetectorValidation

The system SHALL validate FrameworkDetector according to these rules:
- Framework name must be unique
- Category must be one of: 'frontend', 'backend', 'testing', 'database', 'auth'

#### Scenario: DetectReactFramework
- **GIVEN** A project with 'react' in dependencies and .jsx/.tsx files
- **WHEN** Framework detection is run
- **THEN** React framework is detected with high confidence

#### Scenario: DetectNext.jsFramework
- **GIVEN** A project with 'next' in dependencies and pages/ or app/ directory
- **WHEN** Framework detection is run
- **THEN** Next.js framework is detected with high confidence

### Requirement: EmbeddingServiceValidation

The system SHALL validate EmbeddingService according to these rules:
- EMBED_BASE_URL and EMBED_MODEL must be set when using fromEnv()
- Texts longer than MAX_CHARS_PER_TEXT (24000) are truncated
- Embeddings are returned in the same order as input texts

#### Scenario: ComputeEmbeddingsForTexts
- **GIVEN** A list of texts
- **WHEN** embed() is called
- **THEN** Returns one embedding vector per input text in the same order
- **AND** Texts are batched according to batchSize
- **AND** Long texts are truncated to MAX_CHARS_PER_TEXT characters

#### Scenario: CreateEmbeddingserviceFromEnvironmentVariables
- **GIVEN** Environment variables EMBED_BASE_URL and EMBED_MODEL are set
- **WHEN** fromEnv() is called
- **THEN** Returns an EmbeddingService instance configured with the environment variables

#### Scenario: CreateEmbeddingserviceFromSpecgenconfig
- **GIVEN** A SpecGenConfig with embedding configuration
- **WHEN** fromConfig() is called
- **THEN** Returns an EmbeddingService instance configured with the SpecGenConfig
- **AND** Returns null if no embedding config is present in SpecGenConfig

### Requirement: ClusterSummaryValidation

The system SHALL validate ClusterSummary according to these rules:
- fileCount must be a positive integer
- entryPointCount must be a non-negative integer
- hubCount must be a non-negative integer
- dependsOn must be an array of valid cluster IDs
- keyFiles must be an array of relative file paths

#### Scenario: CreateClustersummary
- **GIVEN** A module cluster with files, hubs, and entry points
- **WHEN** buildArchitectureOverview is called
- **THEN** A ClusterSummary is created with the cluster's details

### Requirement: ArchitectureOverviewValidation

The system SHALL validate ArchitectureOverview according to these rules:
- generatedAt must be a valid ISO string
- summary must contain valid statistics
- clusters must be an array of ClusterSummary objects
- globalEntryPoints must be an array of entry point objects
- criticalHubs must be an array of hub objects

#### Scenario: GenerateArchitectureoverview
- **GIVEN** Dependency graph and LLM context
- **WHEN** buildArchitectureOverview is called
- **THEN** An ArchitectureOverview is generated with the architecture details

### Requirement: DependencyNodeValidation

The system SHALL validate DependencyNode according to these rules:
- id must be a non-empty string
- file must be a valid ScoredFile object
- exports must be an array of ExportInfo objects
- metrics must contain inDegree, outDegree, betweenness, and pageRank properties

#### Scenario: CreateDependencynode
- **GIVEN** A file with exports and metrics
- **WHEN** DependencyNode is instantiated
- **THEN** A new DependencyNode is created with the provided file, exports, and metrics

### Requirement: DependencyEdgeValidation

The system SHALL validate DependencyEdge according to these rules:
- source and target must be non-empty strings
- importedNames must be an array of strings
- weight must be a positive number

#### Scenario: CreateDependencyedge
- **GIVEN** Source and target node IDs, imported names, and weight
- **WHEN** DependencyEdge is instantiated
- **THEN** A new DependencyEdge is created with the provided source, target, imported names, and weight

### Requirement: FileClusterValidation

The system SHALL validate FileCluster according to these rules:
- id and name must be non-empty strings
- files must be an array of strings
- internalEdges and externalEdges must be non-negative numbers
- cohesion and coupling must be numbers between 0 and 1
- color must be a valid hex color code

#### Scenario: CreateFilecluster
- **GIVEN** Cluster ID, name, files, and metrics
- **WHEN** FileCluster is instantiated
- **THEN** A new FileCluster is created with the provided ID, name, files, and metrics

### Requirement: DependencyGraphResultValidation

The system SHALL validate DependencyGraphResult according to these rules:
- nodes must be an array of DependencyNode objects
- edges must be an array of DependencyEdge objects
- clusters must be an array of FileCluster objects
- structuralClusters must be an array of FileCluster objects with internalEdges > 0
- rankings must contain byImportance, byConnectivity, clusterCenters, leafNodes, bridgeNodes, and orphanNodes properties
- cycles must be an array of arrays of strings
- statistics must contain nodeCount, edgeCount, importEdgeCount, httpEdgeCount, avgDegree, density, clusterCount, structuralClusterCount, and cycleCount properties

#### Scenario: CreateDependencygraphresult
- **GIVEN** Nodes, edges, clusters, rankings, cycles, and statistics
- **WHEN** DependencyGraphResult is instantiated
- **THEN** A new DependencyGraphResult is created with the provided nodes, edges, clusters, rankings, cycles,
and statistics

### Requirement: DependencyGraphOptionsValidation

The system SHALL validate DependencyGraphOptions according to these rules:
- rootDir must be a non-empty string
- extensions must be an array of strings if provided
- minClusterSize must be a positive number if provided
- dampingFactor must be a number between 0 and 1 if provided
- maxIterations must be a positive number if provided

#### Scenario: CreateDependencygraphoptions
- **GIVEN** Root directory and optional parameters
- **WHEN** DependencyGraphOptions is instantiated
- **THEN** A new DependencyGraphOptions is created with the provided root directory and optional parameters

### Requirement: DependencyGraphValidation

The system SHALL validate DependencyGraph according to these rules:
- Nodes must be unique by file path.
- Edges must connect existing nodes.
- HTTP edges must have a confidence level of 'exact', 'path', or 'fuzzy'.

#### Scenario: BuildDependencyGraph
- **GIVEN** A list of ScoredFile objects
- **WHEN** build() is called
- **THEN** A DependencyGraphResult is returned containing nodes, edges, clusters, rankings, cycles, and
statistics.

#### Scenario: ParseFiles
- **GIVEN** A list of ScoredFile objects
- **WHEN** parseFiles() is called
- **THEN** A map of file paths to FileAnalysis objects is returned.

#### Scenario: BuildHttpCrossEdges
- **GIVEN** A list of ScoredFile objects
- **WHEN** buildHttpCrossEdges() is called
- **THEN** HTTP edges are added to the graph connecting JavaScript/TypeScript files to Python files.

#### Scenario: BuildEdges
- **GIVEN** A list of ScoredFile objects and a map of file paths to FileAnalysis objects
- **WHEN** buildEdges() is called
- **THEN** Edges are added to the graph based on import relationships.

#### Scenario: CalculateDegrees
- **GIVEN** A populated adjacency list and reverse adjacency list
- **WHEN** calculateDegrees() is called
- **THEN** In-degree and out-degree metrics are calculated for each node.

#### Scenario: CalculateBetweenness
- **GIVEN** A populated adjacency list
- **WHEN** calculateBetweenness() is called
- **THEN** Betweenness centrality metrics are calculated for each node.

#### Scenario: CalculatePagerank
- **GIVEN** A populated adjacency list and reverse adjacency list
- **WHEN** calculatePageRank() is called
- **THEN** PageRank metrics are calculated for each node.

#### Scenario: DetectClusters
- **GIVEN** A populated adjacency list and reverse adjacency list
- **WHEN** detectClusters() is called
- **THEN** File clusters are detected based on directory and connectivity.

#### Scenario: DetectCycles
- **GIVEN** A populated adjacency list
- **WHEN** detectCycles() is called
- **THEN** Cycles in the dependency graph are detected.

#### Scenario: GenerateRankings
- **GIVEN** A list of FileCluster objects
- **WHEN** generateRankings() is called
- **THEN** Various rankings of nodes are generated.

#### Scenario: CalculateStatistics
- **GIVEN** A list of FileCluster objects and a list of cycles
- **WHEN** calculateStatistics() is called
- **THEN** Overall statistics about the graph are calculated.

### Requirement: SpecRecordValidation

The system SHALL validate SpecRecord according to these rules:
- id must be a unique string
- section must be one of 'requirements', 'purpose', 'entities', 'design', 'architecture', or 'other'
- linkedFiles must be a valid JSON-encoded string array
- vector must be a non-empty array of numbers

#### Scenario: CreateSpecrecord
- **GIVEN** Valid specification data
- **WHEN** SpecRecord is instantiated
- **THEN** A new SpecRecord is created with the provided data

### Requirement: SpecSearchResultValidation

The system SHALL validate SpecSearchResult according to these rules:
- score must be a number between 0 and 1
- record must be a valid SpecRecord object

#### Scenario: SearchSpecifications
- **GIVEN** A query and a populated vector index
- **WHEN** search is performed
- **THEN** A list of SpecSearchResult objects is returned with relevant records and scores

### Requirement: ParsedSectionValidation

The system SHALL validate ParsedSection according to these rules:
- section must be a non-empty string
- title must be a non-empty string
- content must be a non-empty string
- requirementKey must be a non-empty string if section is 'requirements'

#### Scenario: ParseSpecificationFile
- **GIVEN** A Markdown file content
- **WHEN** parseSpecFile is called
- **THEN** An array of ParsedSection objects is returned

### Requirement: SpecVectorIndexValidation

The system SHALL validate SpecVectorIndex according to these rules:
- outputDir must be a valid path
- specsDir must contain spec.md files
- mappingJsonPath must be a valid JSON file if provided
- query must be a non-empty string
- limit must be a positive integer if provided

#### Scenario: BuildSpecvectorindex
- **GIVEN** A directory containing spec.md files and an optional mapping.json file
- **WHEN** SpecVectorIndex.build is called
- **THEN** A LanceDB table named 'specs' is created with embedded records

#### Scenario: SearchSpecvectorindex
- **GIVEN** An existing SpecVectorIndex
- **WHEN** SpecVectorIndex.search is called with a query
- **THEN** A list of matching spec records is returned with relevance scores

### Requirement: FileWalkerValidation

The system SHALL validate FileWalker according to these rules:
- maxFiles must be a positive integer
- includePatterns and excludePatterns must be valid glob patterns
- onProgress must be a function that accepts FileWalkerProgress

#### Scenario: WalkDirectoryStructure
- **GIVEN** A root directory path
- **WHEN** FileWalker is instantiated and walk() is called
- **THEN** All files in the directory structure are processed according to the options

#### Scenario: SkipIgnoredFiles
- **GIVEN** A directory with .gitignore and .spec-gen-ignore files
- **WHEN** FileWalker processes the directory
- **THEN** Files matching patterns in the ignore files are skipped

#### Scenario: HandleLargeFiles
- **GIVEN** A file larger than MAX_READ_SIZE
- **WHEN** FileWalker processes the file
- **THEN** The file is skipped for line counting and shebang detection

### Requirement: ImportMapValidation

The system SHALL validate ImportMap according to these rules:
- Each file path in the ImportMap must be unique.
- Each local name within a file must be unique.

#### Scenario: BuildImportmap
- **GIVEN** A list of FileAnalysis objects
- **WHEN** buildImportMap is called
- **THEN** An ImportMap is created with resolved source file paths for each imported name in each file

#### Scenario: FindCalleeFile
- **GIVEN** An ImportMap, a caller file path, and a callee name
- **WHEN** findCalleeFileViaImport is called
- **THEN** The resolved source file path for the callee name is returned if it exists in the ImportMap

### Requirement: FileAnalysisValidation

The system SHALL validate FileAnalysis according to these rules:
- filePath must be a valid file path.
- imports must be an array of Import objects.

#### Scenario: ValidFileAnalysisAccepted
- **GIVEN** A valid FileAnalysis object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: ImportValidation

The system SHALL validate Import according to these rules:
- source must be a valid file path.
- importedNames must be an array of strings.

#### Scenario: ValidImportAccepted
- **GIVEN** A valid Import object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: RefactorEntryValidation

The system SHALL validate RefactorEntry according to these rules:
- depth must be a non-negative integer or -1
- sccSize must be a positive integer
- priorityScore must be a non-negative number

#### Scenario: CreateRefactorEntry
- **GIVEN** Function details and issues
- **WHEN** RefactorEntry is created
- **THEN** RefactorEntry is initialized with provided details

### Requirement: CycleSummaryValidation

The system SHALL validate CycleSummary according to these rules:
- size must be a positive integer
- participants must contain at least two functions

#### Scenario: DetectCycle
- **GIVEN** Functions forming a cycle
- **WHEN** computeSCCs is called
- **THEN** CycleSummary is created with details of the cycle

### Requirement: RefactorReportValidation

The system SHALL validate RefactorReport according to these rules:
- generatedAt must be a valid timestamp
- stats must contain valid statistics
- priorities must be sorted by priorityScore in descending order

#### Scenario: GenerateRefactorReport
- **GIVEN** Call graph and refactoring issues
- **WHEN** RefactorReport is generated
- **THEN** RefactorReport is created with statistics and issues

### Requirement: VectorIndexValidation

The system SHALL validate VectorIndex according to these rules:
- Nodes array must not be empty when building the index
- Embedding count must match the number of functions to embed
- Vector index must exist before performing searches
- Embedding service must be available for dense search

#### Scenario: BuildVectorIndex
- **GIVEN** Function nodes, signatures, hub IDs, and entry point IDs
- **WHEN** VectorIndex.build() is called
- **THEN** A vector index is created or updated with the provided data

#### Scenario: SearchVectorIndex
- **GIVEN** An existing vector index
- **WHEN** VectorIndex.search() is called with a query
- **THEN** Relevant function records are returned based on the query

#### Scenario: IncrementalBuild
- **GIVEN** An existing vector index and unchanged functions
- **WHEN** VectorIndex.build() is called with incremental: true
- **THEN** Only changed functions are re-embedded, and unchanged functions reuse their cached vectors

#### Scenario: HybridSearch
- **GIVEN** An existing vector index and an embedding service
- **WHEN** VectorIndex.search() is called with hybrid: true
- **THEN** Results are combined using Reciprocal Rank Fusion (RRF) of dense and sparse retrieval

### Requirement: ImportInfoValidation

The system SHALL validate ImportInfo according to these rules:
- source must be a non-empty string
- line must be a positive integer

#### Scenario: ParseRelativeImport
- **GIVEN** An import statement with a relative path
- **WHEN** isRelativeImport() is called
- **THEN** Returns true

#### Scenario: ParseNode.jsBuiltin
- **GIVEN** An import statement with a Node.js builtin module
- **WHEN** isBuiltinModule() is called
- **THEN** Returns true

### Requirement: ExportInfoValidation

The system SHALL validate ExportInfo according to these rules:
- name must be a non-empty string
- kind must be one of the specified values
- line must be a positive integer

#### Scenario: IdentifyDefaultExport
- **GIVEN** An export statement with default keyword
- **WHEN** ExportInfo is created
- **THEN** isDefault is true

#### Scenario: IdentifyReExport
- **GIVEN** An export statement with from clause
- **WHEN** ExportInfo is created
- **THEN** isReExport is true and reExportSource is set

### Requirement: ResolveOptionsValidation

The system SHALL validate ResolveOptions according to these rules:
- baseDir must be a non-empty string
- pathAliases must be an object if provided
- extensions must be an array of strings if provided

#### Scenario: ResolveWithPathAliases
- **GIVEN** ResolveOptions with pathAliases provided
- **WHEN** Import resolution occurs
- **THEN** Uses the provided path aliases to resolve imports

#### Scenario: ResolveWithExtensions
- **GIVEN** ResolveOptions with extensions provided
- **WHEN** Import resolution occurs
- **THEN** Tries the provided extensions when resolving file paths

### Requirement: ImportExportParserValidation

The system SHALL validate ImportExportParser according to these rules:
- Supports JavaScript/TypeScript and Python file types
- Handles relative and absolute imports differently for Python
- Resolves imports with multiple file extensions
- Caches parsed files to improve performance

#### Scenario: ParseJavascriptFile
- **GIVEN** A JavaScript file with imports and exports
- **WHEN** parseFile() is called
- **THEN** FileAnalysis object is returned with imports and exports extracted
- **AND** Local imports are categorized separately from external imports
- **AND** File is cached for future use

#### Scenario: ParsePythonFile
- **GIVEN** A Python file with relative and absolute imports
- **WHEN** parseFile() is called
- **THEN** FileAnalysis object is returned with imports and exports extracted
- **AND** Python-specific import resolution is applied
- **AND** File is cached for future use

### Requirement: ExportValidation

The system SHALL validate Export according to these rules:
- Tracks both named and default exports
- Stores the name of each exported entity

#### Scenario: ValidExportAccepted
- **GIVEN** A valid Export object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: AnalysisArtifactsValidation

The system SHALL validate AnalysisArtifacts according to these rules:
- All artifacts must be generated from a valid RepositoryMap and DependencyGraph

#### Scenario: ArtifactGenerationAndSaving
- **GIVEN** A RepositoryMap and DependencyGraph
- **WHEN** generateAndSave() is called
- **THEN** AnalysisArtifacts are generated and saved to disk

### Requirement: ArchitectureLayerValidation

The system SHALL validate ArchitectureLayer according to these rules:
- Name must be unique within the RepositoryMap
- Directories must exist in the RepositoryMap

#### Scenario: ArchitectureLayerGeneration
- **GIVEN** A RepositoryMap with directories
- **WHEN** generateArchitectureLayers() is called
- **THEN** ArchitectureLayers are generated based on the directory structure

### Requirement: DetectedDomainValidation

The system SHALL validate DetectedDomain according to these rules:
- Name must be unique within the RepositoryMap
- Entities must be valid and exist in the codebase

#### Scenario: DomainGeneration
- **GIVEN** A RepositoryMap and DependencyGraph with clusters
- **WHEN** generateDomains() is called
- **THEN** DetectedDomains are generated based on the clusters

### Requirement: CppIncludeValidation

The system SHALL validate CppInclude according to these rules:
- headerPath must be a valid file path
- isRelative must be true for relative includes ("header.h")
- isSystem must be true for system headers (e.g., <iostream>)

#### Scenario: ParseRelativeInclude
- **GIVEN** A C++ file with a relative include directive (e.g., #include "header.h")
- **WHEN** parseCppIncludes() is called
- **THEN** A CppInclude object is created with isRelative=true and isSystem=false

#### Scenario: ParseSystemInclude
- **GIVEN** A C++ file with a system include directive (e.g., #include <iostream>)
- **WHEN** parseCppIncludes() is called
- **THEN** A CppInclude object is created with isRelative=false and isSystem=true

### Requirement: HeaderToImplMapValidation

The system SHALL validate HeaderToImplMap according to these rules:
- Header files must follow the convention of having a corresponding .cpp or .cc file with the same base name
- Implementation files must be in the same directory as their header files

#### Scenario: BuildHeaderToImplMap
- **GIVEN** A list of file paths including header and implementation files
- **WHEN** buildHeaderToImplMap() is called
- **THEN** A map is created where each header file path maps to its corresponding implementation file path

### Requirement: CppImportMapValidation

The system SHALL validate CppImportMap according to these rules:
- Only non-system headers should be considered for accessible implementation files
- Implementation files must be reachable via the header-to-impl map

#### Scenario: BuildC++ImportMap
- **GIVEN** A list of C++ source files and their contents
- **WHEN** buildCppImportMap() is called
- **THEN** A map is created where each C++ source file path maps to a set of accessible implementation file
paths

### Requirement: CallGraphResultValidation

The system SHALL validate CallGraphResult according to these rules:
- Nodes and edges must form a directed graph
- Class nodes must be properly connected via inheritance edges
- Layer violations must be detected based on defined layers

#### Scenario: BuildCallGraph
- **GIVEN** List of source files and optional layers
- **WHEN** CallGraphBuilder.build() is called
- **THEN** CallGraphResult is generated with nodes, edges, and class relationships

#### Scenario: DetectLayerViolations
- **GIVEN** Call graph with defined layers
- **WHEN** CallGraphBuilder.detectLayerViolations() is called
- **THEN** LayerViolations are identified and included in the result

### Requirement: CallEdgeValidation

The system SHALL validate CallEdge according to these rules:
- source and target must reference existing FunctionNodes
- lineNumber must be within the source FunctionNode's line range

#### Scenario: CreateCallEdge
- **GIVEN** Function call in source code
- **WHEN** Call graph is built
- **THEN** CallEdge is created between the calling and called FunctionNodes

### Requirement: ClassNodeValidation

The system SHALL validate ClassNode according to these rules:
- id must be unique across all ClassNodes
- filePath must point to an existing file
- startLine must be less than or equal to endLine

#### Scenario: CreateClassNode
- **GIVEN** Class declaration in source code
- **WHEN** Call graph is built
- **THEN** ClassNode is created with all relevant properties and its methods

### Requirement: InheritanceEdgeValidation

The system SHALL validate InheritanceEdge according to these rules:
- source and target must reference existing ClassNodes
- type must be either 'inheritance' or 'implementation'

#### Scenario: CreateInheritanceEdge
- **GIVEN** Inheritance or implementation relationship in source code
- **WHEN** Call graph is built
- **THEN** InheritanceEdge is created between the subclass and superclass ClassNodes

### Requirement: LayerViolationValidation

The system SHALL validate LayerViolation according to these rules:
- sourceLayer and targetLayer must be different
- sourceNode and targetNode must reference existing FunctionNodes
- severity must be one of: 'low', 'medium', 'high'

#### Scenario: DetectLayerViolation
- **GIVEN** Call graph with defined layers
- **WHEN** CallGraphBuilder.detectLayerViolations() is called
- **THEN** LayerViolation is created for each detected violation

### Requirement: CloneInstanceValidation

The system SHALL validate CloneInstance according to these rules:
- startLine must be less than or equal to endLine

#### Scenario: IdentifyCloneInstance
- **GIVEN** A function with duplicate code
- **WHEN** The duplicate detector analyzes the code
- **THEN** A CloneInstance is created for each occurrence of the duplicate code

### Requirement: CloneGroupValidation

The system SHALL validate CloneGroup according to these rules:
- similarity must be between 0 and 1
- lineCount must be a positive integer

#### Scenario: GroupSimilarClones
- **GIVEN** Multiple CloneInstance objects with similar code
- **WHEN** The duplicate detector analyzes the code
- **THEN** A CloneGroup is created for each group of similar clones

### Requirement: DuplicateDetectionResultValidation

The system SHALL validate DuplicateDetectionResult according to these rules:
- cloneGroups must be an array of CloneGroup objects
- stats must be an object with the required properties

#### Scenario: AnalyzeCodeForDuplicates
- **GIVEN** Source code to analyze
- **WHEN** The duplicate detector analyzes the code
- **THEN** A DuplicateDetectionResult is returned with the clone groups and statistics

### Requirement: TypeInferenceEngineValidation

The system SHALL validate TypeInferenceEngine according to these rules:
- Only supports 7 specific programming languages
- Only tracks class names starting with uppercase letters
- False positives are acceptable but false negatives are not

#### Scenario: InferTypesFromPythonSource
- **GIVEN** Python source code with variable declarations
- **WHEN** inferPython() is called
- **THEN** Returns a map of variable names to inferred class names

#### Scenario: ResolveMethodCallViaTypeInference
- **GIVEN** A variable name and method name, and an inferred types map
- **WHEN** resolveViaTypeInference() is called
- **THEN** Returns the corresponding FunctionNode if the type is found

### Requirement: InferredTypesValidation

The system SHALL validate InferredTypes according to these rules:
- Only includes class names starting with uppercase letters

#### Scenario: ValidInferredTypesAccepted
- **GIVEN** A valid InferredTypes object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: HTTP CallValidation

The system SHALL validate HTTP Call according to these rules:
- Method must be one of GET, POST, PUT, PATCH, DELETE, HEAD, OPTIONS
- URL must be a valid URL string
- Client must be one of fetch, axios, ky, got

#### Scenario: ExtractHttpCallsFromAFile
- **GIVEN** A file with HTTP calls
- **WHEN** extractHttpCalls is called with the file path
- **THEN** An array of HTTP calls is returned
- **AND** Each call includes file, method, URL, normalized URL, line number, and client
- **AND** Comments are stripped from the file content before parsing

#### Scenario: HandleUnsupportedFileTypes
- **GIVEN** A file with an unsupported extension
- **WHEN** extractHttpCalls is called with the file path
- **THEN** An empty array is returned

#### Scenario: HandleFileReadErrors
- **GIVEN** A file that cannot be read
- **WHEN** extractHttpCalls is called with the file path
- **THEN** An empty array is returned

### Requirement: FunctionRegistryTrieValidation

The system SHALL validate FunctionRegistryTrie according to these rules:
- FunctionNodes with the same simple name are stored in the same array in byName
- FunctionNodes with className are stored in byQualified with key 'ClassName.methodName'
- Each FunctionNode is stored exactly once in byId with its unique ID

#### Scenario: InsertFunctionnode
- **GIVEN** A FunctionNode instance
- **WHEN** insert() is called
- **THEN** The node is added to byId, byName, and byQualified (if className exists)

#### Scenario: FindBySimpleName
- **GIVEN** A simple function name
- **WHEN** findBySimpleName() is called
- **THEN** Returns all FunctionNodes with that simple name

#### Scenario: FindByQualifiedName
- **GIVEN** A class name and method name
- **WHEN** findByQualifiedName() is called
- **THEN** Returns all FunctionNodes matching 'ClassName.methodName'

#### Scenario: FindById
- **GIVEN** A unique ID
- **WHEN** findById() is called
- **THEN** Returns the specific FunctionNode with that ID

### Requirement: Maprepository

The system SHALL analyzes repository structure and generates a comprehensive map of its contents.

#### Scenario: SuccessfulMapping
- **GIVEN** Valid repository path and options
- **WHEN** mapRepository is called
- **THEN** Repository map is generated and returned
- **AND** Progress events are emitted
- **AND** Analysis files are written to output directory if specified

#### Scenario: ExceedsMaxFiles
- **GIVEN** Repository with more files than maxFiles option
- **WHEN** mapRepository is called
- **THEN** Repository map is generated with limited files
- **AND** Warning is logged about file limit

### Requirement: Detectframeworks

The system SHALL identifies frameworks and libraries used in the repository.

#### Scenario: FrameworksDetected
- **GIVEN** Repository with known frameworks
- **WHEN** detectFrameworks is called
- **THEN** Detected frameworks are returned with confidence levels and evidence

#### Scenario: NoFrameworksDetected
- **GIVEN** Repository with no recognizable frameworks
- **WHEN** detectFrameworks is called
- **THEN** Empty array is returned

### Requirement: Clusterfiles

The system SHALL groups files by domain, layer, and directory.

#### Scenario: FilesClustered
- **GIVEN** Array of scored files
- **WHEN** clusterFiles is called
- **THEN** Files are grouped by directory, domain, and layer

#### Scenario: EmptyInput
- **GIVEN** Empty array of files
- **WHEN** clusterFiles is called
- **THEN** Empty clusters object is returned

### Requirement: Getfilegodfunctions

The system SHALL identifies high-fan-out functions in a file that exceed a specified threshold.

#### Scenario: HighFanOutFunctionsFound
- **GIVEN** A file with functions having fan-out greater than the threshold
- **WHEN** getFileGodFunctions is called
- **THEN** Returns an array of FunctionNode objects representing high-fan-out functions

#### Scenario: NoHighFanOutFunctionsFound
- **GIVEN** A file with no functions exceeding the fan-out threshold
- **WHEN** getFileGodFunctions is called
- **THEN** Returns an empty array

### Requirement: Extractsubgraph

The system SHALL extracts a depth-limited subgraph around a root node, following only outgoing edges.

#### Scenario: SubgraphExtractedWithinDepthLimit
- **GIVEN** A call graph and a root node
- **WHEN** extractSubgraph is called with a specified max depth
- **THEN** Returns a SubGraph object containing nodes and edges within the specified depth limit

#### Scenario: SubgraphExtractedWithinNodeLimit
- **GIVEN** A call graph and a root node
- **WHEN** extractSubgraph is called with a specified max number of nodes
- **THEN** Returns a SubGraph object containing nodes and edges within the specified node limit

### Requirement: Formatsubgraph

The system SHALL formats a subgraph as a compact ascii block for llm prompts.

#### Scenario: SubgraphFormattedSuccessfully
- **GIVEN** A valid SubGraph object
- **WHEN** formatSubgraph is called
- **THEN** Returns a formatted string representing the subgraph

### Requirement: Buildgraphpromptsection

The system SHALL builds a graph-based prompt section for a large file, including signatures and subgraphs for high-complexity functions.

#### Scenario: GraphPromptSectionBuiltSuccessfully
- **GIVEN** A call graph and file signatures for a file with high-complexity functions
- **WHEN** buildGraphPromptSection is called
- **THEN** Returns a formatted string containing the graph-based prompt section

#### Scenario: NoCallGraphDataAvailable
- **GIVEN** No call graph data is available for the file
- **WHEN** buildGraphPromptSection is called
- **THEN** Returns null

#### Scenario: NoHighComplexityFunctionsFound
- **GIVEN** A file with no high-complexity functions
- **WHEN** buildGraphPromptSection is called
- **THEN** Returns null

### Requirement: Inferclusterrole

The system SHALL determines the architectural role of a module cluster based on its metrics.

#### Scenario: EntryLayerCluster
- **GIVEN** Entry count is more than 50% of file count
- **WHEN** inferClusterRole is called
- **THEN** Returns 'entry_layer'

#### Scenario: OrchestratorCluster
- **GIVEN** Both hub and entry counts are greater than zero
- **WHEN** inferClusterRole is called
- **THEN** Returns 'orchestrator'

#### Scenario: CoreUtilitiesCluster
- **GIVEN** Hub count is greater than zero and entry count is zero
- **WHEN** inferClusterRole is called
- **THEN** Returns 'core_utilities'

#### Scenario: ApiLayerCluster
- **GIVEN** Entry count is greater than zero and hub count is zero
- **WHEN** inferClusterRole is called
- **THEN** Returns 'api_layer'

#### Scenario: InternalCluster
- **GIVEN** Neither hub nor entry counts are significant
- **WHEN** inferClusterRole is called
- **THEN** Returns 'internal'

### Requirement: Buildarchitectureoverview

The system SHALL constructs an architectureoverview from dependency graph and llm context data.

#### Scenario: CompleteDataAvailable
- **GIVEN** Both dependency graph and LLM context are provided
- **WHEN** buildArchitectureOverview is called
- **THEN** Returns a complete ArchitectureOverview with all metrics populated

#### Scenario: MissingDependencyGraph
- **GIVEN** Dependency graph is null but LLM context is provided
- **WHEN** buildArchitectureOverview is called
- **THEN** Returns an ArchitectureOverview with cluster data derived from LLM context

#### Scenario: MissingLlmContext
- **GIVEN** LLM context is null but dependency graph is provided
- **WHEN** buildArchitectureOverview is called
- **THEN** Returns an ArchitectureOverview with cluster data derived from dependency graph

#### Scenario: MissingBothInputs
- **GIVEN** Both dependency graph and LLM context are null
- **WHEN** buildArchitectureOverview is called
- **THEN** Throws an error indicating at least one input must be provided

### Requirement: Builddependencygraph

The system SHALL constructs a complete dependency graph from file analysis data.

#### Scenario: BasicGraphConstruction
- **GIVEN** A set of analyzed files with imports
- **WHEN** buildDependencyGraph is called
- **THEN** A complete dependency graph is returned with nodes, edges, and clusters

#### Scenario: EmptyInput
- **GIVEN** No files to analyze
- **WHEN** buildDependencyGraph is called
- **THEN** An empty graph is returned with zero nodes and edges

### Requirement: Identifyclusters

The system SHALL identifies clusters of related files based on dependency patterns.

#### Scenario: ClusterDetection
- **GIVEN** Files with interconnected dependencies
- **WHEN** identifyClusters is called
- **THEN** Clusters are identified with cohesion and coupling metrics

#### Scenario: NoClusters
- **GIVEN** Files with no dependencies between them
- **WHEN** identifyClusters is called
- **THEN** No clusters are returned

### Requirement: Calculatemetrics

The system SHALL computes various graph metrics for nodes and edges.

#### Scenario: MetricCalculation
- **GIVEN** A populated dependency graph
- **WHEN** calculateMetrics is called
- **THEN** Nodes are updated with in-degree, out-degree, betweenness, and PageRank metrics

### Requirement: Detectcycles

The system SHALL identifies cycles in the dependency graph.

#### Scenario: CycleDetection
- **GIVEN** A graph with circular dependencies
- **WHEN** detectCycles is called
- **THEN** All cycles are identified and returned

#### Scenario: NoCycles
- **GIVEN** A graph with no circular dependencies
- **WHEN** detectCycles is called
- **THEN** An empty array is returned

### Requirement: Ranknodes

The system SHALL ranks nodes based on various criteria like importance and connectivity.

#### Scenario: NodeRanking
- **GIVEN** A populated dependency graph
- **WHEN** rankNodes is called
- **THEN** Nodes are ranked by importance, connectivity, and other criteria

### Requirement: Build

The system SHALL builds the dependency graph from scored files and returns the graph result including nodes, edges, clusters, rankings, cycles, and statistics.

#### Scenario: SuccessfulGraphBuild
- **GIVEN** A list of scored files
- **WHEN** build is called
- **THEN** A complete dependency graph is built and returned

#### Scenario: EmptyFileList
- **GIVEN** An empty list of files
- **WHEN** build is called
- **THEN** An empty graph result is returned

### Requirement: Parsefiles

The system SHALL parses all files to extract imports/exports.

#### Scenario: SuccessfulParsing
- **GIVEN** A list of files with valid imports/exports
- **WHEN** parseFiles is called
- **THEN** A map of file analyses is returned

#### Scenario: FailedParsing
- **GIVEN** A list of files with invalid imports/exports
- **WHEN** parseFiles is called
- **THEN** An empty map is returned for files that couldn't be parsed

### Requirement: Buildhttpcrossedges

The system SHALL builds cross-language edges by matching http calls in js/ts files to fastapi/flask/django route definitions in python files.

#### Scenario: SuccessfulHttpEdgeBuild
- **GIVEN** A list of files with HTTP calls and route definitions
- **WHEN** buildHttpCrossEdges is called
- **THEN** HTTP edges are built and added to the graph

#### Scenario: NoHttpEdges
- **GIVEN** A list of files without HTTP calls or route definitions
- **WHEN** buildHttpCrossEdges is called
- **THEN** No edges are added to the graph

### Requirement: Buildedges

The system SHALL builds edges from import relationships.

#### Scenario: SuccessfulEdgeBuild
- **GIVEN** A list of files with valid imports
- **WHEN** buildEdges is called
- **THEN** Edges are built and added to the graph

#### Scenario: NoEdges
- **GIVEN** A list of files without valid imports
- **WHEN** buildEdges is called
- **THEN** No edges are added to the graph

### Requirement: Calculatedegrees

The system SHALL calculates in-degree and out-degree for each node.

#### Scenario: SuccessfulDegreeCalculation
- **GIVEN** A graph with nodes and edges
- **WHEN** calculateDegrees is called
- **THEN** In-degree and out-degree are calculated and updated for each node

#### Scenario: EmptyGraph
- **GIVEN** An empty graph
- **WHEN** calculateDegrees is called
- **THEN** No degrees are calculated

### Requirement: Calculatebetweenness

The system SHALL calculates betweenness centrality using brandes' algorithm.

#### Scenario: SuccessfulBetweennessCalculation
- **GIVEN** A graph with nodes and edges
- **WHEN** calculateBetweenness is called
- **THEN** Betweenness centrality is calculated and updated for each node

#### Scenario: EmptyGraph
- **GIVEN** An empty graph
- **WHEN** calculateBetweenness is called
- **THEN** No betweenness is calculated

### Requirement: Calculatepagerank

The system SHALL calculates pagerank-style importance scores.

#### Scenario: SuccessfulPagerankCalculation
- **GIVEN** A graph with nodes and edges
- **WHEN** calculatePageRank is called
- **THEN** PageRank scores are calculated and updated for each node

#### Scenario: EmptyGraph
- **GIVEN** An empty graph
- **WHEN** calculatePageRank is called
- **THEN** No PageRank scores are calculated

### Requirement: Detectclusters

The system SHALL detects clusters using a simple community detection approach.

#### Scenario: SuccessfulClusterDetection
- **GIVEN** A graph with nodes and edges
- **WHEN** detectClusters is called
- **THEN** Clusters are detected and returned

#### Scenario: NoClusters
- **GIVEN** A graph without clusters
- **WHEN** detectClusters is called
- **THEN** An empty array is returned

### Requirement: Suggestdomainname

The system SHALL suggests a domain name based on directory and file contents.

#### Scenario: SuccessfulDomainNameSuggestion
- **GIVEN** A directory and list of files
- **WHEN** suggestDomainName is called
- **THEN** A domain name is suggested and returned

#### Scenario: NoDomainNameSuggestion
- **GIVEN** An empty directory and list of files
- **WHEN** suggestDomainName is called
- **THEN** A default domain name is returned

### Requirement: Detectcycles

The system SHALL detects cycles in the dependency graph using dfs.

#### Scenario: SuccessfulCycleDetection
- **GIVEN** A graph with cycles
- **WHEN** detectCycles is called
- **THEN** Cycles are detected and returned

#### Scenario: NoCycles
- **GIVEN** A graph without cycles
- **WHEN** detectCycles is called
- **THEN** An empty array is returned

### Requirement: Isduplicatecycle

The system SHALL checks if a cycle is a duplicate or rotation of an existing cycle.

#### Scenario: DuplicateCycle
- **GIVEN** An existing cycle and a new cycle that is a duplicate
- **WHEN** isDuplicateCycle is called
- **THEN** True is returned

#### Scenario: UniqueCycle
- **GIVEN** An existing cycle and a new cycle that is unique
- **WHEN** isDuplicateCycle is called
- **THEN** False is returned

### Requirement: Normalizecycle

The system SHALL normalizes a cycle for comparison.

#### Scenario: SuccessfulNormalization
- **GIVEN** A cycle
- **WHEN** normalizeCycle is called
- **THEN** A normalized cycle is returned

#### Scenario: EmptyCycle
- **GIVEN** An empty cycle
- **WHEN** normalizeCycle is called
- **THEN** An empty string is returned

### Requirement: Generaterankings

The system SHALL generates various rankings of nodes.

#### Scenario: SuccessfulRankingsGeneration
- **GIVEN** A graph with nodes and clusters
- **WHEN** generateRankings is called
- **THEN** Rankings are generated and returned

#### Scenario: EmptyGraph
- **GIVEN** An empty graph
- **WHEN** generateRankings is called
- **THEN** Empty rankings are returned

### Requirement: Calculatestatistics

The system SHALL calculates overall statistics.

#### Scenario: SuccessfulStatisticsCalculation
- **GIVEN** A graph with nodes, edges, clusters, and cycles
- **WHEN** calculateStatistics is called
- **THEN** Statistics are calculated and returned

#### Scenario: EmptyGraph
- **GIVEN** An empty graph
- **WHEN** calculateStatistics is called
- **THEN** Default statistics are returned

### Requirement: Builddependencygraph

The system SHALL constructs a complete dependency graph from scored files.

#### Scenario: SuccessfulGraphConstruction
- **GIVEN** Valid scored files and options
- **WHEN** buildDependencyGraph is called
- **THEN** A complete dependency graph is returned

### Requirement: Injectcallgraphedges

The system SHALL injects call graph edges into an existing dependency graph for languages without explicit imports.

#### Scenario: EdgesInjectedSuccessfully
- **GIVEN** Valid dependency graph and call edges
- **WHEN** injectCallGraphEdges is called
- **THEN** Call edges are injected into the dependency graph

### Requirement: Tod3format

The system SHALL converts a dependency graph to d3.js force graph format.

#### Scenario: SuccessfulConversion
- **GIVEN** Valid dependency graph result
- **WHEN** toD3Format is called
- **THEN** D3.js compatible graph format is returned

### Requirement: Tomermaidformat

The system SHALL converts a dependency graph to mermaid diagram syntax.

#### Scenario: SuccessfulConversion
- **GIVEN** Valid dependency graph result and max nodes
- **WHEN** toMermaidFormat is called
- **THEN** Mermaid diagram syntax is returned

### Requirement: Todotformat

The system SHALL converts a dependency graph to dot format for graphviz.

#### Scenario: SuccessfulConversion
- **GIVEN** Valid dependency graph result
- **WHEN** toDotFormat is called
- **THEN** DOT format string is returned

### Requirement: Parsespecfile

The system SHALL parses a markdown specification file into logical sections.

#### Scenario: ValidMarkdownFile
- **GIVEN** A valid Markdown file with H2 and H3 headings
- **WHEN** parseSpecFile is called
- **THEN** An array of ParsedSection objects is returned

#### Scenario: EmptyMarkdownFile
- **GIVEN** An empty Markdown file
- **WHEN** parseSpecFile is called
- **THEN** An empty array is returned

### Requirement: Buildmappingindex

The system SHALL builds a lookup index for mapping requirements to source files.

#### Scenario: ValidMappings
- **GIVEN** An array of valid MappingEntry objects
- **WHEN** buildMappingIndex is called
- **THEN** A Map object with requirement keys and associated source files is returned

#### Scenario: EmptyMappings
- **GIVEN** An empty array of MappingEntry objects
- **WHEN** buildMappingIndex is called
- **THEN** An empty Map object is returned

### Requirement: Buildtext

The system SHALL constructs a text representation of a specification section for embedding.

#### Scenario: ValidSection
- **GIVEN** A valid ParsedSection object
- **WHEN** buildText is called
- **THEN** A concatenated string representing the section is returned

#### Scenario: EmptySection
- **GIVEN** A ParsedSection object with empty content
- **WHEN** buildText is called
- **THEN** A string with only the section title is returned

### Requirement: Walkdirectory

The system SHALL recursively scans a directory and collects metadata for all relevant files.

#### Scenario: SuccessfulDirectoryWalk
- **GIVEN** A valid directory path with files
- **WHEN** walkDirectory is called
- **THEN** Returns metadata for all relevant files and directories scanned

#### Scenario: DirectoryWithIgnoredFiles
- **GIVEN** A directory with files matching ignore patterns
- **WHEN** walkDirectory is called
- **THEN** Excludes ignored files from the result

#### Scenario: DirectoryWithNoRelevantFiles
- **GIVEN** A directory with only ignored files
- **WHEN** walkDirectory is called
- **THEN** Returns an empty result

### Requirement: Hasshebang

The system SHALL checks if a file has a shebang line at the beginning.

#### Scenario: FileWithShebang
- **GIVEN** A file with a shebang line
- **WHEN** hasShebang is called
- **THEN** Returns true

#### Scenario: FileWithoutShebang
- **GIVEN** A file without a shebang line
- **WHEN** hasShebang is called
- **THEN** Returns false

### Requirement: Countlines

The system SHALL counts the number of lines in a file.

#### Scenario: SmallFile
- **GIVEN** A file smaller than MAX_READ_SIZE
- **WHEN** countLines is called
- **THEN** Returns the correct line count

#### Scenario: LargeFile
- **GIVEN** A file larger than MAX_READ_SIZE
- **WHEN** countLines is called
- **THEN** Returns -1

### Requirement: Istestfile

The system SHALL determines if a file is a test file based on its path and name.

#### Scenario: TestFileInTestDirectory
- **GIVEN** A file in a directory matching test patterns
- **WHEN** isTestFile is called
- **THEN** Returns true

#### Scenario: TestFileWithTestExtension
- **GIVEN** A file with a name matching test patterns
- **WHEN** isTestFile is called
- **THEN** Returns true

#### Scenario: NonTestFile
- **GIVEN** A file not matching any test patterns
- **WHEN** isTestFile is called
- **THEN** Returns false

### Requirement: Isconfigfile

The system SHALL determines if a file is a configuration file based on its name.

#### Scenario: ConfigFile
- **GIVEN** A file with a name matching config patterns
- **WHEN** isConfigFile is called
- **THEN** Returns true

#### Scenario: NonConfigFile
- **GIVEN** A file not matching any config patterns
- **WHEN** isConfigFile is called
- **THEN** Returns false

### Requirement: Isgeneratedfile

The system SHALL determines if a file is likely generated based on its name and path.

#### Scenario: GeneratedFile
- **GIVEN** A file with a name or path matching generated patterns
- **WHEN** isGeneratedFile is called
- **THEN** Returns true

#### Scenario: NonGeneratedFile
- **GIVEN** A file not matching any generated patterns
- **WHEN** isGeneratedFile is called
- **THEN** Returns false

### Requirement: Isentrypoint

The system SHALL determines if a file is likely an entry point based on its name, path, and content.

#### Scenario: EntryPointFile
- **GIVEN** A file with a name matching entry point patterns
- **WHEN** isEntryPoint is called
- **THEN** Returns true

#### Scenario: EntryPointFileWithShebang
- **GIVEN** A file with a shebang line
- **WHEN** isEntryPoint is called
- **THEN** Returns true

#### Scenario: NonEntryPointFile
- **GIVEN** A file not matching any entry point patterns
- **WHEN** isEntryPoint is called
- **THEN** Returns false

### Requirement: Loadignorepatterns

The system SHALL loads and combines ignore patterns from various sources.

#### Scenario: PatternsLoadedSuccessfully
- **GIVEN** A directory with .gitignore and .spec-gen-ignore files
- **WHEN** loadIgnorePatterns is called
- **THEN** Returns combined ignore patterns

#### Scenario: MissingIgnoreFiles
- **GIVEN** A directory without .gitignore or .spec-gen-ignore files
- **WHEN** loadIgnorePatterns is called
- **THEN** Returns only built-in ignore patterns

### Requirement: Buildimportmap

The system SHALL builds an importmap from typescript/javascript/python fileanalysis objects.

#### Scenario: ValidAnalyses
- **GIVEN** An array of FileAnalysis objects with relative imports
- **WHEN** buildImportMap is called
- **THEN** An ImportMap is created with resolved file paths for each imported name

#### Scenario: NoRelativeImports
- **GIVEN** An array of FileAnalysis objects with no relative imports
- **WHEN** buildImportMap is called
- **THEN** An empty ImportMap is returned

### Requirement: Findcalleefileviaimport

The system SHALL given a caller file and a callee name, returns the source file the name was imported from.

#### Scenario: CalleeFileFound
- **GIVEN** An ImportMap with the caller file and callee name
- **WHEN** findCalleeFileViaImport is called
- **THEN** The resolved source file path is returned

#### Scenario: CalleeFileNotFound
- **GIVEN** An ImportMap without the caller file or callee name
- **WHEN** findCalleeFileViaImport is called
- **THEN** undefined is returned

### Requirement: Parsegoimports

The system SHALL parses go imports and builds a map of local names to resolved source file paths.

#### Scenario: ValidGoImports
- **GIVEN** Go file content with relative imports
- **WHEN** parseGoImports is called
- **THEN** A map of local names to resolved source file paths is returned

#### Scenario: NoRelativeImports
- **GIVEN** Go file content with no relative imports
- **WHEN** parseGoImports is called
- **THEN** An empty map is returned

### Requirement: Parserustimports

The system SHALL parses rust imports and builds a map of local names to resolved source file paths.

#### Scenario: ValidRustImports
- **GIVEN** Rust file content with relative imports
- **WHEN** parseRustImports is called
- **THEN** A map of local names to resolved source file paths is returned

#### Scenario: NoRelativeImports
- **GIVEN** Rust file content with no relative imports
- **WHEN** parseRustImports is called
- **THEN** An empty map is returned

### Requirement: Parserubyimports

The system SHALL parses ruby imports and builds a map of local names to resolved source file paths.

#### Scenario: ValidRubyImports
- **GIVEN** Ruby file content with relative imports
- **WHEN** parseRubyImports is called
- **THEN** A map of local names to resolved source file paths is returned

#### Scenario: NoRelativeImports
- **GIVEN** Ruby file content with no relative imports
- **WHEN** parseRubyImports is called
- **THEN** An empty map is returned

### Requirement: Parsejavaimports

The system SHALL parses java imports and builds a map of local names to resolved source file paths.

#### Scenario: ValidJavaImports
- **GIVEN** Java file content with relative imports
- **WHEN** parseJavaImports is called
- **THEN** A map of local names to resolved source file paths is returned

#### Scenario: NoRelativeImports
- **GIVEN** Java file content with no relative imports
- **WHEN** parseJavaImports is called
- **THEN** An empty map is returned

### Requirement: Computedepths

The system SHALL calculates the depth of each function from entry points using bfs.

#### Scenario: EntryPoints
- **GIVEN** Functions with fanIn === 0
- **WHEN** computeDepths is called
- **THEN** Depths are set to 0 for entry points

#### Scenario: UnreachableNodes
- **GIVEN** Functions not reachable from any entry point
- **WHEN** computeDepths is called
- **THEN** Depths are set to -1

### Requirement: Computesccs

The system SHALL identifies strongly connected components in the call graph using tarjan's algorithm.

#### Scenario: SingleScc
- **GIVEN** A cycle in the call graph
- **WHEN** computeSCCs is called
- **THEN** All nodes in the cycle are assigned the same SCC ID

#### Scenario: MultipleSccs
- **GIVEN** Multiple cycles in the call graph
- **WHEN** computeSCCs is called
- **THEN** Each cycle is assigned a unique SCC ID

### Requirement: Iscrosscuttinghub

The system SHALL determines if a high-fanin node is a cross-cutting utility sink by design.

#### Scenario: UtilitySink
- **GIVEN** A function with fanOut === 0 and a utility file path
- **WHEN** isCrossCuttingHub is called
- **THEN** Returns true

#### Scenario: NonUtilitySink
- **GIVEN** A function with fanOut === 0 and a non-utility file path
- **WHEN** isCrossCuttingHub is called
- **THEN** Returns false

### Requirement: Buildbm25corpus

The system SHALL constructs a bm25 corpus from function records for sparse retrieval.

#### Scenario: ValidRecords
- **GIVEN** Array of function records with text content
- **WHEN** buildBm25Corpus is called
- **THEN** BM25 corpus is constructed with token frequency maps and document statistics

### Requirement: Bm25score

The system SHALL calculates bm25 relevance score for a document given a query.

#### Scenario: ValidQueryAndDocument
- **GIVEN** BM25 corpus and query tokens
- **WHEN** bm25Score is called
- **THEN** Relevance score is calculated based on BM25 formula

### Requirement: Rrfscore

The system SHALL merges two ranked lists into a single relevance score using reciprocal rank fusion.

#### Scenario: ValidRanks
- **GIVEN** Ranks from dense and sparse retrieval methods
- **WHEN** rrfScore is called
- **THEN** Combined relevance score is calculated using RRF formula

### Requirement: Buildtext

The system SHALL constructs the text to embed for a function, combining language, path, qualified name, signature, docstring, and skeleton body.

#### Scenario: ValidFunctionNode
- **GIVEN** Function node with file contents
- **WHEN** buildText is called
- **THEN** Text is constructed combining various function details and skeleton body

### Requirement: Buildsignatureindex

The system SHALL builds a lookup map for file paths to signature entries.

#### Scenario: ValidSignatures
- **GIVEN** Array of file signature maps
- **WHEN** buildSignatureIndex is called
- **THEN** Lookup map is constructed for file paths to signature entries

### Requirement: Findsignatureentry

The system SHALL finds the best matching signature entry for a function node.

#### Scenario: ValidFunctionNode
- **GIVEN** Function node and signature index
- **WHEN** findSignatureEntry is called
- **THEN** Best matching signature entry is found for the function node

### Requirement: Detectlanguage

The system SHALL determines the programming language of a file based on its extension and content.

#### Scenario: PythonFile
- **GIVEN** A file with .py extension
- **WHEN** detectLanguage is called
- **THEN** Returns 'python'

#### Scenario: TypescriptFile
- **GIVEN** A file with .ts extension
- **WHEN** detectLanguage is called
- **THEN** Returns 'typescript'

### Requirement: Extractsignatures

The system SHALL extracts code signatures from a file's content based on its detected language.

#### Scenario: PythonFileWithDocstrings
- **GIVEN** Python file content with docstrings
- **WHEN** extractSignatures is called
- **THEN** Returns FileSignatureMap with extracted docstrings

#### Scenario: TypescriptFileWithJsdoc
- **GIVEN** TypeScript file content with JSDoc comments
- **WHEN** extractSignatures is called
- **THEN** Returns FileSignatureMap with extracted JSDoc comments

### Requirement: Formatsignaturemaps

The system SHALL formats signature maps into compact text blocks for stage 1 llm processing.

#### Scenario: LargeSignatureMap
- **GIVEN** Signature map exceeding STAGE1_MAX_CHARS
- **WHEN** formatSignatureMaps is called
- **THEN** Returns truncated text block with ellipsis

#### Scenario: SmallSignatureMap
- **GIVEN** Signature map below STAGE1_MAX_CHARS
- **WHEN** formatSignatureMaps is called
- **THEN** Returns complete text block without truncation


## Sub-components

> `SignatureExtractor` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: Languagedetection

> Implements: `detectLanguage`

Determines the programming language of a source file based on its file extension and content.

#### Requirement: Detectlanguage

The system SHALL the function shall analyze the file path and content to determine the programming language.

#### Scenario: DetectPythonFile
- **GIVEN** A file path ending with .py
- **WHEN** The function is called with the file path
- **THEN** The function SHALL return 'python'

### Sub-component: Pythonsignatureextraction

> Implements: `extractPython`

Extracts function signatures from Python source files using docstrings.

#### Requirement: Extractpythondocstring

The system SHALL the function shall parse python docstrings to extract function signatures.

#### Scenario: ExtractPythonFunctionSignature
- **GIVEN** A Python file with a function having a docstring
- **WHEN** The function is called with the file content
- **THEN** The function SHALL return the extracted function signature

### Sub-component: Typescriptsignatureextraction

> Implements: `extractTypeScript`

Extracts function signatures from TypeScript source files using JSDoc comments.

#### Requirement: Extractjsdoc

The system SHALL the function shall parse jsdoc comments to extract function signatures.

#### Scenario: ExtractTypescriptFunctionSignature
- **GIVEN** A TypeScript file with a function having a JSDoc comment
- **WHEN** The function is called with the file content
- **THEN** The function SHALL return the extracted function signature

#### Requirement: Compactparams

The system SHALL the function shall compact parameter descriptions into a concise format.

#### Scenario: CompactTypescriptParameters
- **GIVEN** A list of parameters with detailed descriptions
- **WHEN** The function is called with the parameters
- **THEN** The function SHALL return the compacted parameter descriptions

### Sub-component: Gosignatureextraction

> Implements: `extractGo`

Extracts function signatures from Go source files.

#### Requirement: Extractgosignature

The system SHALL the function shall parse go source code to extract function signatures.

#### Scenario: ExtractGoFunctionSignature
- **GIVEN** A Go file with a function
- **WHEN** The function is called with the file content
- **THEN** The function SHALL return the extracted function signature

### Sub-component: Rustsignatureextraction

> Implements: `extractRust`

Extracts function signatures from Rust source files.

#### Requirement: Extractrustsignature

The system SHALL the function shall parse rust source code to extract function signatures.

#### Scenario: ExtractRustFunctionSignature
- **GIVEN** A Rust file with a function
- **WHEN** The function is called with the file content
- **THEN** The function SHALL return the extracted function signature

#### Requirement: Compactparams

The system SHALL the function shall compact parameter descriptions into a concise format.

#### Scenario: CompactRustParameters
- **GIVEN** A list of parameters with detailed descriptions
- **WHEN** The function is called with the parameters
- **THEN** The function SHALL return the compacted parameter descriptions

### Sub-component: Rubysignatureextraction

> Implements: `extractRuby`

Extracts function signatures from Ruby source files.

#### Requirement: Extractrubysignature

The system SHALL the function shall parse ruby source code to extract function signatures.

#### Scenario: ExtractRubyFunctionSignature
- **GIVEN** A Ruby file with a function
- **WHEN** The function is called with the file content
- **THEN** The function SHALL return the extracted function signature

### Sub-component: Cppsignatureextraction

> Implements: `extractCpp`

Extracts function signatures from C++ source files.

#### Requirement: Extractcppsignature

The system SHALL the function shall parse c++ source code to extract function signatures.

#### Scenario: ExtractC++FunctionSignature
- **GIVEN** A C++ file with a function
- **WHEN** The function is called with the file content
- **THEN** The function SHALL return the extracted function signature

#### Requirement: Compactparams

The system SHALL the function shall compact parameter descriptions into a concise format.

#### Scenario: CompactC++Parameters
- **GIVEN** A list of parameters with detailed descriptions
- **WHEN** The function is called with the parameters
- **THEN** The function SHALL return the compacted parameter descriptions

### Sub-component: Swiftsignatureextraction

> Implements: `extractSwift`

Extracts function signatures from Swift source files.

#### Requirement: Extractswiftsignature

The system SHALL the function shall parse swift source code to extract function signatures.

#### Scenario: ExtractSwiftFunctionSignature
- **GIVEN** A Swift file with a function
- **WHEN** The function is called with the file content
- **THEN** The function SHALL return the extracted function signature

#### Requirement: Compactparams

The system SHALL the function shall compact parameter descriptions into a concise format.

#### Scenario: CompactSwiftParameters
- **GIVEN** A list of parameters with detailed descriptions
- **WHEN** The function is called with the parameters
- **THEN** The function SHALL return the compacted parameter descriptions

### Sub-component: Genericsignatureextraction

> Implements: `extractGeneric`

Extracts function signatures from source files using generic parsing.

#### Requirement: Extractgenericsignature

The system SHALL the function shall parse source code to extract function signatures using generic parsing.

#### Scenario: ExtractGenericFunctionSignature
- **GIVEN** A source file with a function
- **WHEN** The function is called with the file content
- **THEN** The function SHALL return the extracted function signature

### Requirement: Generateartifacts

The system SHALL generates all analysis artifacts including repository structure, architecture layers, domains, and llm context.

#### Scenario: ValidRepositoryMapAndDependencyGraph
- **GIVEN** Valid RepositoryMap and DependencyGraph objects
- **WHEN** generate is called
- **THEN** All analysis artifacts are generated successfully

#### Scenario: EmptyRepositoryMap
- **GIVEN** Empty RepositoryMap object
- **WHEN** generate is called
- **THEN** Artifacts are generated with empty or default values

### Requirement: Saveartifacts

The system SHALL generates and saves all analysis artifacts to disk in specified output directory.

#### Scenario: ValidOutputDirectory
- **GIVEN** Valid output directory path in options
- **WHEN** generateAndSave is called
- **THEN** Artifacts are saved to disk successfully

#### Scenario: InvalidOutputDirectory
- **GIVEN** Invalid output directory path in options
- **WHEN** generateAndSave is called
- **THEN** Error is thrown and artifacts are not saved

### Requirement: Generaterepostructure

The system SHALL generates repository structure artifact from repository map and dependency graph.

#### Scenario: ValidInputs
- **GIVEN** Valid RepositoryMap and DependencyGraph objects
- **WHEN** generateRepoStructure is called
- **THEN** RepoStructure artifact is generated successfully

#### Scenario: EmptyInputs
- **GIVEN** Empty RepositoryMap and DependencyGraph objects
- **WHEN** generateRepoStructure is called
- **THEN** Empty RepoStructure artifact is generated

### Requirement: Detectarchitecturepattern

The system SHALL detects architecture pattern from code structure in repository map.

#### Scenario: LayeredArchitecture
- **GIVEN** RepositoryMap with clear layer separation
- **WHEN** detectArchitecturePattern is called
- **THEN** Returns 'layered' as the architecture pattern

#### Scenario: NoClearPattern
- **GIVEN** RepositoryMap with no clear architectural pattern
- **WHEN** detectArchitecturePattern is called
- **THEN** Returns 'unknown' as the architecture pattern

### Requirement: Generatearchitecturelayers

The system SHALL generates architecture layers from repository map.

#### Scenario: ValidRepositoryMap
- **GIVEN** RepositoryMap with valid structure
- **WHEN** generateArchitectureLayers is called
- **THEN** Returns array of ArchitectureLayer objects

#### Scenario: EmptyRepositoryMap
- **GIVEN** Empty RepositoryMap object
- **WHEN** generateArchitectureLayers is called
- **THEN** Returns empty array

### Requirement: Generatedomains

The system SHALL generates domains from clusters in repository map and dependency graph.

#### Scenario: ValidInputs
- **GIVEN** Valid RepositoryMap and DependencyGraph objects with clusters
- **WHEN** generateDomains is called
- **THEN** Returns array of DetectedDomain objects

#### Scenario: EmptyInputs
- **GIVEN** Empty RepositoryMap and DependencyGraph objects
- **WHEN** generateDomains is called
- **THEN** Returns empty array


## Sub-components

> `AnalysisArtifactGenerator` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: Istestfile

> Implements: `isTestFile`

Determine if a file is a test/spec file based on its path and content

#### Requirement: Testfiledetection

The system SHALL the function shall return true if the file path matches common test/spec file patterns for supported languages

#### Scenario: Pythontestfile
- **GIVEN** A file path ending with '_test.py'
- **WHEN** The function is called with this path
- **THEN** The function SHALL return true

### Sub-component: Extractsignatures

> Implements: `extractSignatures`

Extract function and class signatures from source code files

#### Requirement: Signatureextraction

The system SHALL the function shall extract function and class signatures from the provided source code content

#### Scenario: Typescriptfunction
- **GIVEN** A TypeScript file with a function 'calculateTotal'
- **WHEN** The function is called with this file's content
- **THEN** The function SHALL return a signature map containing the 'calculateTotal' function signature

### Sub-component: Detectlanguage

> Implements: `detectLanguage`

Determine the programming language of a source code file

#### Requirement: Languagedetection

The system SHALL the function shall detect the programming language based on the file extension and content patterns

#### Scenario: Pythonfile
- **GIVEN** A file path ending with '.py'
- **WHEN** The function is called with this path
- **THEN** The function SHALL return 'python'

### Sub-component: Build

> Implements: `build`

Construct a dependency graph from scored files

#### Requirement: Graphconstruction

The system SHALL the function shall build a dependency graph from the provided scored files

#### Scenario: Typescriptproject
- **GIVEN** An array of scored TypeScript files
- **WHEN** The function is called with these files
- **THEN** The function SHALL return a dependency graph containing TypeScript-specific relationships

### Sub-component: Serializecallgraph

> Implements: `serializeCallGraph`

Serialize a call graph into a portable format

#### Requirement: Callgraphserialization

The system SHALL the function shall serialize the call graph into a format suitable for storage or transmission

#### Scenario: Basiccallgraph
- **GIVEN** A simple call graph with two functions
- **WHEN** The function is called with this call graph
- **THEN** The function SHALL return a serialized representation of the call graph

### Sub-component: Injectcallgraphedges

> Implements: `injectCallGraphEdges`

Inject call graph edges into the dependency graph

#### Requirement: Edgeinjection

The system SHALL the function shall inject call graph edges into the dependency graph for the specified file

#### Scenario: Singlefileinjection
- **GIVEN** A dependency graph and call edges for a specific file
- **WHEN** The function is called with these inputs
- **THEN** The function SHALL modify the dependency graph to include the call edges for the specified file

### Sub-component: Detectduplicates

> Implements: `detectDuplicates`

Identify duplicate functions in the codebase using the call graph

#### Requirement: Duplicatedetection

The system SHALL the function shall detect duplicate functions in the codebase using the provided call graph

#### Scenario: Duplicatefunctions
- **GIVEN** A call graph with two identical functions in different files
- **WHEN** The function is called with these inputs
- **THEN** The function SHALL identify and report the duplicate functions

### Sub-component: Analyzeforrefactoring

> Implements: `analyzeForRefactoring`

Analyze a call graph for potential refactoring opportunities

#### Requirement: Refactoringanalysis

The system SHALL the function shall analyze the call graph for potential refactoring opportunities

#### Scenario: Highcomplexityfunction
- **GIVEN** A call graph with a function that has high cyclomatic complexity
- **WHEN** The function is called with this call graph
- **THEN** The function SHALL identify the high complexity function as a potential refactoring candidate

### Requirement: Parsecppincludes

The system SHALL parses #include directives from a c++ source file and categorizes them as relative, system, or other.

#### Scenario: RelativeInclude
- **GIVEN** A file with #include "relative/path.h"
- **WHEN** parseCppIncludes is called
- **THEN** Returns CppInclude with isRelative: true and isSystem: false

#### Scenario: SystemInclude
- **GIVEN** A file with #include <iostream>
- **WHEN** parseCppIncludes is called
- **THEN** Returns CppInclude with isSystem: true

### Requirement: Buildheadertoimplmap

The system SHALL builds a map from header file paths to their corresponding implementation file paths based on naming conventions.

#### Scenario: MatchingHeaderAndImplementation
- **GIVEN** Files engine.h and engine.cpp exist in the same directory
- **WHEN** buildHeaderToImplMap is called
- **THEN** Map contains entry from engine.h to engine.cpp

### Requirement: Buildcppimportmap

The system SHALL builds a map from c++ source files to sets of implementation files accessible via their #include directives.

#### Scenario: AccessibleImplementations
- **GIVEN** A file with #include "engine.h" and engine.h maps to engine.cpp
- **WHEN** buildCppImportMap is called
- **THEN** Map contains entry from the file to a set containing engine.cpp

### Requirement: Build

The system SHALL constructs a call graph from source files, analyzing language-specific syntax and relationships.

#### Scenario: PythonProject
- **GIVEN** Python source files
- **WHEN** build is called
- **THEN** Call graph with Python-specific nodes and edges is created

#### Scenario: MultiLanguageProject
- **GIVEN** Source files in multiple languages
- **WHEN** build is called
- **THEN** Call graph with nodes and edges for each language is created

### Requirement: Detectlayerviolations

The system SHALL identifies architectural layer violations in the call graph.

#### Scenario: ViolationDetected
- **GIVEN** Calls between layers that violate architecture rules
- **WHEN** detectLayerViolations is called
- **THEN** Layer violations are identified and returned

#### Scenario: NoViolations
- **GIVEN** Calls that follow architecture rules
- **WHEN** detectLayerViolations is called
- **THEN** Empty array is returned

### Requirement: Extractclassrelationships

The system SHALL extracts class relationships from source files.

#### Scenario: ClassInheritance
- **GIVEN** Source files with class inheritance
- **WHEN** extractClassRelationships is called
- **THEN** Class nodes and inheritance edges are extracted

#### Scenario: InterfaceImplementation
- **GIVEN** Source files with interface implementation
- **WHEN** extractClassRelationships is called
- **THEN** Class nodes and inheritance edges are extracted


## Sub-components

> `CallGraphBuilder` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: Gettsparser

> Implements: `getTSParser`

Provides a parser for TypeScript source code

#### Requirement: Providetsparser

The system SHALL the function shall provide a parser for typescript source code

#### Scenario: Gettsparser
- **GIVEN** A request for a TypeScript parser
- **WHEN** The function is called
- **THEN** A TypeScript parser is returned

### Sub-component: Safequery

> Implements: `safeQuery`

Safely queries the AST to avoid runtime errors

#### Requirement: Safeastquery

The system SHALL the function shall safely query the ast to avoid runtime errors

#### Scenario: Safequery
- **GIVEN** An AST node and a query string
- **WHEN** The function is called
- **THEN** The query result is returned if successful, otherwise null is returned

### Sub-component: Merge

> Implements: `merge`

Merges multiple call graphs into a single graph

#### Requirement: Mergecallgraphs

The system SHALL the function shall merge multiple call graphs into a single graph

#### Scenario: Merge
- **GIVEN** An array of call graphs
- **WHEN** The function is called
- **THEN** A merged call graph is returned

### Sub-component: Getpyparser

> Implements: `getPyParser`

Provides a parser for Python source code

#### Requirement: Providepyparser

The system SHALL the function shall provide a parser for python source code

#### Scenario: Getpyparser
- **GIVEN** A request for a Python parser
- **WHEN** The function is called
- **THEN** A Python parser is returned

### Sub-component: Getjavaparser

> Implements: `getJavaParser`

Provides a parser for Java source code

#### Requirement: Providejavaparser

The system SHALL the function shall provide a parser for java source code

#### Scenario: Getjavaparser
- **GIVEN** A request for a Java parser
- **WHEN** The function is called
- **THEN** A Java parser is returned

### Sub-component: Getcppparser

> Implements: `getCppParser`

Provides a parser for C++ source code

#### Requirement: Providecppparser

The system SHALL the function shall provide a parser for c++ source code

#### Scenario: Getcppparser
- **GIVEN** A request for a C++ parser
- **WHEN** The function is called
- **THEN** A C++ parser is returned

### Sub-component: Getrubyparser

> Implements: `getRubyParser`

Provides a parser for Ruby source code

#### Requirement: Providerubyparser

The system SHALL the function shall provide a parser for ruby source code

#### Scenario: Getrubyparser
- **GIVEN** A request for a Ruby parser
- **WHEN** The function is called
- **THEN** A Ruby parser is returned

### Sub-component: Getgoparser

> Implements: `getGoParser`

Provides a parser for Go source code

#### Requirement: Providegoparser

The system SHALL the function shall provide a parser for go source code

#### Scenario: Getgoparser
- **GIVEN** A request for a Go parser
- **WHEN** The function is called
- **THEN** A Go parser is returned

### Sub-component: Extractpygraph

> Implements: `extractPyGraph`

Extracts a call graph from Python source code

#### Requirement: Extractpythongraph

The system SHALL the function shall extract a call graph from python source code

#### Scenario: Extractpygraph
- **GIVEN** Python source code
- **WHEN** The function is called
- **THEN** A call graph is returned

### Sub-component: Extracttsgraph

> Implements: `extractTSGraph`

Extracts a call graph from TypeScript source code

#### Requirement: Extracttypescriptgraph

The system SHALL the function shall extract a call graph from typescript source code

#### Scenario: Extracttsgraph
- **GIVEN** TypeScript source code
- **WHEN** The function is called
- **THEN** A call graph is returned

### Sub-component: Extractgograph

> Implements: `extractGoGraph`

Extracts a call graph from Go source code

#### Requirement: Extractgograph

The system SHALL the function shall extract a call graph from go source code

#### Scenario: Extractgograph
- **GIVEN** Go source code
- **WHEN** The function is called
- **THEN** A call graph is returned

### Sub-component: Extractrustgraph

> Implements: `extractRustGraph`

Extracts a call graph from Rust source code

#### Requirement: Extractrustgraph

The system SHALL the function shall extract a call graph from rust source code

#### Scenario: Extractrustgraph
- **GIVEN** Rust source code
- **WHEN** The function is called
- **THEN** A call graph is returned

### Sub-component: Extractrubygraph

> Implements: `extractRubyGraph`

Extracts a call graph from Ruby source code

#### Requirement: Extractrubygraph

The system SHALL the function shall extract a call graph from ruby source code

#### Scenario: Extractrubygraph
- **GIVEN** Ruby source code
- **WHEN** The function is called
- **THEN** A call graph is returned

### Sub-component: Extractjavagraph

> Implements: `extractJavaGraph`

Extracts a call graph from Java source code

#### Requirement: Extractjavagraph

The system SHALL the function shall extract a call graph from java source code

#### Scenario: Extractjavagraph
- **GIVEN** Java source code
- **WHEN** The function is called
- **THEN** A call graph is returned

### Sub-component: Extractcppgraph

> Implements: `extractCppGraph`

Extracts a call graph from C++ source code

#### Requirement: Extractcppgraph

The system SHALL the function shall extract a call graph from c++ source code

#### Scenario: Extractcppgraph
- **GIVEN** C++ source code
- **WHEN** The function is called
- **THEN** A call graph is returned

### Sub-component: Extractswiftgraph

> Implements: `extractSwiftGraph`

Extracts a call graph from Swift source code

#### Requirement: Extractswiftgraph

The system SHALL the function shall extract a call graph from swift source code

#### Scenario: Extractswiftgraph
- **GIVEN** Swift source code
- **WHEN** The function is called
- **THEN** A call graph is returned

### Sub-component: Findbyqualifiedname

> Implements: `findByQualifiedName`

Finds function nodes by their qualified names

#### Requirement: Findfunctionbyqualifiedname

The system SHALL the function shall find function nodes by their qualified names

#### Scenario: Findbyqualifiedname
- **GIVEN** A class name and method name
- **WHEN** The function is called
- **THEN** An array of function nodes is returned

### Sub-component: Infertypesfromsource

> Implements: `inferTypesFromSource`

Infers types from source code for various languages

#### Requirement: Infertypes

The system SHALL the function shall infer types from source code for various languages

#### Scenario: Infertypesfromsource
- **GIVEN** Source code and a language
- **WHEN** The function is called
- **THEN** Inferred types are returned

### Sub-component: Resolveviatypeinference

> Implements: `resolveViaTypeInference`

Resolves callee objects via type inference

#### Requirement: Resolvecalleeviatypeinference

The system SHALL the function shall resolve callee objects via type inference

#### Scenario: Resolveviatypeinference
- **GIVEN** A callee object, callee name, inferred types, and a trie
- **WHEN** The function is called
- **THEN** A resolved callee object is returned

### Sub-component: Findbysimplename

> Implements: `findBySimpleName`

Finds function nodes by their simple names

#### Requirement: Findfunctionbysimplename

The system SHALL the function shall find function nodes by their simple names

#### Scenario: Findbysimplename
- **GIVEN** A function name
- **WHEN** The function is called
- **THEN** An array of function nodes is returned

### Sub-component: Extractallhttpedges

> Implements: `extractAllHttpEdges`

Extracts HTTP edges from mixed JS+Python codebase

#### Requirement: Extracthttpedges

The system SHALL the function shall extract http edges from mixed js+python codebase

#### Scenario: Extractallhttpedges
- **GIVEN** An array of file paths
- **WHEN** The function is called
- **THEN** A promise of HTTP edges is returned

### Sub-component: Findenclosingfunction

> Implements: `findEnclosingFunction`

Finds the enclosing function for a given node

#### Requirement: Findenclosingfunction

The system SHALL the function shall find the enclosing function for a given node

#### Scenario: Findenclosingfunction
- **GIVEN** An AST node
- **WHEN** The function is called
- **THEN** The enclosing function node is returned

### Sub-component: Extractclassrelationships

> Implements: `extractClassRelationships`

Extracts class relationships from source code

#### Requirement: Extractclassrelationships

The system SHALL the function shall extract class relationships from source code

#### Scenario: Extractclassrelationships
- **GIVEN** Source code
- **WHEN** The function is called
- **THEN** Class relationships are returned

### Sub-component: Buildclassnodes

> Implements: `buildClassNodes`

Builds class nodes from source code

#### Requirement: Buildclassnodes

The system SHALL the function shall build class nodes from source code

#### Scenario: Buildclassnodes
- **GIVEN** Source code
- **WHEN** The function is called
- **THEN** An array of class nodes is returned

### Requirement: Detectclones

The system SHALL analyzes call graph data to identify duplicate code instances across the codebase.

#### Scenario: ExactCloneDetection
- **GIVEN** Multiple functions with identical normalized code
- **WHEN** detectClones is called
- **THEN** Functions are grouped into exact clone groups

#### Scenario: StructuralCloneDetection
- **GIVEN** Functions with similar structure but different identifiers
- **WHEN** detectClones is called
- **THEN** Functions are grouped into structural clone groups

#### Scenario: NearCloneDetection
- **GIVEN** Functions with high Jaccard similarity
- **WHEN** detectClones is called
- **THEN** Functions are grouped into near clone groups

### Requirement: Normalizecode

The system SHALL normalizes source code to facilitate comparison for duplicate detection.

#### Scenario: Type1Normalization
- **GIVEN** Source code with comments and varying whitespace
- **WHEN** normalizeCode is called with type 'type1'
- **THEN** Comments are removed and whitespace is normalized

#### Scenario: Type2Normalization
- **GIVEN** Source code with non-keyword identifiers
- **WHEN** normalizeCode is called with type 'type2'
- **THEN** Non-keyword identifiers are replaced with sequential placeholders

### Requirement: Computesimilarity

The system SHALL computes similarity between code blocks using jaccard similarity on token n-grams.

#### Scenario: HighSimilarity
- **GIVEN** Two code blocks with many common n-grams
- **WHEN** computeSimilarity is called
- **THEN** High similarity score is returned

#### Scenario: LowSimilarity
- **GIVEN** Two code blocks with few common n-grams
- **WHEN** computeSimilarity is called
- **THEN** Low similarity score is returned

### Requirement: Infertypesfromsource

The system SHALL infer types from source code for a given language.

#### Scenario: PythonTypeInference
- **GIVEN** Python source code with class instantiations
- **WHEN** inferTypesFromSource is called with language 'Python'
- **THEN** Returns a map of variable names to their inferred class names

#### Scenario: C++TypeInference
- **GIVEN** C++ source code with class instantiations
- **WHEN** inferTypesFromSource is called with language 'C++'
- **THEN** Returns a map of variable names to their inferred class names

#### Scenario: TypescriptTypeInference
- **GIVEN** TypeScript source code with class instantiations
- **WHEN** inferTypesFromSource is called with language 'TypeScript'
- **THEN** Returns a map of variable names to their inferred class names

#### Scenario: GoTypeInference
- **GIVEN** Go source code with class instantiations
- **WHEN** inferTypesFromSource is called with language 'Go'
- **THEN** Returns a map of variable names to their inferred class names

#### Scenario: RustTypeInference
- **GIVEN** Rust source code with class instantiations
- **WHEN** inferTypesFromSource is called with language 'Rust'
- **THEN** Returns a map of variable names to their inferred class names

#### Scenario: JavaTypeInference
- **GIVEN** Java source code with class instantiations
- **WHEN** inferTypesFromSource is called with language 'Java'
- **THEN** Returns a map of variable names to their inferred class names

#### Scenario: RubyTypeInference
- **GIVEN** Ruby source code with class instantiations
- **WHEN** inferTypesFromSource is called with language 'Ruby'
- **THEN** Returns a map of variable names to their inferred class names

### Requirement: Resolveviatypeinference

The system SHALL resolve a method call to a functionnode using inferred types.

#### Scenario: SuccessfulResolution
- **GIVEN** A variable name with an inferred type and a method name
- **WHEN** resolveViaTypeInference is called with valid inputs
- **THEN** Returns the corresponding FunctionNode from the trie

#### Scenario: UnsuccessfulResolution
- **GIVEN** A variable name without an inferred type
- **WHEN** resolveViaTypeInference is called with invalid inputs
- **THEN** Returns undefined

### Requirement: Calculatenamescore

The system SHALL calculates a score for a file based on its name patterns.

#### Scenario: HighValueNamePattern
- **GIVEN** A file named 'auth-service.ts'
- **WHEN** calculateNameScore is called
- **THEN** The score should be 25

#### Scenario: NegativeNamePattern
- **GIVEN** A file named 'test-utils.ts'
- **WHEN** calculateNameScore is called
- **THEN** The score should be -10

### Requirement: Calculatepathscore

The system SHALL calculates a score for a file based on its path patterns.

#### Scenario: HighValuePathPattern
- **GIVEN** A file path 'src/core/auth-service.ts'
- **WHEN** calculatePathScore is called
- **THEN** The score should be 15

#### Scenario: DeeplyNestedFile
- **GIVEN** A file path 'src/utils/helpers/deeply/nested/file.ts'
- **WHEN** calculatePathScore is called
- **THEN** The score should be reduced due to depth penalty

### Requirement: Calculatestructurescore

The system SHALL calculates a score for a file based on its structure and content.

#### Scenario: FileWithClassDefinitions
- **GIVEN** A file containing class definitions
- **WHEN** calculateStructureScore is called
- **THEN** The score should include points for class definitions

#### Scenario: LargeFile
- **GIVEN** A file with more than 1000 lines
- **WHEN** calculateStructureScore is called
- **THEN** The score should be reduced due to size penalty

### Requirement: Buildfilerelationships

The system SHALL builds a map of file relationships based on import analysis.

#### Scenario: MultipleFiles
- **GIVEN** An array of file metadata
- **WHEN** buildFileRelationships is called
- **THEN** A map of file relationships should be created

### Requirement: Calculateconnectivityscore

The system SHALL calculates a score for a file based on its connectivity in the codebase.

#### Scenario: HighlyImportedFile
- **GIVEN** A file imported by multiple other files
- **WHEN** calculateConnectivityScore is called
- **THEN** The score should be high

#### Scenario: OrphanFile
- **GIVEN** A file with no imports or imports by other files
- **WHEN** calculateConnectivityScore is called
- **THEN** The score should be reduced due to orphan penalty

### Requirement: Parsehttpcalls

The system SHALL extracts http calls from javascript/typescript source files.

#### Scenario: ValidHttpCallExtraction
- **GIVEN** A JavaScript file with HTTP calls
- **WHEN** parseHttpCalls is called
- **THEN** An array of HttpCall objects is returned

### Requirement: Parseroutedefinitions

The system SHALL extracts route definitions from python source files.

#### Scenario: ValidRouteDefinitionExtraction
- **GIVEN** A Python file with route definitions
- **WHEN** parseRouteDefinitions is called
- **THEN** An array of RouteDefinition objects is returned

### Requirement: Matchhttpcallstoroutes

The system SHALL matches http calls to route definitions to identify cross-language edges.

#### Scenario: ExactMatch
- **GIVEN** An HTTP call and a route definition with the same normalized path
- **WHEN** matchHttpCallsToRoutes is called
- **THEN** An HttpEdge object with confidence 'exact' is returned

#### Scenario: PathMatch
- **GIVEN** An HTTP call and a route definition with similar normalized paths
- **WHEN** matchHttpCallsToRoutes is called
- **THEN** An HttpEdge object with confidence 'path' is returned

#### Scenario: FuzzyMatch
- **GIVEN** An HTTP call and a route definition with different normalized paths
- **WHEN** matchHttpCallsToRoutes is called
- **THEN** An HttpEdge object with confidence 'fuzzy' is returned

### Requirement: Normalizeurl

The system SHALL normalizes a url or path to a comparable canonical form.

#### Scenario: UrlWithProtocolAndHost
- **GIVEN** A URL with protocol and host (e.g., 'https://example.com/foo')
- **WHEN** normalizeUrl is called
- **THEN** The normalized URL is '/foo'

#### Scenario: UrlWithQueryStringAndFragment
- **GIVEN** A URL with query string and fragment (e.g., '/foo?query=1#fragment')
- **WHEN** normalizeUrl is called
- **THEN** The normalized URL is '/foo'

#### Scenario: UrlWithPathParameters
- **GIVEN** A URL with path parameters (e.g., '/users/{id}')
- **WHEN** normalizeUrl is called
- **THEN** The normalized URL is '/users/:param'

### Requirement: Candidatepaths

The system SHALL generates candidate normalized paths for a frontend url.

#### Scenario: UrlWithApiPrefix
- **GIVEN** A URL with an API prefix (e.g., '/api/v1/users')
- **WHEN** candidatePaths is called
- **THEN** The candidates include both the original URL and the URL with the prefix stripped (e.g., '/users')

#### Scenario: UrlWithoutApiPrefix
- **GIVEN** A URL without an API prefix (e.g., '/users')
- **WHEN** candidatePaths is called
- **THEN** The candidates include only the original URL

### Requirement: Extracthttpcalls

The system SHALL extracts http calls from javascript/typescript files using various http clients (fetch, axios, ky, got).

#### Scenario: ValidJavascriptFile
- **GIVEN** A valid JavaScript file with HTTP calls
- **WHEN** extractHttpCalls is called
- **THEN** An array of HttpCall objects is returned

#### Scenario: InvalidFilePath
- **GIVEN** An invalid file path
- **WHEN** extractHttpCalls is called
- **THEN** An empty array is returned

#### Scenario: FileWithNoHttpCalls
- **GIVEN** A JavaScript file with no HTTP calls
- **WHEN** extractHttpCalls is called
- **THEN** An empty array is returned

### Requirement: Getparserforlanguage

The system SHALL retrieves a tree-sitter parser instance for a given programming language.

#### Scenario: SupportedLanguage
- **GIVEN** A supported language (e.g., TypeScript)
- **WHEN** getParserForLanguage is called
- **THEN** Returns a valid Parser instance for the language

#### Scenario: UnsupportedLanguage
- **GIVEN** An unsupported language
- **WHEN** getParserForLanguage is called
- **THEN** Returns null

### Requirement: Isimportnode

The system SHALL determines if a syntax node represents an import statement.

#### Scenario: ImportNode
- **GIVEN** A node type that represents an import (e.g., 'import_statement')
- **WHEN** isImportNode is called
- **THEN** Returns true

#### Scenario: NonImportNode
- **GIVEN** A node type that does not represent an import (e.g., 'function_declaration')
- **WHEN** isImportNode is called
- **THEN** Returns false

### Requirement: Extractheader

The system SHALL extracts the import/header block from the top of a file.

#### Scenario: FileWithImports
- **GIVEN** A file with import statements at the top
- **WHEN** extractHeader is called
- **THEN** Returns the source text up to the last import statement

#### Scenario: FileWithoutImports
- **GIVEN** A file without import statements
- **WHEN** extractHeader is called
- **THEN** Returns an empty string

### Requirement: Blanklinechunk

The system SHALL splits content into chunks based on blank lines.

#### Scenario: ContentSmallerThanMaxchars
- **GIVEN** Content length is less than maxChars
- **WHEN** blankLineChunk is called
- **THEN** Returns an array with the entire content as a single chunk

#### Scenario: ContentLargerThanMaxchars
- **GIVEN** Content length is greater than maxChars
- **WHEN** blankLineChunk is called
- **THEN** Returns an array of chunks split by blank lines

### Requirement: Astchunkcontent

The system SHALL splits content into chunks based on ast boundaries.

#### Scenario: ContentSmallerThanMaxchars
- **GIVEN** Content length is less than maxChars
- **WHEN** astChunkContent is called
- **THEN** Returns an array with the entire content as a single chunk

#### Scenario: SupportedLanguage
- **GIVEN** A supported language and valid content
- **WHEN** astChunkContent is called
- **THEN** Returns an array of chunks split by AST boundaries

#### Scenario: UnsupportedLanguage
- **GIVEN** An unsupported language
- **WHEN** astChunkContent is called
- **THEN** Falls back to blankLineChunk and returns an array of chunks split by blank lines

### Requirement: Generatedigest

The system SHALL creates a digest file containing codebase metrics and dependency information.

#### Scenario: SuccessfulDigestGeneration
- **GIVEN** Valid project root path and output directory
- **WHEN** generateDigest is called
- **THEN** Digest file is created with architecture metrics

### Requirement: Rel

The system SHALL converts an absolute path to a relative path based on the project root.

#### Scenario: PathConversion
- **GIVEN** Absolute path and project root path
- **WHEN** rel is called
- **THEN** Returns relative path from project root

### Requirement: Filecoupling

The system SHALL extracts file coupling metrics from dependency graph.

#### Scenario: ValidDependencyGraph
- **GIVEN** Non-null dependency graph
- **WHEN** fileCoupling is called
- **THEN** Returns sorted array of file coupling metrics

#### Scenario: NullDependencyGraph
- **GIVEN** Null dependency graph
- **WHEN** fileCoupling is called
- **THEN** Returns empty array

### Requirement: Generatecodebasedigest

The system SHALL creates a codebase.md file containing architectural insights about the codebase.

#### Scenario: SuccessfulGeneration
- **GIVEN** Valid codebase analysis data and output directory
- **WHEN** generateCodebaseDigest is called
- **THEN** CODEBASE.md file is created with architectural insights

#### Scenario: FailedGeneration
- **GIVEN** Invalid input data or inaccessible output directory
- **WHEN** generateCodebaseDigest is called
- **THEN** Function returns false and no file is created

### Requirement: Buildadjacency

The system SHALL builds forward and backward adjacency maps from a serialized call graph.

#### Scenario: ValidCallGraph
- **GIVEN** A valid serialized call graph
- **WHEN** buildAdjacency is called
- **THEN** Forward and backward adjacency maps are built successfully

### Requirement: Bfs

The system SHALL performs a breadth-first search on the adjacency map.

#### Scenario: ValidSeedsAndAdjacency
- **GIVEN** Valid seeds and adjacency map
- **WHEN** bfs is called
- **THEN** Nodes are visited up to the specified max depth

### Requirement: Computeriskscore

The system SHALL computes a risk score for a node in the call graph.

#### Scenario: ValidNode
- **GIVEN** A valid function node
- **WHEN** computeRiskScore is called
- **THEN** A risk score between 0 and 100 is computed

### Requirement: Recommendstrategy

The system SHALL recommends a refactoring strategy based on risk score and node metrics.

#### Scenario: ValidInputs
- **GIVEN** Valid risk score and node metrics
- **WHEN** recommendStrategy is called
- **THEN** A refactoring strategy is recommended

### Requirement: Nodetosummary

The system SHALL converts a function node to a summary object.

#### Scenario: ValidNode
- **GIVEN** A valid function node
- **WHEN** nodeToSummary is called
- **THEN** A summary object is returned

### Requirement: Handlegetcallgraph

The system SHALL returns the call graph summary from cached analysis.

#### Scenario: ValidDirectory
- **GIVEN** A valid directory with cached analysis
- **WHEN** handleGetCallGraph is called
- **THEN** The call graph summary is returned

### Requirement: Handlegetsubgraph

The system SHALL extracts a depth-limited subgraph centered on a named function.

#### Scenario: ValidInputs
- **GIVEN** Valid directory, function name, direction, max depth, and format
- **WHEN** handleGetSubgraph is called
- **THEN** A depth-limited subgraph is returned

### Requirement: Handleanalyzeimpact

The system SHALL performs deep impact analysis for a single symbol.

#### Scenario: ValidInputs
- **GIVEN** Valid directory, symbol, and depth
- **WHEN** handleAnalyzeImpact is called
- **THEN** Impact analysis results are returned

### Requirement: Handlegetlowriskrefactorcandidates

The system SHALL returns the safest functions to refactor.

#### Scenario: ValidInputs
- **GIVEN** Valid directory, limit, and optional file pattern
- **WHEN** handleGetLowRiskRefactorCandidates is called
- **THEN** Low-risk refactor candidates are returned

### Requirement: Handlegetleaffunctions

The system SHALL returns leaf functions (fan-out === 0).

#### Scenario: ValidInputs
- **GIVEN** Valid directory, limit, optional file pattern, and optional sort by
- **WHEN** handleGetLeafFunctions is called
- **THEN** Leaf functions are returned

### Requirement: Handlegetcriticalhubs

The system SHALL returns critical hub functions ranked by composite criticality.

#### Scenario: ValidInputs
- **GIVEN** Valid directory, limit, and min fan in
- **WHEN** handleGetCriticalHubs is called
- **THEN** Critical hub functions are returned

### Requirement: Handlegetgodfunctions

The system SHALL detects god functions (high fan-out) and returns their call-graph neighborhood.

#### Scenario: ValidInputs
- **GIVEN** Valid directory, optional file path, and fan out threshold
- **WHEN** handleGetGodFunctions is called
- **THEN** God functions and their neighborhoods are returned

### Requirement: Handlegetfiledependencies

The system SHALL returns the file-level import dependencies for a given file.

#### Scenario: ValidInputs
- **GIVEN** Valid directory, file path, and direction
- **WHEN** handleGetFileDependencies is called
- **THEN** File dependencies are returned

### Requirement: Handletraceexecutionpath

The system SHALL finds all execution paths between two functions in the call graph.

#### Scenario: ValidInputs
- **GIVEN** Valid directory, entry function, target function, max depth, and max paths
- **WHEN** handleTraceExecutionPath is called
- **THEN** Execution paths are returned


## Sub-components

> `GraphService` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: Validatedirectory

> Implements: `validateDirectory`

Resolve and validate a user-supplied directory path.

#### Requirement: Validatedirectorypath

The system SHALL the function shall resolve and validate the provided directory path.

#### Scenario: Validdirectorypath
- **GIVEN** A valid directory path is provided
- **WHEN** The function is called with the directory path
- **THEN** The function SHALL resolve the path and return it as a string

### Sub-component: Readcachedcontext

> Implements: `readCachedContext`

Read the cached context from a specified directory.

#### Requirement: Readcontext

The system SHALL the function shall read the cached context from the specified directory.

#### Scenario: Contextexists
- **GIVEN** A directory with a cached context is provided
- **WHEN** The function is called with the directory path
- **THEN** The function SHALL return the cached context

### Sub-component: Buildadjacency

> Implements: `buildAdjacency`

Build forward and backward adjacency maps from a serialized call graph.

#### Requirement: Buildadjacencymaps

The system SHALL the function shall build forward (caller→callees) and backward (callee→callers) adjacency maps from the provided serialized call graph.

#### Scenario: Validcallgraph
- **GIVEN** A valid serialized call graph is provided
- **WHEN** The function is called with the call graph
- **THEN** The function SHALL build and store the adjacency maps

### Sub-component: Readspecgenconfig

> Implements: `readSpecGenConfig`

Read the spec-gen configuration from a specified root path.

#### Requirement: Readconfig

The system SHALL the function shall read the spec-gen configuration from the specified root path.

#### Scenario: Configexists
- **GIVEN** A root path with a spec-gen configuration is provided
- **WHEN** The function is called with the root path
- **THEN** The function SHALL return the spec-gen configuration

### Sub-component: Bfs

> Implements: `bfs`

Perform a breadth-first search on the adjacency maps.

#### Requirement: Performbfs

The system SHALL the function shall perform a breadth-first search on the adjacency maps starting from the provided seeds and within the specified maximum depth.

#### Scenario: Validseeds
- **GIVEN** Valid seeds and adjacency maps are provided
- **WHEN** The function is called with the seeds, adjacency maps, and maximum depth
- **THEN** The function SHALL perform the breadth-first search and return the results

### Sub-component: Nodetosummary

> Implements: `nodeToSummary`

Convert a function node to a summary.

#### Requirement: Convertnodetosummary

The system SHALL the function shall convert the provided function node to a summary.

#### Scenario: Validnode
- **GIVEN** A valid function node is provided
- **WHEN** The function is called with the node
- **THEN** The function SHALL return the summary of the node

### Sub-component: Computeriskscore

> Implements: `computeRiskScore`

Compute a risk score for a node based on its blast radius and hub status.

#### Requirement: Calculateriskscore

The system SHALL the function shall compute a risk score [0–100] for the provided node based on its blast radius and hub status.

#### Scenario: Validnode
- **GIVEN** A valid node, blast radius, and hub status are provided
- **WHEN** The function is called with the node, blast radius, and hub status
- **THEN** The function SHALL return the computed risk score

### Sub-component: Recommendstrategy

> Implements: `recommendStrategy`

Recommend a strategy based on the risk score, fan-in, fan-out, and hub status.

#### Requirement: Recommendstrategy

The system SHALL the function shall recommend a strategy based on the provided risk score, fan-in, fan-out, and hub status.

#### Scenario: Validinputs
- **GIVEN** Valid risk score, fan-in, fan-out, and hub status are provided
- **WHEN** The function is called with the inputs
- **THEN** The function SHALL return the recommended strategy

### Requirement: Getskeletoncontent

The system SHALL strips implementation noise from source code while preserving logical structure.

#### Scenario: BasicSkeletonExtraction
- **GIVEN** Source code with logs and comments
- **WHEN** getSkeletonContent is called
- **THEN** Returns code with logs and comments removed while preserving structure

#### Scenario: LanguageSpecificPatterns
- **GIVEN** Source code in Python with logging statements
- **WHEN** getSkeletonContent is called with language=Python
- **THEN** Returns code with Python-specific logging statements removed

### Requirement: Detectlanguage

The system SHALL detects the programming language of a file based on its extension.

#### Scenario: KnownExtension
- **GIVEN** File path with .ts extension
- **WHEN** detectLanguage is called
- **THEN** Returns 'TypeScript'

#### Scenario: UnknownExtension
- **GIVEN** File path with .unknown extension
- **WHEN** detectLanguage is called
- **THEN** Returns 'unknown'

### Requirement: Isskeletonworthincluding

The system SHALL determines if the skeleton is worth including based on reduction ratio.

#### Scenario: SignificantReduction
- **GIVEN** Original code is 1000 characters and skeleton is 200 characters
- **WHEN** isSkeletonWorthIncluding is called
- **THEN** Returns true

#### Scenario: MinimalReduction
- **GIVEN** Original code is 1000 characters and skeleton is 900 characters
- **WHEN** isSkeletonWorthIncluding is called
- **THEN** Returns false

## Technical Notes

- **Implementation**: `src/core/analyzer/repository-mapper.ts, src/api/types.ts, src/core/analyzer/embedding-service.ts, src/core/analyzer/subgraph-extractor.ts, src/core/analyzer/architecture-writer.ts, src/core/analyzer/dependency-graph.ts, src/core/analyzer/spec-vector-index.ts, src/core/analyzer/file-walker.ts, src/core/analyzer/import-resolver-bridge.ts, src/core/analyzer/refactor-analyzer.ts, src/core/analyzer/vector-index.ts, src/core/analyzer/import-parser.ts, src/core/analyzer/signature-extractor.ts, src/core/analyzer/artifact-generator.ts, src/core/analyzer/cpp-header-resolver.ts, src/core/analyzer/call-graph.ts, src/core/analyzer/duplicate-detector.ts, src/core/analyzer/type-inference-engine.ts, src/core/analyzer/significance-scorer.ts, src/core/analyzer/http-route-parser.ts, src/core/analyzer/function-registry-trie.ts`
- **Dependencies**: FileWalker, SignificanceScorer, SerializedCallGraph, FunctionNode, FileSignatureMap, DependencyGraphResult, LLMContext, ImportExportParser, HttpRouteParser, ScoredFile, resolveImport, extractAllHttpEdges, EmbeddingService, fileExists, node:fs/promises, node:path, ignore, FileAnalysis, CallGraph, DuplicateDetector, CodeShaper, ExtractedSignature, RepositoryMap, DependencyGraph, FileSystem, TypeInferenceEngine, LanguageParsers, ImportMap, CallGraphResult, FunctionRegistryTrie, FileMetadata, ScoringConfig, FileRelationship, tree-sitter, tree-sitter-typescript, tree-sitter-python, tree-sitter-go, tree-sitter-rust, tree-sitter-ruby, tree-sitter-java, code-shaper, DigestOptions
