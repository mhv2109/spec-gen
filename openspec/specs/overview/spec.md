# System Overview

> Generated by spec-gen v1.0.0 on 2026-03-25 22:10
> Confidence: 90%

## Purpose

OpenSpec is a CLI tool that generates and manages OpenSpec specifications from codebases using LLM
analysis. It helps maintain accurate, up-to-date documentation by analyzing code structure,
detecting changes, and verifying specifications against the codebase.

## Domains

This system is organized into the following domains:

| Domain | Description | Spec |
|--------|-------------|------|
| Analyzer | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Analyzes code structure to identify potential refactoring opportunities and architectural issues. | [spec.md](../analyzer/spec.md) |
| Api | Defines core data models: ProgressEvent, BaseOptions, InitApiOptions and 12 more. | [spec.md](../api/spec.md) |
| Services | Manages reading and writing configuration files for spec-gen and OpenSpec projects. | [spec.md](../services/spec.md) |
| Generator | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Generates OpenSpec formatted documentation from pipeline results, organizing content by domain and type. | [spec.md](../generator/spec.md) |
| Drift | Identifies discrepancies between code changes and specification documentation, ensuring documentation remains accurate and up-to-date. | [spec.md](../drift/spec.md) |
| Verifier | [PARTIAL SPEC — file too large to fully analyze (3 parts)] Validates the accuracy of OpenSpec specifications against actual code implementation. | [spec.md](../verifier/spec.md) |
| Types | Defines core data models: OpenSpecDomain. | [spec.md](../types/spec.md) |
| Spec | Returns the full content of a spec domain's spec.md plus its mapping entries. | [spec.md](../spec/spec.md) |
| Openspec | [PARTIAL SPEC — file too large to fully analyze (3 parts)] Handles compatibility and validation of OpenSpec configurations and specifications. | [spec.md](../openspec/spec.md) |
| Import | [PARTIAL SPEC — file too large to fully analyze (5 parts)] Parses and analyzes import/export statements in source files to extract module dependencies and relationships. | [spec.md](../import/spec.md) |
| Chat | [PARTIAL SPEC — file too large to fully analyze (4 parts)] Manages interactions with language model providers (Gemini, Anthropic, OpenAI-compatible) to facilitate code analysis and query resolution. | [spec.md](../chat/spec.md) |
| Mcp-handlers | Provides utility functions for handling directory paths, file operations, and error sanitization in the MCP tool. | [spec.md](../mcp-handlers/spec.md) |

## Technical Stack

- **Type**: Command Line Tool
- **Primary Language**: TypeScript
- **Key Frameworks**: React, Vitest
- **Architecture**: Layered Architecture

## Requirements

### Requirement: SystemCapabilities

The system SHALL provide the following capabilities:
- Use of layered architecture to separate concerns and enable modular development
- Integration with multiple LLM providers to support different use cases and environments
- Use of vector indexing for efficient semantic search and retrieval of code and specification content
- Emphasis on static analysis and code structure understanding to reduce reliance on LLM capabilities
- Support for incremental analysis and re-indexing to handle large codebases and frequent changes
- Use of Git for change detection and drift analysis to ensure specifications remain accurate
- Modular design of domain analyzers to enable extension and customization for different programming languages and frameworks

#### Scenario: CapabilitiesProvided
- **GIVEN** the system is operational
- **WHEN** a user interacts with the system
- **THEN** the system provides the documented capabilities

### Requirement: DataFlow

The system SHALL process data through defined layers:

#### Scenario: StandardDataFlow
- **GIVEN** an incoming request
- **WHEN** the request is processed
- **THEN** data flows through: CLI/API request → core services → domain analyzers → LLM providers → utilities; results are persisted to disk and can be viewed or processed further

## Technical Notes

- **Architecture Style**: Layered architecture: CLI interface → core services → domain-specific analyzers → LLM providers and utilities. This pattern separates concerns and allows for modular development and testing.
- **Security Model**: No explicit authentication or authorization mechanisms are present in the code. The tool relies on local file system access and assumes users have appropriate permissions.
- **External Integrations**: OpenAI-compatible LLM providers (OpenAI, Ollama, LocalAI, vLLM, LM Studio), Git for version control and change detection, LanceDB for vector indexing and semantic search
