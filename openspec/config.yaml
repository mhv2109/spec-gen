schema: spec-driven
spec-gen:
  version: 1.0.0
  generatedAt: 2026-03-08T15:15:09.407Z
  domains:
    - analyzer
    - api
    - services
    - generator
    - drift
    - verifier
    - types
    - cli
    - spec
    - openspec
    - chat
    - mcp-handlers
  confidence: 0.9
context: >-
  # Auto-detected by spec-gen

  Tech stack: TypeScript, React, Vitest

  Architecture: Layered (routes -> controllers -> services -> repositories)

  Domains: analyzer, api, services, generator, drift, verifier, types, cli,
  viewer, spec, openspec, chat, stages, mcp-handlers, utilities, commands,
  components, app

  Key patterns: Component-based UI
