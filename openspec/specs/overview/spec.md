# System Overview

> Generated by spec-gen v1.0.0 on 2026-03-25 21:54
> Confidence: 90%

## Purpose

OpenSpec is a CLI tool designed to generate and manage OpenSpec specifications from codebases. It
provides a comprehensive framework for analyzing code, generating specifications, verifying
accuracy, and detecting drift between code and specifications.

## Domains

This system is organized into the following domains:

| Domain | Description | Spec |
|--------|-------------|------|
| Analyzer | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Analyzes codebase structure to identify potential refactoring opportunities by examining function relationships, call graphs, and code quality metrics. | [spec.md](../analyzer/spec.md) |
| Api | Defines core data models: ProgressEvent, BaseOptions, InitApiOptions and 13 more. | [spec.md](../api/spec.md) |
| Services | Manages reading, writing, and merging configuration files for spec-gen and OpenSpec. | [spec.md](../services/spec.md) |
| Generator | [PARTIAL SPEC — file too large to fully analyze (2 parts)] Generates OpenSpec formatted documentation from analysis results, organizing content into domain-specific specifications. | [spec.md](../generator/spec.md) |
| Drift | Detects discrepancies between code changes and specification updates to identify potential drift issues. | [spec.md](../drift/spec.md) |
| Verifier | [PARTIAL SPEC — file too large to fully analyze (3 parts)] Verifies the accuracy of OpenSpec specifications against actual code files. | [spec.md](../verifier/spec.md) |
| Types | Defines core data models: AnalysisConfig, GenerationConfig, LLMConfig and 10 more. | [spec.md](../types/spec.md) |
| Cli | Provides interactive prompts for user input during the spec-gen CLI workflow. | [spec.md](../cli/spec.md) |
| Viewer | Defines core data models: ColorPalette, FileExtensionColor, RoleColor and 2 more. | [spec.md](../viewer/spec.md) |
| Spec | Defines core data models: SpecGenError. | [spec.md](../spec/spec.md) |
| Openspec | [PARTIAL SPEC — file too large to fully analyze (3 parts)] Validates OpenSpec documentation structure, requirements, scenarios, and config.yaml files to ensure compliance with OpenSpec conventions and standards. | [spec.md](../openspec/spec.md) |
| Import | [PARTIAL SPEC — file too large to fully analyze (5 parts)] Parses and analyzes import/export statements in source files to extract module dependencies and relationships. | [spec.md](../import/spec.md) |
| Chat | [PARTIAL SPEC — file too large to fully analyze (4 parts)] Handles interactions with different LLM providers (Gemini, Anthropic, OpenAI-compatible) to process chat messages and execute tool calls. | [spec.md](../chat/spec.md) |
| Mcp-handlers | Provides utility functions for handling MCP tool operations, including directory validation, error sanitization, and bidirectional code-spec mapping. | [spec.md](../mcp-handlers/spec.md) |
| Utilities | Provides utility functions for creating progress indicators and displaying success messages. | [spec.md](../utilities/spec.md) |

## Technical Stack

- **Type**: Command Line Tool
- **Primary Language**: TypeScript
- **Key Frameworks**: React, Vitest
- **Architecture**: Layered Architecture

## Requirements

### Requirement: SystemCapabilities

The system SHALL provide the following capabilities:
- Use layered architecture to separate concerns and improve maintainability
- Leverage LLM for specification generation and verification to reduce manual effort
- Implement incremental analysis and generation for better performance with large codebases
- Use vector indexing for efficient semantic search and retrieval of specifications
- Provide both CLI and API interfaces for flexibility in usage

#### Scenario: CapabilitiesProvided
- **GIVEN** the system is operational
- **WHEN** a user interacts with the system
- **THEN** the system provides the documented capabilities

### Requirement: DataFlow

The system SHALL process data through defined layers:

#### Scenario: StandardDataFlow
- **GIVEN** an incoming request
- **WHEN** the request is processed
- **THEN** data flows through: CLI/API request → service layer → core analysis/generation layer → data persistence; async LLM interactions for generation and verification

## Technical Notes

- **Architecture Style**: Layered architecture: CLI layer → API layer → service layer → core analysis and generation layer. This pattern separates concerns and allows for modular development and testing.
- **Security Model**: API key management for LLM providers; no explicit authentication/authorization for CLI commands
- **External Integrations**: Git, LLM providers (Claude, Mistral, OpenAI-compatible), LanceDB for vector indexing, tree-sitter for AST parsing
