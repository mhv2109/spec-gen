# Verifier Specification

> Generated by spec-gen v1.0.0 on 2026-03-25 22:10
> Source files: src/core/verifier/verification-engine.ts

## Purpose

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Validates the accuracy of OpenSpec
specifications against actual code implementation.

## Entities

### VerificationCandidate

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents a file candidate for
verification in the system.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| path | string | Relative path of the file |
| absolutePath | string | Absolute path of the file |
| domain | string | Domain the file belongs to |
| usedInGeneration | boolean | Whether the file was used in generation |
| complexity | number | Complexity score of the file |
| lines | number | Number of lines in the file |
| imports | number | Number of imports in the file |
| exports | number | Number of exports in the file |

### FilePrediction

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents the LLM's prediction about a
file's purpose and structure.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| predictedPurpose | string | Predicted purpose of the file |
| predictedImports | string[] | Predicted imports of the file |
| predictedExports | string[] | Predicted exports of the file |
| predictedLogic | string[] | Predicted logic patterns in the file |
| relatedRequirements | string[] | Requirements related to the file |
| confidence | number | Confidence score of the prediction |
| reasoning | string | Reasoning behind the prediction |

### PurposeMatch

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents the match result between
predicted and actual purpose of a file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| predicted | string | Predicted purpose |
| actual | string | Actual purpose |
| similarity | number | Similarity score between predicted and actual |

### SetMatch

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents the match result between
predicted and actual sets of imports/exports.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| predicted | string[] | Predicted set of imports/exports |
| actual | string[] | Actual set of imports/exports |
| precision | number | Precision score |
| recall | number | Recall score |
| f1Score | number | F1 score |

### RequirementCoverage

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents the coverage of requirements
by a file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| relatedRequirements | string[] | Requirements related to the file |
| actuallyImplements | string[] | Requirements actually implemented by the file |
| coverage | number | Coverage score |

### VerificationResult

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents the result of verifying a
single file.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| filePath | string | Path of the verified file |
| domain | string | Domain of the verified file |
| purposeMatch | PurposeMatch | Purpose match result |
| importMatch | SetMatch | Import match result |
| exportMatch | SetMatch | Export match result |
| requirementCoverage | RequirementCoverage | Requirement coverage result |
| overallScore | number | Overall score of the verification |
| llmConfidence | number | Confidence score from LLM |
| feedback | string[] | Feedback on the verification |

**Relationships:**

- contains PurposeMatch (VerificationResult contains a PurposeMatch)
- contains SetMatch (VerificationResult contains SetMatch for imports and exports)
- contains RequirementCoverage (VerificationResult contains a RequirementCoverage)

### DomainBreakdown

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents the breakdown of verification
results by domain.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| domain | string | Domain name |
| specPath | string | Path to the domain specification |
| filesVerified | number | Number of files verified in the domain |
| averageScore | number | Average score of the domain |
| weakestArea | string | Weakest area in the domain |

### SuggestedImprovement

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents a suggested improvement for a
domain.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| domain | string | Domain name |
| issue | string | Issue identified in the domain |
| suggestion | string | Suggestion for improvement |

### VerificationReport

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents a complete verification report
for the system.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| timestamp | string | Timestamp of the report |
| specVersion | string | Version of the specifications |
| sampledFiles | number | Number of files sampled |
| passedFiles | number | Number of files that passed verification |
| overallConfidence | number | Overall confidence score |
| domainBreakdown | DomainBreakdown[] | Breakdown of results by domain |
| commonGaps | string[] | Common gaps identified |
| recommendation | 'ready' | 'needs-review' | 'regenerate' | Recommendation for the system |
| suggestedImprovements | SuggestedImprovement[] | Suggested improvements |
| results | VerificationResult[] | Verification results for each file |

**Relationships:**

- contains DomainBreakdown (VerificationReport contains DomainBreakdown objects)
- contains SuggestedImprovement (VerificationReport contains SuggestedImprovement objects)
- contains VerificationResult (VerificationReport contains VerificationResult objects)

### VerificationEngineOptions

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Represents the options for the
verification engine.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| rootPath | string | Root directory of the project |
| openspecPath | string | Path to openspec directory |
| outputDir | string | Output directory for reports |
| minComplexity | number | Minimum complexity (lines) for candidate files |
| maxComplexity | number | Maximum complexity (lines) for candidate files |
| filesPerDomain | number | Number of files to sample per domain |
| passThreshold | number | Passing threshold for overall score |
| generationContext | string[] | Files used in generation (to exclude) |

### SpecVerificationEngine

[PARTIAL SPEC — file too large to fully analyze (3 parts)] Engine for verifying that specifications
accurately describe the codebase by using specs to predict code behavior and comparing against
actual files.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| llm | LLMService | Service for interacting with language models |
| options | Required<VerificationEngineOptions> | Configuration options for the verification engine |
| specs | LoadedSpec[] | Array of loaded specifications |
| parser | ImportExportParser | Parser for analyzing file imports and exports |

**Relationships:**

- uses LLMService (Uses LLMService for generating predictions)
- uses ImportExportParser (Uses ImportExportParser for analyzing file imports and exports)
- uses DependencyGraphResult (Uses DependencyGraphResult for selecting verification candidates)

## Requirements

### Requirement: VerificationCandidateValidation

The system SHALL validate VerificationCandidate according to these rules:
- path and absolutePath must be valid file paths
- complexity must be a positive number
- lines, imports, and exports must be non-negative numbers

#### Scenario: CreateVerificationCandidate
- **GIVEN** A file path and domain information
- **WHEN** VerificationCandidate is instantiated
- **THEN** A new VerificationCandidate object is created with the provided properties

### Requirement: FilePredictionValidation

The system SHALL validate FilePrediction according to these rules:
- confidence must be between 0 and 1
- predictedImports and predictedExports must be arrays of strings
- predictedLogic and relatedRequirements must be arrays of strings

#### Scenario: CreateFilePrediction
- **GIVEN** Prediction data from LLM
- **WHEN** FilePrediction is instantiated
- **THEN** A new FilePrediction object is created with the provided properties

### Requirement: PurposeMatchValidation

The system SHALL validate PurposeMatch according to these rules:
- similarity must be between 0 and 1

#### Scenario: CreatePurposeMatch
- **GIVEN** Predicted and actual purpose strings
- **WHEN** PurposeMatch is instantiated
- **THEN** A new PurposeMatch object is created with the provided properties

### Requirement: SetMatchValidation

The system SHALL validate SetMatch according to these rules:
- precision, recall, and f1Score must be between 0 and 1
- predicted and actual must be arrays of strings

#### Scenario: CreateSetMatch
- **GIVEN** Predicted and actual sets of imports/exports
- **WHEN** SetMatch is instantiated
- **THEN** A new SetMatch object is created with the provided properties

### Requirement: RequirementCoverageValidation

The system SHALL validate RequirementCoverage according to these rules:
- coverage must be between 0 and 1
- relatedRequirements and actuallyImplements must be arrays of strings

#### Scenario: CreateRequirementCoverage
- **GIVEN** Related and implemented requirements
- **WHEN** RequirementCoverage is instantiated
- **THEN** A new RequirementCoverage object is created with the provided properties

### Requirement: VerificationResultValidation

The system SHALL validate VerificationResult according to these rules:
- overallScore and llmConfidence must be between 0 and 1
- feedback must be an array of strings

#### Scenario: CreateVerificationResult
- **GIVEN** Verification data for a file
- **WHEN** VerificationResult is instantiated
- **THEN** A new VerificationResult object is created with the provided properties

### Requirement: DomainBreakdownValidation

The system SHALL validate DomainBreakdown according to these rules:
- averageScore must be between 0 and 1
- filesVerified must be a non-negative number

#### Scenario: CreateDomainBreakdown
- **GIVEN** Verification results for a domain
- **WHEN** DomainBreakdown is instantiated
- **THEN** A new DomainBreakdown object is created with the provided properties

### Requirement: VerificationReportValidation

The system SHALL validate VerificationReport according to these rules:
- overallConfidence must be between 0 and 1
- sampledFiles and passedFiles must be non-negative numbers
- recommendation must be one of 'ready', 'needs-review', or 'regenerate'

#### Scenario: CreateVerificationReport
- **GIVEN** Verification results and analysis data
- **WHEN** VerificationReport is instantiated
- **THEN** A new VerificationReport object is created with the provided properties

### Requirement: VerificationEngineOptionsValidation

The system SHALL validate VerificationEngineOptions according to these rules:
- minComplexity and maxComplexity must be positive numbers if provided
- filesPerDomain must be a positive number if provided
- passThreshold must be between 0 and 1 if provided

#### Scenario: CreateVerificationEngineOptions
- **GIVEN** Configuration data for the verification engine
- **WHEN** VerificationEngineOptions is instantiated
- **THEN** A new VerificationEngineOptions object is created with the provided properties

### Requirement: SpecVerificationEngineValidation

The system SHALL validate SpecVerificationEngine according to these rules:
- Must have valid LLMService instance
- Must have valid VerificationEngineOptions
- Specs must be loaded before verification can begin
- Verification candidates must be selected before verification can proceed

#### Scenario: VerifyCodebaseAgainstSpecifications
- **GIVEN** A codebase with specifications
- **WHEN** verify() is called with a dependency graph and spec version
- **THEN** A verification report is generated and saved
- **AND** All specs are loaded
- **AND** Verification candidates are selected
- **AND** Each candidate file is verified
- **AND** A report is generated and saved

#### Scenario: GetListOfDomains
- **GIVEN** A SpecVerificationEngine instance
- **WHEN** getDomains() is called
- **THEN** An array of domain names is returned
- **AND** Specs are loaded if not already loaded

### Requirement: Verifyspecifications

The system SHALL compares openspec specifications with actual code implementation to verify accuracy.

#### Scenario: SuccessfulVerification
- **GIVEN** Valid OpenSpec specifications and corresponding code files
- **WHEN** verifySpecifications is called
- **THEN** A VerificationReport is generated with high accuracy scores

#### Scenario: PartialVerification
- **GIVEN** Incomplete or outdated OpenSpec specifications
- **WHEN** verifySpecifications is called
- **THEN** A VerificationReport is generated with lower accuracy scores and suggestions for improvement

### Requirement: Generateverificationreport

The system SHALL generates a detailed report comparing specifications with code implementation.

#### Scenario: ReportGeneration
- **GIVEN** Verification results for multiple files
- **WHEN** generateVerificationReport is called
- **THEN** A comprehensive VerificationReport is generated with domain breakdown and overall confidence score

### Requirement: Analyzefileprediction

The system SHALL analyzes the llm prediction for a single file against its actual implementation.

#### Scenario: FileAnalysis
- **GIVEN** An LLM prediction and the actual file implementation
- **WHEN** analyzeFilePrediction is called
- **THEN** A VerificationResult is generated with purpose match, import/export match, and requirement coverage

### Requirement: Verify

The system SHALL runs full verification process including loading specs, selecting candidates, verifying files, and generating a report.

#### Scenario: SuccessfulVerification
- **GIVEN** Valid dependency graph and spec version
- **WHEN** verify is called
- **THEN** Verification report is generated and saved

#### Scenario: NoSpecsFound
- **GIVEN** No specs available in the openspec directory
- **WHEN** verify is called
- **THEN** Error is thrown indicating no specs found

#### Scenario: NoCandidatesFound
- **GIVEN** No suitable verification candidates found
- **WHEN** verify is called
- **THEN** Error is thrown indicating no candidates found

### Requirement: Loadspecs

The system SHALL loads all specifications from the openspec directory.

#### Scenario: SpecsLoadedSuccessfully
- **GIVEN** Specs directory exists and contains valid specs
- **WHEN** loadSpecs is called
- **THEN** Specs are loaded into the specs array

#### Scenario: SpecsDirectoryDoesNotExist
- **GIVEN** Specs directory does not exist
- **WHEN** loadSpecs is called
- **THEN** No specs are loaded

### Requirement: Selectcandidates

The system SHALL selects verification candidate files based on complexity and domain.

#### Scenario: CandidatesSelectedSuccessfully
- **GIVEN** Dependency graph with suitable files
- **WHEN** selectCandidates is called
- **THEN** Array of verification candidates is returned

#### Scenario: NoCandidatesFound
- **GIVEN** Dependency graph with no suitable files
- **WHEN** selectCandidates is called
- **THEN** Empty array is returned

### Requirement: Verifyfile

The system SHALL verifies a single file by comparing llm predictions to actual file content.

#### Scenario: FileVerifiedSuccessfully
- **GIVEN** Valid verification candidate
- **WHEN** verifyFile is called
- **THEN** Verification result is returned

#### Scenario: FileVerificationFails
- **GIVEN** Invalid verification candidate
- **WHEN** verifyFile is called
- **THEN** Error is thrown

### Requirement: Getprediction

The system SHALL gets predictions from llm for a given file.

#### Scenario: PredictionSuccessful
- **GIVEN** Valid verification candidate
- **WHEN** getPrediction is called
- **THEN** File prediction is returned

#### Scenario: PredictionFails
- **GIVEN** Invalid verification candidate
- **WHEN** getPrediction is called
- **THEN** Default prediction with low confidence is returned

### Requirement: Comparepurpose

The system SHALL compares predicted purpose to actual file content.

#### Scenario: PurposeMatchSuccessful
- **GIVEN** Valid predicted and actual purpose
- **WHEN** comparePurpose is called
- **THEN** Purpose match is returned

#### Scenario: PurposeMatchFails
- **GIVEN** Invalid predicted or actual purpose
- **WHEN** comparePurpose is called
- **THEN** Purpose match with zero similarity is returned

### Requirement: Compareimports

The system SHALL compares predicted imports to actual imports.

#### Scenario: ImportMatchSuccessful
- **GIVEN** Valid predicted and actual imports
- **WHEN** compareImports is called
- **THEN** Set match is returned

#### Scenario: ImportMatchFails
- **GIVEN** Invalid predicted or actual imports
- **WHEN** compareImports is called
- **THEN** Set match with zero scores is returned

### Requirement: Compareexports

The system SHALL compares predicted exports to actual exports.

#### Scenario: ExportMatchSuccessful
- **GIVEN** Valid predicted and actual exports
- **WHEN** compareExports is called
- **THEN** Set match is returned

#### Scenario: ExportMatchFails
- **GIVEN** Invalid predicted or actual exports
- **WHEN** compareExports is called
- **THEN** Set match with zero scores is returned

### Requirement: Analyzerequirementcoverage

The system SHALL analyzes requirement coverage in the file content.

#### Scenario: RequirementCoverageSuccessful
- **GIVEN** Valid related requirements and file content
- **WHEN** analyzeRequirementCoverage is called
- **THEN** Requirement coverage is returned

#### Scenario: RequirementCoverageFails
- **GIVEN** Invalid related requirements or file content
- **WHEN** analyzeRequirementCoverage is called
- **THEN** Requirement coverage with zero coverage is returned

### Requirement: Generatereport

The system SHALL generates a verification report from the results.

#### Scenario: ReportGeneratedSuccessfully
- **GIVEN** Valid verification results and spec version
- **WHEN** generateReport is called
- **THEN** Verification report is returned

#### Scenario: ReportGenerationFails
- **GIVEN** Invalid verification results or spec version
- **WHEN** generateReport is called
- **THEN** Error is thrown

### Requirement: Savereport

The system SHALL saves the verification report to the output directory.

#### Scenario: ReportSavedSuccessfully
- **GIVEN** Valid verification report
- **WHEN** saveReport is called
- **THEN** Report is saved to the output directory

#### Scenario: ReportSaveFails
- **GIVEN** Invalid verification report
- **WHEN** saveReport is called
- **THEN** Error is thrown

### Requirement: Getdomains

The system SHALL gets list of domains from loaded specs.

#### Scenario: DomainsRetrievedSuccessfully
- **GIVEN** Specs are loaded
- **WHEN** getDomains is called
- **THEN** Array of domains is returned

#### Scenario: DomainsRetrievalFails
- **GIVEN** Specs are not loaded
- **WHEN** getDomains is called
- **THEN** Specs are loaded and array of domains is returned

## Technical Notes

- **Implementation**: `src/core/verifier/verification-engine.ts`
- **Dependencies**: LLMService, DependencyGraphResult, ImportExportParser, DependencyNode
