# Drift Specification

> Generated by spec-gen v1.0.0 on 2026-03-25 21:54
> Source files: src/core/drift/git-diff.ts, src/core/drift/spec-mapper.ts, src/core/drift/drift-detector.ts

## Purpose

Detects discrepancies between code changes and specification updates to identify potential drift
issues.

## Entities

### GitDiffOptions

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Configuration options for generating git
diffs.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| rootPath | string | Root directory of the git repository |
| baseRef | string | Git reference to compare against |
| pathFilter | string[] | Optional paths to include in the diff |
| includeUnstaged | boolean | Whether to include unstaged changes |

### GitDiffResult

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Result of a git diff operation containing
changed files and metadata.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| resolvedBase | string | The resolved base reference used for comparison |
| files | ChangedFile[] | List of changed files |
| hasUnstagedChanges | boolean | Whether there are unstaged changes |
| currentBranch | string | Name of the current git branch |

**Relationships:**

- contains ChangedFile (GitDiffResult contains a list of ChangedFile objects)

### ChangedFile

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents a file that has changed in a
git repository.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| path | string | Path to the changed file |
| status | string | Status of the change (added, modified, deleted, renamed) |
| oldPath | string | Original path if the file was renamed |
| isTest | boolean | Whether the file is a test file |
| isConfig | boolean | Whether the file is a configuration file |
| isGenerated | boolean | Whether the file is generated |
| extension | string | File extension |

### GitDiff

[PARTIAL SPEC — file too large to fully analyze (2 parts)] Represents the diff information between
the working tree and a base reference in a Git repository.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| rootPath | string | The root path of the Git repository |
| baseRef | string | The base reference (branch or commit) to compare against |
| includeUnstaged | boolean | Whether to include unstaged changes in the diff |
| maxChars | number | The maximum number of characters to include in the diff output |

**Relationships:**

- contains ChangedFile (A GitDiff contains multiple ChangedFile entities)

### SpecMapping

Represents the mapping between a domain and its associated source files and specifications.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| domain | string | The domain name |
| specPath | string | Path to the specification file |
| declaredSourceFiles | string[] | Source files explicitly declared in the spec header |
| inferredSourceFiles | string[] | Source files inferred from the spec content |
| allSourceFiles | string[] | Combined list of declared and inferred source files |
| requirements | string[] | List of requirements extracted from the spec |
| entities | string[] | List of entities extracted from the spec |

### SpecMap

Represents the bidirectional mapping between domains and source files.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| byDomain | Map<string, SpecMapping> | Mapping from domain names to SpecMapping objects |
| byFile | Map<string, string[]> | Mapping from source file paths to domain names |
| domainCount | number | Total number of domains in the map |
| totalMappedFiles | number | Total number of source files mapped |

**Relationships:**

- contains SpecMapping (SpecMap contains multiple SpecMapping objects)

### SpecMapperOptions

Options for configuring the SpecMapper.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| rootPath | string | Root path of the project |
| openspecPath | string | Path to the OpenSpec directory |
| repoStructurePath | string | Optional path to the repository structure file |

### RepoStructureDomain

Represents a domain in the repository structure.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| name | string | Name of the domain |
| files | string[] | List of files associated with the domain |
| suggestedSpecPath | string | Suggested path for the specification file |

### DriftDetectorOptions

Configuration options for the drift detector, including thresholds and LLM settings.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| threshold | number | Minimum change magnitude to consider for drift detection |
| llmModel | string | Language model to use for LLM-enhanced analysis |
| maxLlmCalls | number | Maximum number of LLM calls allowed |

### DriftIssue

Represents a detected drift issue between code and specifications.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| kind | DriftIssueKind | Type of drift issue (gap, stale, uncovered, orphaned) |
| severity | DriftSeverity | Severity level of the issue |
| file | ChangedFile | File involved in the drift issue |
| domain | string | Domain affected by the drift |
| message | string | Description of the drift issue |

**Relationships:**

- belongs to ChangedFile (Drift issue is associated with a changed file)
- references SpecMap (Drift issue may reference specifications)

### DriftResult

Combined result of all drift detection operations.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| issues | DriftIssue[] | List of detected drift issues |
| summary | object | Summary statistics of drift issues |

**Relationships:**

- contains DriftIssue (Result contains multiple drift issues)

### ADRMap

Mapping between architectural decision records and their domains.

**Properties:**

| Name | Type | Description |
|------|------|-------------|
| adrMap | Map<string, string[]> | Map of ADR IDs to their associated domains |

## Requirements

### Requirement: GitDiffOptionsValidation

The system SHALL validate GitDiffOptions according to these rules:
- rootPath must be a valid directory path
- baseRef must be a valid git reference
- includeUnstaged must be a boolean

#### Scenario: ConfigureGitDiffOptions
- **GIVEN** A valid git repository path
- **WHEN** GitDiffOptions is created with valid parameters
- **THEN** Options are configured for generating git diffs

### Requirement: GitDiffResultValidation

The system SHALL validate GitDiffResult according to these rules:
- resolvedBase must be a valid git reference
- files must be an array of ChangedFile objects
- hasUnstagedChanges must be a boolean
- currentBranch must be a string

#### Scenario: GenerateGitDiffResult
- **GIVEN** A valid git repository and diff options
- **WHEN** gitDiff is called with valid parameters
- **THEN** GitDiffResult is returned with changed files and metadata

### Requirement: ChangedFileValidation

The system SHALL validate ChangedFile according to these rules:
- path must be a valid file path
- status must be one of: added, modified, deleted, renamed
- oldPath must be a valid file path if status is renamed
- isTest, isConfig, isGenerated must be booleans
- extension must be a string

#### Scenario: ClassifyChangedFile
- **GIVEN** A file path
- **WHEN** classifyFile is called with the file path
- **THEN** File is classified as test, config, or generated based on patterns

#### Scenario: CheckIfFileIsSkippable
- **GIVEN** A file path
- **WHEN** isSkippableFile is called with the file path
- **THEN** Returns true if the file is a binary or lock file, false otherwise

### Requirement: GitDiffValidation

The system SHALL validate GitDiff according to these rules:
- rootPath must be a valid directory path
- baseRef must be a valid Git reference (branch, tag, or commit hash)
- maxChars must be a positive integer

#### Scenario: GetFileDiff
- **GIVEN** A Git repository with changes between the working tree and a base reference
- **WHEN** getFileDiff is called with a file path and base reference
- **THEN** The unified diff content for the specified file is returned, truncated if necessary

#### Scenario: GetChangedFiles
- **GIVEN** A Git repository with changes between the working tree and a base reference
- **WHEN** getChangedFiles is called with the repository root path and base reference
- **THEN** A list of changed files with their addition and deletion counts is returned

### Requirement: DriftIssueValidation

The system SHALL validate DriftIssue according to these rules:
- Severity must be computed based on issue kind and change magnitude
- Message must describe the specific drift issue

#### Scenario: DetectGapIssue
- **GIVEN** Code changes in a domain without corresponding spec updates
- **WHEN** detectGaps() is called
- **THEN** A gap issue is created with appropriate severity

#### Scenario: DetectStaleSpec
- **GIVEN** Spec references files that were deleted or heavily modified
- **WHEN** detectStaleSpecs() is called
- **THEN** A stale spec issue is created

### Requirement: DriftResultValidation

The system SHALL validate DriftResult according to these rules:
- Summary must include counts of each issue type and severity level

#### Scenario: GenerateDriftReport
- **GIVEN** Drift detection has completed
- **WHEN** detectDrift() is called
- **THEN** A comprehensive result is returned with all issues and summary statistics

### Requirement: ADRMapValidation

The system SHALL validate ADRMap according to these rules:
- Each ADR must reference at least one domain

#### Scenario: ValidADRMapAccepted
- **GIVEN** A valid ADRMap object with all required fields
- **WHEN** The object is validated
- **THEN** Validation passes with no errors

### Requirement: Getchangedfiles

The system SHALL retrieves a list of changed files between two git references, including their status and classification.

#### Scenario: ValidGitRepositoryWithChanges
- **GIVEN** A Git repository with committed changes
- **WHEN** getChangedFiles is called with valid parameters
- **THEN** Returns a list of changed files with their status and classification

#### Scenario: EmptyGitRepository
- **GIVEN** A Git repository with no commits
- **WHEN** getChangedFiles is called
- **THEN** Returns an empty list of changed files

#### Scenario: InvalidGitReference
- **GIVEN** An invalid Git reference
- **WHEN** getChangedFiles is called with an invalid reference
- **THEN** Throws an error indicating the invalid reference

### Requirement: Classifyfile

The system SHALL classifies a file as test, configuration, generated, or other based on its path and extension.

#### Scenario: TestFileClassification
- **GIVEN** A file path matching a test pattern
- **WHEN** classifyFile is called
- **THEN** Returns isTest as true

#### Scenario: ConfigurationFileClassification
- **GIVEN** A file path matching a configuration pattern
- **WHEN** classifyFile is called
- **THEN** Returns isConfig as true

#### Scenario: GeneratedFileClassification
- **GIVEN** A file path matching a generated pattern
- **WHEN** classifyFile is called
- **THEN** Returns isGenerated as true

### Requirement: Isskippablefile

The system SHALL determines if a file should be skipped based on its extension or filename.

#### Scenario: SkippableFile
- **GIVEN** A file path with a skippable extension or filename
- **WHEN** isSkippableFile is called
- **THEN** Returns true

#### Scenario: NonSkippableFile
- **GIVEN** A file path with a non-skippable extension or filename
- **WHEN** isSkippableFile is called
- **THEN** Returns false

### Requirement: Isgitrepository

The system SHALL checks if the given path is a git repository.

#### Scenario: ValidGitRepository
- **GIVEN** A path to a valid Git repository
- **WHEN** isGitRepository is called
- **THEN** Returns true

#### Scenario: InvalidGitRepository
- **GIVEN** A path to an invalid Git repository
- **WHEN** isGitRepository is called
- **THEN** Returns false

### Requirement: Getcurrentbranch

The system SHALL retrieves the current branch name of the git repository.

#### Scenario: ValidGitRepository
- **GIVEN** A path to a valid Git repository
- **WHEN** getCurrentBranch is called
- **THEN** Returns the current branch name

#### Scenario: InvalidGitRepository
- **GIVEN** A path to an invalid Git repository
- **WHEN** getCurrentBranch is called
- **THEN** Returns 'unknown'

### Requirement: Validategitref

The system SHALL validates a user-supplied git reference to prevent unexpected git argument injection.

#### Scenario: ValidGitReference
- **GIVEN** A valid Git reference
- **WHEN** validateGitRef is called
- **THEN** Does not throw an error

#### Scenario: InvalidGitReference
- **GIVEN** An invalid Git reference
- **WHEN** validateGitRef is called
- **THEN** Throws an error indicating the invalid reference

### Requirement: Resolvebaseref

The system SHALL resolves a base git reference, falling back through main → master → head~1 if the preferred reference is not found.

#### Scenario: ValidPreferredReference
- **GIVEN** A valid preferred Git reference
- **WHEN** resolveBaseRef is called
- **THEN** Returns the preferred reference

#### Scenario: InvalidPreferredReference
- **GIVEN** An invalid preferred Git reference
- **WHEN** resolveBaseRef is called
- **THEN** Returns a fallback reference

### Requirement: Parsespecheader

The system SHALL extracts source file references and generation date from a spec file header.

#### Scenario: ValidHeaderWithSourceFiles
- **GIVEN** Spec content with 'Source files:' line
- **WHEN** parseSpecHeader is called
- **THEN** Returns array of source file paths and null date if not present

#### Scenario: HeaderWithGenerationDate
- **GIVEN** Spec content with 'Generated on' line
- **WHEN** parseSpecHeader is called
- **THEN** Returns the parsed date string

### Requirement: Parsespecreferences

The system SHALL extracts file references, requirements, and entities from a spec file body.

#### Scenario: ImplementationSectionWithFileReferences
- **GIVEN** Spec content with '**Implementation**' section
- **WHEN** parseSpecReferences is called
- **THEN** Returns array of file references from implementation section

#### Scenario: RequirementsSection
- **GIVEN** Spec content with '### Requirement:' lines
- **WHEN** parseSpecReferences is called
- **THEN** Returns array of requirement descriptions

#### Scenario: EntitiesSection
- **GIVEN** Spec content with '## Entities' section
- **WHEN** parseSpecReferences is called
- **THEN** Returns array of entity names

### Requirement: Inferdomainfrompath

The system SHALL infers a domain name from a file path based on directory structure.

#### Scenario: ExactDomainMatchInPath
- **GIVEN** File path containing one of known domains
- **WHEN** inferDomainFromPath is called
- **THEN** Returns the matching domain name

#### Scenario: NoDomainMatch
- **GIVEN** File path with no matching domains
- **WHEN** inferDomainFromPath is called
- **THEN** Returns null

### Requirement: Buildspecmap

The system SHALL builds a bidirectional mapping between spec domains and source files.

#### Scenario: ValidSpecDirectory
- **GIVEN** Directory with valid spec files
- **WHEN** buildSpecMap is called
- **THEN** Returns complete SpecMap with domain and file mappings

#### Scenario: MissingSpecDirectory
- **GIVEN** Non-existent spec directory
- **WHEN** buildSpecMap is called
- **THEN** Returns empty SpecMap

### Requirement: Getspeccontent

The system SHALL loads and returns the content of a spec file for a given domain.

#### Scenario: ExistingSpecFile
- **GIVEN** Valid domain with existing spec file
- **WHEN** getSpecContent is called
- **THEN** Returns the spec content truncated to maxChars

#### Scenario: MissingSpecFile
- **GIVEN** Domain with no spec file
- **WHEN** getSpecContent is called
- **THEN** Returns null

### Requirement: Detectdrift

The system SHALL runs all drift detection algorithms and produces a combined result.

#### Scenario: SuccessfulDetection
- **GIVEN** Valid options and spec map
- **WHEN** detectDrift is called
- **THEN** Returns a DriftResult with detected issues

#### Scenario: NoIssuesFound
- **GIVEN** No relevant changes in code or specs
- **WHEN** detectDrift is called
- **THEN** Returns an empty DriftResult

### Requirement: Detectgaps

The system SHALL detects gaps where code changed in a domain but the spec wasn't updated.

#### Scenario: GapsDetected
- **GIVEN** Code changes in domains without spec updates
- **WHEN** detectGaps is called
- **THEN** Returns array of DriftIssue objects

#### Scenario: NoGapsDetected
- **GIVEN** All code changes have corresponding spec updates
- **WHEN** detectGaps is called
- **THEN** Returns empty array

### Requirement: Detectstalespecs

The system SHALL detects stale specs that reference files that were deleted or heavily modified.

#### Scenario: StaleSpecsDetected
- **GIVEN** Specs reference deleted or heavily modified files
- **WHEN** detectStaleSpecs is called
- **THEN** Returns array of DriftIssue objects

#### Scenario: NoStaleSpecsDetected
- **GIVEN** All specs reference valid files
- **WHEN** detectStaleSpecs is called
- **THEN** Returns empty array

### Requirement: Detectuncoveredfiles

The system SHALL detects new files that don't map to any spec domain.

#### Scenario: UncoveredFilesDetected
- **GIVEN** New files without spec coverage
- **WHEN** detectUncoveredFiles is called
- **THEN** Returns array of DriftIssue objects

#### Scenario: NoUncoveredFilesDetected
- **GIVEN** All new files have spec coverage
- **WHEN** detectUncoveredFiles is called
- **THEN** Returns empty array

### Requirement: Detectorphanedspecs

The system SHALL detects orphaned specs that declare source files that don't exist on disk.

#### Scenario: OrphanedSpecsDetected
- **GIVEN** Specs reference non-existent files
- **WHEN** detectOrphanedSpecs is called
- **THEN** Returns array of DriftIssue objects

#### Scenario: NoOrphanedSpecsDetected
- **GIVEN** All specs reference valid files
- **WHEN** detectOrphanedSpecs is called
- **THEN** Returns empty array

### Requirement: Detectadrgaps

The system SHALL detects adr gaps where code changed in a domain referenced by an adr.

#### Scenario: AdrGapsDetected
- **GIVEN** Code changes in domains referenced by ADRs
- **WHEN** detectADRGaps is called
- **THEN** Returns array of DriftIssue objects

#### Scenario: NoAdrGapsDetected
- **GIVEN** No code changes in domains referenced by ADRs
- **WHEN** detectADRGaps is called
- **THEN** Returns empty array

### Requirement: Detectadrorphaned

The system SHALL detects orphaned adrs that reference domains that no longer exist in specs.

#### Scenario: OrphanedAdrsDetected
- **GIVEN** ADRs reference non-existent domains
- **WHEN** detectADROrphaned is called
- **THEN** Returns array of DriftIssue objects

#### Scenario: NoOrphanedAdrsDetected
- **GIVEN** All ADRs reference valid domains
- **WHEN** detectADROrphaned is called
- **THEN** Returns empty array

### Requirement: Enhancegapswithllm

The system SHALL post-processes gap issues using an llm to determine if code changes are significant enough to require spec updates.

#### Scenario: IssuesEnhanced
- **GIVEN** Valid LLM provider and issues to enhance
- **WHEN** enhanceGapsWithLLM is called
- **THEN** Returns enhanced array of DriftIssue objects

#### Scenario: NoIssuesEnhanced
- **GIVEN** No issues to enhance or LLM provider unavailable
- **WHEN** enhanceGapsWithLLM is called
- **THEN** Returns original array of DriftIssue objects


## Sub-components

> `DriftDetector` is an orchestrator. Each sub-component below implements one logical block.

### Sub-component: Extractchangedspecdomains

> Implements: `extractChangedSpecDomains`

Extracts domain names from specification files that were changed in the current changeset.

#### Requirement: Extractdomains

The system SHALL the function shall extract domain names from specification files that were modified in the current changeset.

#### Scenario: ExtractDomainsFromChangedSpecFiles
- **GIVEN** A changeset containing modified specification files for domains 'auth' and 'user'
- **WHEN** The function is called with the changeset and the openspec directory path
- **THEN** The function SHALL return a set containing the domain names 'auth' and 'user'

### Sub-component: Detectgaps

> Implements: `detectGaps`

Detects gaps where code was changed in a domain but the corresponding specification was not updated.

#### Requirement: Detectgaps

The system SHALL the function shall detect gaps by identifying code changes in domains where the corresponding specifications were not updated.

#### Scenario: DetectGapsInCodeChangesWithoutSpecUpdates
- **GIVEN** A changeset with code changes in the 'auth' domain and a specification update in the 'user' domain
- **WHEN** The function is called with the changeset and the specification map
- **THEN** The function SHALL return an array of DriftIssue objects indicating gaps in the 'auth' domain

### Sub-component: Detectstalespecs

> Implements: `detectStaleSpecs`

Detects stale specifications that reference files that were deleted or heavily modified.

#### Requirement: Detectstalespecs

The system SHALL the function shall detect stale specifications by identifying references to files that were deleted or heavily modified.

#### Scenario: DetectStaleSpecsReferencingDeletedFiles
- **GIVEN** A changeset with a deleted file referenced in a specification
- **WHEN** The function is called with the changeset and the specification map
- **THEN** The function SHALL return an array of DriftIssue objects indicating the stale specification

### Sub-component: Detectuncoveredfiles

> Implements: `detectUncoveredFiles`

Detects new files that do not map to any specification domain.

#### Requirement: Detectuncoveredfiles

The system SHALL the function shall detect new files that do not map to any specification domain.

#### Scenario: DetectUncoveredFilesNotMappedToAnyDomain
- **GIVEN** A changeset with a new file that is not mapped to any specification domain
- **WHEN** The function is called with the changeset and the specification map
- **THEN** The function SHALL return an array of DriftIssue objects indicating the uncovered file

### Sub-component: Detectorphanedspecs

> Implements: `detectOrphanedSpecs`

Detects orphaned specifications that declare source files that do not exist on disk.

#### Requirement: Detectorphanedspecs

The system SHALL the function shall detect orphaned specifications by identifying references to files that do not exist on disk.

#### Scenario: DetectOrphanedSpecsReferencingNonExistentFiles
- **GIVEN** A specification that references a file that does not exist on disk
- **WHEN** The function is called with the specification map and the root path
- **THEN** The function SHALL return an array of DriftIssue objects indicating the orphaned specification

### Sub-component: Extractchangedadrids

> Implements: `extractChangedADRIds`

Extracts ADR IDs that were changed in the current changeset.

#### Requirement: Extractadrids

The system SHALL the function shall extract adr ids from files that were changed in the current changeset.

#### Scenario: ExtractAdrIdsFromChangedFiles
- **GIVEN** A changeset containing modified files that include ADRs with IDs 'ADR-001' and 'ADR-002'
- **WHEN** The function is called with the changeset and the openspec directory path
- **THEN** The function SHALL return a set containing the ADR IDs 'ADR-001' and 'ADR-002'

### Sub-component: Detectadrgaps

> Implements: `detectADRGaps`

Detects gaps where code was changed in a domain referenced by an ADR but the corresponding specification was not updated.

#### Requirement: Detectadrgaps

The system SHALL the function shall detect adr gaps by identifying code changes in domains referenced by adrs where the corresponding specifications were not updated.

#### Scenario: DetectAdrGapsInCodeChangesWithoutSpecUpdates
- **GIVEN** A changeset with code changes in a domain referenced by an ADR and a specification update in a
different domain
- **WHEN** The function is called with the changeset, ADR map, specification map, and changed ADR IDs
- **THEN** The function SHALL return an array of DriftIssue objects indicating the ADR gap

### Sub-component: Detectadrorphaned

> Implements: `detectADROrphaned`

Detects orphaned ADRs that reference domains that no longer exist in specifications.

#### Requirement: Detectadrorphaned

The system SHALL the function shall detect orphaned adrs by identifying references to domains that no longer exist in specifications.

#### Scenario: DetectOrphanedAdrsReferencingNonExistentDomains
- **GIVEN** An ADR that references a domain that no longer exists in specifications
- **WHEN** The function is called with the ADR map and the specification map
- **THEN** The function SHALL return an array of DriftIssue objects indicating the orphaned ADR

### Sub-component: Enhancegapswithllm

> Implements: `enhanceGapsWithLLM`

Post-processes gap issues using an LLM to determine if code changes are relevant to the specifications.

#### Requirement: Enhancegaps

The system SHALL the function shall post-process gap issues using an llm to determine if code changes are relevant to the specifications.

#### Scenario: EnhanceGapsWithLlmProcessing
- **GIVEN** An array of DriftIssue objects representing detected gaps
- **WHEN** The function is called with the issues, LLM options, and other parameters
- **THEN** The function SHALL return an array of DriftIssue objects with enhanced information from LLM
processing

### Sub-component: Isspecrelevantchange

> Implements: `isSpecRelevantChange`

Determines if a changed file is relevant for specification drift detection.

#### Requirement: Checkrelevance

The system SHALL the function shall determine if a changed file is relevant for specification drift detection.

#### Scenario: CheckRelevanceOfAChangedFile
- **GIVEN** A changed file that is a specification file
- **WHEN** The function is called with the file and the openspec directory path
- **THEN** The function SHALL return true indicating the file is relevant for specification drift detection

## Technical Notes

- **Implementation**: `src/core/drift/git-diff.ts, src/core/drift/spec-mapper.ts, src/core/drift/drift-detector.ts`
- **Dependencies**: node:child_process, node:fs/promises, node:path, node:util, logger, fs/promises, path, SpecMap, ADRMap, LLMProvider, ChangedFile, DriftIssue, DriftResult, DriftDetectorOptions
